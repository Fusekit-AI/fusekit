# FuseKit Implementation Plan

## Current Status

- [x] Local Python project base exists.
- [x] Dev tooling exists: pytest, Ruff, MyPy.
- [x] Product goal has been reframed around a real-capable detonated setup worker and capability vault.
- [x] MVP architecture modules exist.
- [x] Real-capable provider adapter contracts exist.
- [x] Cipher vault format for created passwords, API keys, SSH keys, DNS settings, provider tokens, and service credentials exists.
- [ ] First real website setup path exists. Local acceptance works; the 2026-06-20 live acceptance gate failed closed because Moonlite has no retrieved `.fusekit/remote-artifacts` bundle yet, so the real provider run still awaits supervised GitHub/Vercel/Cloudflare/Resend/OpenAI/OCI authorization and DNS approval.
- [ ] Hosted universal launcher exists. The open-core contract, trust-first page, browser-visible source repository/license/entrypoint proof, public capability-vault boundary proof, browser-visible selected-repository GitHub token boundary, pre-install GitHub intake trust/open-core/no-terminal/proof/reversal JSON, pre-install GitHub permission-copy drift detection, no-terminal launch-path checklist, public plain-language click-path contract, browser-visible completion proof checklist including recording proof, outside-in homepage trust-story and completion-proof label verification, browser-visible proof-receipt completion evidence checklist, public deployment/intake completion-evidence key drift detection, protected-action and worker-claim receipt evidence-key alignment, browser-visible reversible setup checklist, embedded homepage public contract blocks, GitHub App install helpers, signed callback state, public callback receipt, selected-repository listing page, source fetch/scan, visible hosted plan, browser-visible approved action ids with drift requiring fresh approval, public-safe hosted job/control-room shell, redacted hosted-worker contract with non-secret GitHub App installation id for backend source fetch, browser-visible permission boundary, browser-visible provider gate checklist, signed stateless job handoff, browser-facing proof receipt page, redacted hosted readiness contract with config-shape validation and fail-closed GitHub intake routing, post-start redacted worker-request handoff for live acceptance and remote-artifact proof, backend-only worker-secret claim endpoint with redacted claim receipt, backend-only hosted worker source preparation with internal GitHub App token exchange and approved-plan drift rejection, backend-only hosted worker launch/acceptance invocation with public path redaction, backend-only worker-proof payload assembly from real acceptance/artifact evidence, `fusekit-hosted-worker` one-job claim/run/proof entrypoint, production-required signed hosted-worker dispatch envelope for button-driven worker wakeup, HMAC-verified `fusekit-hosted-worker-dispatch` receiver with env-backed job-token handoff and redacted readiness endpoint, outside-in `fusekit-hosted-verify` deployment verifier with public DNS propagation proof, automatic deployment-contract worker dispatch verification, top-level blocking-check/next-action summary, homepage trust drift detection, homepage GitHub boundary drift detection, homepage completion proof checklist drift detection, homepage reversible-setup step drift detection, parsed homepage embedded-contract drift detection, one-click launch contract drift detection, plain-language click-path drift detection, capability-vault boundary drift detection, deployment/pre-install intake trust drift detection, hosted canonical subdomain contract drift detection, public GitHub token-boundary drift detection, public worker-dispatch contract drift detection, public HTML/JSON credential-text leak detection, public HTTP security-header drift detection, operator setup contract exactness, and Cloudflare Error 1000 DNS diagnosis, redacted protected-action receipts for worker start/rollback/detonation proof expectations, browser-visible latest action/dispatch receipts after protected button clicks, hosted worker maintenance invocation for rollback/detonation requests using existing CLI surfaces, backend-only redacted worker-proof submission endpoint with completion gating, public hosted deployment contract plus operator setup checklist for `fusekit.snowmanai.org`, worker dispatch receiver setup/verification, hardened no-store/security headers, redacted job status API, browser-native protected start/pre-worker stop/rollback/detonation controls, and Vercel/AWS-compatible WSGI entrypoint are in place; production worker operation, full rollback/detonation provider execution proof, hosted custom-domain attachment, and Cloudflare DNS correction still need implementation.
  Hosted launcher controls now avoid dead links: not-ready homepage and launch-plan start states render as disabled non-links, and preview controls link to the relevant trust section.
  Hosted GitHub intake and backend worker source preparation now fail closed if GitHub returns all-repository access or broader installation-token permissions than selected-repository `contents:read` plus GitHub metadata read, and the browser homepage plus public intake/deployment contracts now expose that exact token boundary for outside-in verification.
  Hosted readiness now publishes redacted blocking checks and deduplicated next actions, and the homepage renders that launch-readiness summary while keeping the start button disabled until required config is present and valid.
  The hosted browser UI now includes a public reversal playbook covering pre-worker stop, GitHub App installation revocation/narrowing, rollback proof, and worker detonation proof; known non-secret GitHub installation ids now render an actionable settings link for revocation without exposing installation tokens.
  Hosted protected controls now include a pre-worker stop action that freezes the job before worker claim or provider mutation.
  Hosted worker dispatch is now required before public hosted readiness can enable the one-click launch button; the public operator setup checklist names the worker dispatch receiver deployment, `FUSEKIT_HOSTED_WORKER_DISPATCH_URL` wiring, and external receiver verification steps, and the public deployment verifier discovers the worker receiver from the hosted deployment contract, verifies it automatically when the hosted surfaces pass, and fails closed if the worker-dispatch URL is missing, treated as optional, or not marked production-required/no-terminal wakeup-required.
  Hosted worker dispatch now rejects duplicate protected starts, rollbacks, or detonations for the same origin/job/action with a non-secret workspace/state-dir/process idempotency marker before spawning the worker, public receipts label the exact idempotency mode/scope without leaking paths, readiness requires private non-symlink durable state storage before reporting `production_ready`, and the public verifier requires durable dispatch mode, scope, and reservation-before-spawn proof before production worker dispatch can be marked ready.
  Durable hosted worker dispatch markers now bind the stored non-secret marker to the same origin, job id, action, dispatch binding, and worker id before returning a duplicate receipt, so a replay with a different plan or price-label hash cannot claim accepted duplicate proof without matching the original marker.
  Hosted worker dispatch envelopes and receiver receipts now carry a signed public dispatch binding for job id, action, managed lane, paid status, approved plan fingerprint, and public price-label hash, and both deployment-contract and receiver-readiness verification fail closed if that binding contract drifts, so worker wakeup proof remains tied to the approved paid run without rendering job tokens or worker secrets.
  Hosted worker dispatch binding now also carries and verifies the public Stripe Price hash, so worker wakeup proof stays tied to the same managed-run Price object as the paid Checkout receipt without exposing raw Stripe ids.
  Hosted worker dispatch signed envelopes now reject unexpected top-level and binding fields, so signed worker wakeup requests cannot carry hidden provider tokens, Stripe object ids, or other side-channel data outside the documented public dispatch contract.
  Hosted worker-dispatch spawn receipts now reject boolean process ids as public numeric proof, so `true` cannot render as spawned pid `1` in browser-visible worker wakeup evidence.
  Outside-in worker dispatch readiness verification now rejects unexpected readiness, dispatch-binding, idempotency, or storage fields, so public worker-wakeup proof cannot carry raw logs, state paths, Stripe ids, or other sidecar diagnostics outside the documented contract.
  Outside-in hosted deployment verification now rejects unexpected Cloudflare DNS, DNS dry-run policy, and rollback-requirement fields, so public DNS and reversible-setup proof cannot carry provider zone ids, raw DNS diffs, inventory exports, or other hidden provider sidecars outside the documented redacted contract.
  Hosted worker dispatch JSON responses now carry no-store, no-framing, no-referrer, nosniff, HSTS, disabled-permissions, and default-deny CSP headers just like the hosted launcher control surface.
  The public deployment contract now publishes the required hosted security-header policy, and the outside-in verifier rejects drift between that public contract and the hardened response headers.
  The public deployment contract now also publishes source-integrity proof for the hosted launcher, including the public repository, license, reviewable hosted entrypoint/config files, hosted source modules, public contract endpoints, and the fact that no private generated artifact is required for the hosted click flow; the outside-in verifier rejects drift in that proof.
  The hosted homepage and public deployment contract now publish hosted Git source provenance, and the outside-in verifier rejects deployments that do not expose production provider metadata for the expected GitHub repo, branch/ref, and commit SHA.
  Hosted readiness now fails closed when source provenance is not verified, keeping the public start button disabled until the deployed Vercel or AWS/Git metadata proves the expected open-source repo and commit, and the outside-in verifier rejects readiness endpoint drift for that same proof.
  Hosted homepage copy now comes from the selected deployment provider contract instead of universal hardcoded Vercel text, and the outside-in verifier fails provider-copy drift when an AWS-hosted page leaks Vercel-only operator instructions or a Vercel-hosted page leaks AWS-only operator instructions.
  Hosted launch jobs now carry a public approved-plan fingerprint through the browser job contract, protected action receipts, backend worker request, worker claim receipt, and worker proof receipt, so the visible plan can be matched to the worker handoff and any provider/action/gate/artifact/source drift requires a fresh approval.
  Hosted GitHub source URLs are now normalized to exact public `https://github.com/{owner}/{repo}` form before visible plans, hosted jobs, worker contracts, payment bindings, or decoded public payloads can render them, rejecting credentials, query strings, fragments, non-GitHub hosts, and secret-shaped source text.
  Hosted app names are now normalized as short printable public labels before visible plans, hosted jobs, worker contracts, receipts, or decoded payloads can render them, rejecting secret-shaped markers, control text, and markup-like characters.
  Hosted visible-plan provider labels, env-var labels, action ids, action providers, summaries, and risk labels are now sanitized before launcher rendering or worker-contract decoding, so manifest metadata cannot smuggle token-shaped text into public plans or approved-action proof.
  Hosted public job decoding now rejects unexpected top-level job, step-row, and worker-contract fields and validates that the embedded lane contract exactly matches the selected lane, so signed public job tokens and status APIs cannot carry hidden worker logs, installation tokens, or weakened managed/BYO lane proof outside the documented browser-safe contract.
  Hosted public job serialization now self-scans before browser, status API, or signed-token rendering, so direct job-state drift cannot smuggle token-shaped provider, worker, OCI, AWS, PEM, payment, or vault material into public job JSON.
  Hosted signed job-token envelopes and worker-contract plan-integrity metadata now reject unexpected fields even when the HMAC signature is valid, so public resume/status/proof tokens cannot carry private notes, raw plan JSON, or other sidecars around the exact browser-safe job contract.
  Repository scanning no longer invents Vercel as the default deployment provider: Vercel-specific setup and DNS records are emitted only when Vercel evidence exists, and otherwise the visible plan requires an explicit hosting-provider selection before DNS or rollback assumptions are made.
  BYO OCI bootstrap receipts now publish a machine-checkable AMD/x86_64 runner shape guard and fail closed if the configured disposable worker profile drifts toward ARM shapes or ARM architectures.
  BYO OCI returned proof bundles now verify that same AMD/x86_64 runner shape guard as a first-class redacted proof field, rejecting missing guards, ARM drift, and sidecar image/instance identifiers before marking BYO evidence ready.
  Hosted OCI inventory now emits explicit read-only cost-review proof for non-target running instances, so FuseKit cannot claim a clean/no-orphan tenancy while unrelated Snowman, Mailpilot, or other OCI workloads still require human review.
  Hosted worker requests now publish an explicit secret-boundary statement and self-scan before response rendering, blocking token-shaped GitHub, Stripe, OCI, AWS, PEM, worker-secret, provider credential, payment-method, or vault material from entering the backend worker handoff JSON.
  Hosted protected-action JSON responses now self-scan before returning, and unconfigured worker-dispatch receipts carry the same public secret-boundary proof as successful dispatch receipts, so start/rollback/detonation responses cannot leak token-shaped sidecars while still proving when no managed worker dispatch URL exists.
  Hosted proof receipts now self-scan at the receipt builder before JSON download or HTML rendering, so proof-page drift fails closed instead of rendering token-shaped provider, worker, OCI, AWS, PEM, or vault material.
  Hosted action, worker-claim, and worker-proof receipt builders now also self-scan before returning, so direct receipt use and backend server responses share the same no-token/no-PEM/no-OCI/no-AWS public boundary.
  Hosted job payment status serialization now exact-shapes embedded Checkout receipts before browser, status API, or signed-token rendering, so customer sidecars, payment-method fields, raw Stripe session data, or malformed receipt objects cannot ride inside otherwise public job JSON.
  Hosted job payment status serialization now also verifies the paid status against a complete matching Checkout receipt and rejects payment state on BYO OCI jobs, so public job JSON cannot claim paid managed-run proof or paid-lane readiness from in-memory status drift.
  Hosted managed payment job creation and serialization now require a valid public price label for every payment-required, pending, or paid state, and reject stray price labels on non-payment jobs, so public Checkout readiness cannot hide or drift the cost disclosure.
  Hosted managed payment job creation and serialization now also require the public Stripe Price hash for every payment-required, pending, or paid state, so Checkout proof remains bound to the configured Price object without exposing the raw Stripe Price id.
  Hosted public job decoding now enforces the same managed payment price-label and price-hash invariants before accepting signed job tokens or status payloads, so resumed public state cannot downgrade Checkout cost or Price binding proof.
  Hosted payment-required receipts now fail closed unless the lane is `managed-fusekit-run` and the public price label is valid, so BYO OCI or malformed-cost states cannot emit ambiguous payment-required proof.
  Stripe Checkout paid receipts now fail closed unless Stripe returns a complete public reconciliation shape: paid payment mode, positive amount, 3-letter currency, client reference, managed lane, matching metadata job id, and all source/plan/price/label hashes.
  Hosted payment checkout JSON now uses the same public-response self-scan as protected actions, and payment return/cancel receipts publish explicit card/payment-method/client-secret boundaries, so the paid-lane transition from Stripe authorization to managed worker start cannot leak Stripe or provider sidecars.
  Hosted Checkout handoff now refuses to persist pending payment state or redirect the browser unless Stripe returns an exact public `https://checkout.stripe.com/c/pay/...` payment URL, turning malformed Checkout sessions into a controlled upstream error while the job remains payment-required.
  Hosted negative payment-return responses now publish self-scanned public error receipts with dispatch-blocked proof, the next required Stripe Checkout authorization proof, and explicit card/payment-method/client-secret/raw-session boundaries instead of returning bare error labels.
  Hosted negative Checkout creation responses now use the same self-scanned public payment error receipts, so unready billing config, rejected Stripe receipt shapes, or missing Checkout payment URLs prove dispatch remains blocked without persisting pending payment state or leaking raw Stripe session details.
  Hosted Checkout protected-control failures now also return self-scanned public payment error receipts, so malformed form bodies, missing controls, bad Checkout control tokens, and cross-origin Checkout posts prove dispatch remains blocked without calling Stripe or advancing managed payment state.
  Hosted payment-return and payment-cancel browser action receipts are now built through self-scanning helpers before control-room rendering, preserving the paid-start or cancelled-dispatch proof while excluding card data, payment method ids, client secrets, raw Checkout sessions, Stripe keys, and provider credentials.
  Hosted managed-start payment-required responses now self-scan and publish an explicit boundary before returning the Checkout route, so unpaid managed starts prove worker dispatch is blocked without rendering Stripe secret keys, client secrets, payment method ids, card data, provider credentials, or vault material.
  Hosted managed rollback and detonation requests now use the same paid-receipt gate before managed worker dispatch when the job is otherwise in a dispatch-capable state, so unpaid managed runs cannot consume FuseKit worker infrastructure through maintenance actions while preserving invalid-action failures before worker start.
  Hosted payment-required receipts now self-scan inside the billing helper itself, so any future caller that passes secret-shaped lane or payment metadata fails closed before returning public unpaid-run cost-control proof.
  Hosted home, pre-install GitHub intake/deployment contracts, visible plans, and backend worker requests now publish the same prohibited-action boundary, covering no MFA/CAPTCHA/passkey, billing, fraud, consent, or domain-gate bypass; no raw secret rendering; no DNS or paid-resource mutation without explicit approval; and no completion claim before live acceptance, retrieved artifacts, and detonation proof pass.
  Hosted browser protected actions now require distinct short-lived, action-bound control tokens submitted through URL-encoded POST form fields instead of capability-bearing URLs, query or JSON control parameters are rejected, mismatching browser Origin/Referer headers are rejected when present, and the public deployment contract plus outside-in verifier now reject drift in that protected-control transport and origin policy, so start/stop/rollback/detonation links cannot be replayed indefinitely, across jobs, across protected actions, or from unrelated web origins, and missing-token control rooms render disabled protected controls instead of hiding or faking unsafe actions.
  Hosted job routes now require the signed public job token even when the job is present in process memory; memory is only a state cache after token verification, so a known `hosted-*` id cannot mint fresh job/control tokens or trigger protected actions by itself.
  Hosted rollback maintenance proof is now action-aware and fail-closed: rollback metadata alone cannot complete a rollback request without a rollback execution receipt and explicit post-rollback verification flag in the redacted worker proof.
  Hosted detonation maintenance proof is now action-aware and fail-closed: a detonation request cannot complete from a generic detonation artifact unless the worker also proves workspace detonation receipt, scratch-state destruction, provider-auth session closure, and preserved redacted public proof.
  Hosted worker proof assembly now rejects directory or empty-file placeholders for required public artifacts, requires the retrieved remote-artifacts bundle to contain the complete expected survivor file set without unexpected entries, and preserves empty gate-event streams for launches with no gate wake events.
  Backend worker claim and worker-proof success responses now run the same public payload self-scan used by protected action/payment responses before returning job tokens, worker requests, claim receipts, or proof receipts, so future response drift fails closed instead of rendering token-shaped provider, worker, OCI, AWS, PEM, or vault material.
  Root hosted deployment wiring is now explicit with `vercel.json` routing every Vercel path to `app.py`, `Procfile` starting `gunicorn app:app` for AWS Python WSGI runtimes, `.python-version` pinning Python 3.12, and a minimal launcher `requirements.txt`.
  AWS hosted-launch hardening now has a plan-only guard that emits redacted account/region/tag/IAM/env/DNS/rollback metadata, blocks protected MailPilot/SOC 2 tagged resources, validates only the `fusekit.snowmanai.org` Cloudflare CNAME proposal, keeps Cloudflare Error 1000 repair guidance provider-neutral, and requires AWS provenance to publish a clean HTTPS Elastic Beanstalk origin.
  Hosted universal-launch hardening now also checks wrong-account/wrong-region AWS plans, validates one-record Cloudflare DNS dry-run diffs, exposes provider permission copy and rollback proof requirements in the public deployment contract, verifies those contracts outside-in, emits a top-level readiness summary, and renders trust evidence in public proof receipts.
  Final pre-gate hosted hardening now rejects malformed AWS account ids, invalid allowed-region lists, non-Elastic Beanstalk AWS origin CNAMEs, hidden or malformed Cloudflare DNS dry-run fields, PII-tagged AWS resources, and non-provider Vercel provenance URLs before public readiness can pass.
  Hosted launch selection now has explicit managed and BYO lanes: managed runs publish a Stripe Checkout payment/cost-control contract, block FuseKit-managed worker dispatch until server-side payment verification records a paid redacted receipt bound to the same job, lane, GitHub source hash, plan fingerprint, Stripe Price hash, and public price-label hash, and BYO OCI runs publish a concrete open-core OCI Cloud Shell bootstrap for the selected GitHub source without dispatching FuseKit-managed infrastructure or exporting hosted worker/GitHub private-key secrets.
  Hosted lane readiness is now explicit and fail-closed: public readiness/deployment contracts report managed and BYO lane launchability separately, the plan UI disables the managed lane until Stripe Checkout is fully configured, and managed worker start remains blocked unless the job has a server-verified paid receipt.
  Managed-lane Stripe Checkout creation and retrieval now reject private OCI, GitHub, Stripe, AWS, and PEM markers in pre-network job/source/token/session bindings, and the hosted payment-return route proves private-looking session ids are rejected without a Stripe lookup or echoed error body, so FuseKit cannot send private-looking identifiers into Stripe metadata, return URLs, or session lookup paths.
  Managed-lane payment checkout and cancellation are now lane-gated: BYO OCI jobs receive a redacted `payment_not_required` error instead of managed Stripe checkout or cancellation receipts, preserving the free/open-core cost boundary and avoiding false managed-payment state on user-owned OCI runs.
  Managed-lane Stripe Checkout normalization and hosted payment receipt persistence now reject private OCI, GitHub, Stripe, AWS, and PEM markers in public receipt and metadata fields before receipt storage or browser rendering, so malformed paid-lane data cannot bypass the generic secret detector.
  Direct hosted control-room URLs now enforce the same lane readiness gate before exchanging GitHub installation tokens or fetching source, so an unlaunchable managed lane returns a redacted `lane_not_launchable` response instead of creating a job or doing provider work.
  Lane readiness now publishes launchable lane ids and a recommended lane, so live hosted deployments can point users to BYO OCI while paid Managed FuseKit runs are still waiting on Stripe price configuration.
  The outside-in hosted verifier now also validates lane readiness cost-policy and secret-boundary statements, blocking deployments that stop proving the paid-receipt requirement, BYO no-managed-infrastructure boundary, or no Stripe/GitHub/worker/OCI/vault secret rendering guarantee.
  Paid managed runs now also require a public `FUSEKIT_MANAGED_RUN_PRICE_LABEL` before the managed lane can launch, so the browser shows a human-readable price disclosure before Stripe Checkout without exposing Stripe price ids or secret keys.
  BYO OCI bootstrap receipts and public launch-lane deployment contracts now publish a structured user-owned cost boundary and BYO security contract, making clear that the user's OCI tenancy owns spend, FuseKit-managed worker dispatch is disabled, hosted secrets are not exported, AMD/x86_64 workers are required, and completion still requires the full redacted proof set.
  BYO OCI bootstrap self-validation now also fails closed if the generated Cloud Shell handoff stops targeting Oracle Cloud Shell, drops or prefix-spoofs the exact visible open-core `fusekit` package or selected GitHub repo binding, drops the `oci-existing` runner, omits the AMD `VM.Standard.E5.Flex` shape or noVNC runner, or tries to launch hosted-worker/recording flows inside the bootstrap command.
  BYO OCI Cloud Shell handoff validation now requires the bootstrap command's `app_source=` assignment to exactly match the canonical selected GitHub repository URL, and rejects branch/tree/blob path variants instead of reducing them to a repo slug.
  BYO OCI bootstrap public self-scanning now uses the shared BYO private-marker detector, including underscore-normalized OCI identifiers and AWS/Stripe restricted-key markers, so fallback instructions cannot leak provider inventory labels that avoid the older dot-form token scan.
  BYO OCI bootstrap denial paths now return self-scanned public error receipts with the same user-owned cost boundary, disabled managed-worker dispatch proof, AMD-only security contract, and no hosted-secret/export boundary instead of bare route errors.
  BYO OCI protected start, rollback, and detonation actions now all publish an explicit no-managed-dispatch receipt and prove the FuseKit worker-dispatch opener is unused, so user-owned OCI runs cannot silently fall back to managed infrastructure during reversible setup or detonation.
  Hosted browser-embedded JSON contracts now use a shared script-safe JSON encoder instead of HTML-escaping, so launcher plans, GitHub intake, readiness, deployment, BYO bootstrap, control-room job state, and proof receipt scripts remain directly parseable while still neutralizing tag/script-breakout content.
  The outside-in hosted verifier now rejects homepage embedded contracts that only parse after HTML entity unescaping, so deployments cannot drift back to `&quot;`-escaped script JSON while still passing trust/readiness checks.
  The outside-in hosted verifier now also rejects duplicate homepage embedded contract scripts, so stale or malicious GitHub intake, readiness, or deployment JSON blocks cannot hide behind a later valid block while preserving trust/readiness status.
  The outside-in hosted verifier now redacts secret-shaped unexpected field names before publishing contract blocker codes, so malformed readiness/deployment JSON cannot leak token-like keys while being rejected.
  BYO OCI proof return now has a repo-native redacted bundle verifier contract: the bootstrap names the input/output schemas, and `verify_hosted_byo_oci_proof_bundle` fails closed unless returned artifact paths, labels, SHA-256 labels, redaction flags, and completion-evidence booleans match the manifest without exposing artifact contents or provider secrets.
  BYO OCI proof bundles now reject unexpected top-level, job-binding, artifact-row, and completion-evidence sidecar fields, so returned redacted proof cannot quietly carry worker OCIDs, local paths, console-session flags, raw logs, or other hidden state outside the public manifest.
  BYO OCI proof sidecar blocker labels now redact secret-looking field names and reject secret-looking artifact paths before public report serialization, so malformed returned proof still yields a redacted verifier report instead of leaking key-shaped text.
  BYO OCI proof artifact rows now explicitly block and redact secret-shaped artifact labels or SHA-256 fields before public report serialization, so malformed proof inventories cannot use otherwise-valid artifact paths as a side channel for provider tokens, Stripe keys, OCI ids, or private material.
  BYO OCI proof job-binding values now reject the same private OCI, GitHub, Stripe, AWS, and PEM markers used by artifact/path checks before public report serialization, so replay or source-binding drift cannot echo provider identifiers while being rejected.
  Hosted worker proof notes now reject BYO private markers such as OCI ids, GitHub tokens, Stripe keys, AWS keys, and PEM markers at the field boundary before a public proof receipt is built, keeping receipt self-scans as defense in depth instead of the first line of note validation.
  BYO OCI proof reports now also suppress mismatched artifact labels and invalid SHA-256 strings after recording the blocker, so public proof summaries keep only expected labels and valid hash labels instead of echoing arbitrary returned text.
  BYO OCI proof verification now rejects zero-byte required survivor artifacts except for the explicitly empty-allowed gate-event stream, so a returned remote-artifact inventory cannot satisfy Run Record, acceptance, rollback, or detonation proof with empty placeholders.
  BYO OCI proof summaries now count only valid required artifacts as present and publish invalid required artifact paths separately, so a redacted bundle with the right path but a bad hash, wrong label, missing redaction flag, invalid size, or empty required receipt cannot overstate proof completeness.
  BYO OCI proof and Stripe Checkout payment evidence now reject boolean values where byte counts or payment amounts are required, so `true` cannot be interpreted as a one-byte artifact or one-cent paid-run proof through Python's bool-as-int behavior.
  BYO OCI proof bundles now also have to echo the hosted bootstrap's user-owned cost boundary and BYO security contract before verification can pass, so returned proof preserves the Oracle-spend owner, no managed-worker dispatch, no hosted-secret export, and AMD/x86_64-only runner promises.
  BYO OCI proof cost/security contracts now reject unexpected sidecar fields and only publish exact hosted-manifest contract bodies, so region/session/resource hints cannot ride inside public proof reports.
  BYO OCI proof cost/security contract validation now also uses the shared BYO private-marker scan before exact matching, so underscore-form OCI ids, AWS keys, Stripe restricted keys, or PEM markers fail closed as unsafe proof data.
  BYO OCI proof bundles are now bound to the originating hosted job id, lane, GitHub source hash, and approved plan fingerprint, and the browser-visible proof-return verifier contract names those required binding fields, so a redacted artifact inventory cannot be replayed across another hosted run or source selection to claim completion.
  BYO OCI proof job bindings now require source and plan hash fields to be exact `sha256:` labels before they appear in public verifier output, so malformed binding hashes become explicit blockers instead of being echoed as proof.
  Direct BYO OCI proof verification now fails closed before artifact or evidence parsing when invoked for a Managed FuseKit Run job, so a valid returned BYO bundle cannot satisfy paid managed-worker completion gates or receive proof credit on the wrong lane.
  BYO OCI proof reports now publish only the exact supported proof-bundle schema as `input_schema_version`; malformed or secret-shaped schema labels still fail verification but are not echoed into public reports.
  BYO worker-proof completion now enforces that proof-bundle verifier: even complete live-evidence flags and required artifact labels leave the hosted job in `proof_submitted` until the returned redacted BYO OCI proof bundle verifies cleanly.
  Hosted worker proof payloads are now exact-shape at the top level before receipt rendering, and `byo_oci_proof_bundle` is accepted only for the BYO lane, so raw logs, worker-local sidecars, or user-owned OCI proof bundles cannot be silently ignored on managed runs.
  Hosted worker claim and proof receipts now collapse secret-shaped worker ids to a generic public label before browser rendering, so backend worker labels cannot leak GitHub tokens, Stripe key-shaped text, OCI identifiers, or PEM markers into job steps or receipts.
  Hosted proof receipts and worker-proof receipts now carry the same redacted lane contract, so saved proof keeps the BYO/managed cost, worker-owner, and secret-boundary story attached after job start and completion.
  Hosted worker proof completion now requires both `recording_ready` and `recording_proof_ready` from the live acceptance report before setting the public `recording` evidence flag, so a launch cannot claim complete recording proof from a readiness-only acceptance summary.
  Stripe Checkout creation and verified-return receipts now both carry the same public managed-run price label, so the paid lane's cost disclosure survives redirect, payment proof, and worker dispatch without exposing Stripe price ids or secret keys.
  Stripe managed-run Product names, Product descriptions, and public price labels are now sanitized as display-safe FuseKit-scoped text before setup, reuse, or verification can pass, so a shared Snowman AI Stripe account cannot accidentally reuse or publish secret-shaped or markup-like public object labels.
  Hosted paid job state now requires paid Checkout receipts to bind back to the same hosted job id, managed/BYO lane, GitHub source hash, approved-plan fingerprint, and public price-label hash before persisted job tokens or control-room state can deserialize as paid.
  Hosted paid job state now also carries the expected non-secret Stripe Price hash and rejects paid receipt persistence or signed job-token decode when returned Checkout metadata binds to a different price hash, so paid proof cannot be replayed across managed-run price objects without exposing raw Stripe ids.
  Stripe Checkout receipts now fail closed if the redacted receipt object still contains durable secret-looking text, so Stripe session metadata or other provider-returned fields cannot be persisted into hosted job state as raw secret material.
  Stripe Checkout receipts now only preserve and redirect to exact Stripe-hosted Checkout payment URLs under `https://checkout.stripe.com/c/pay/`, redacting arbitrary same-host Stripe URLs, fragments, and userinfo before receipt storage.
  Stripe Checkout receipts now also reject query-bearing Checkout URLs and secret-shaped metadata before public receipt serialization, so client-secret-like URL parameters or malformed provider metadata cannot become public paid-lane proof.
  Stripe Checkout creation now validates the outbound public binding before any Stripe API call: only the managed lane may create Checkout, job ids must be public-safe, GitHub source text must be the canonical public GitHub origin form without secret-shaped text, and plan fingerprints must be exact `sha256:` labels, so malformed local state cannot be sent to Stripe as paid-lane reconciliation metadata.
  Stripe Checkout creation now also validates the return/cancel origin inside the billing helper as an exact HTTPS origin with no path, query, fragment, params, or userinfo before any Stripe API call, so malformed hosted config cannot create ambiguous payment redirects.
  Hosted signed state/job tokens and decoded job payloads now reject boolean timestamp fields, so `true` cannot stand in for issued-at or created-at proof in browser-carried launch state through Python's bool-as-int behavior.
  Hosted Cloudflare DNS dry-run TTLs and GitHub installation ids now reject booleans as numeric proof, so DNS propagation plans and selected-repository worker contracts cannot quietly treat `true` as TTL/id `1`.
  Hosted Stripe price setup and verification now reject boolean cent amounts from both operator input and Stripe Price payloads, so paid Managed FuseKit Run cost controls cannot mistake `true` for one cent.
  Managed worker dispatch now requires the server-returned payment receipt to be an exact paid Stripe Checkout payment receipt with hosted payment schema/provider, `mode=payment`, `status=complete`, `payment_status=paid`, a public `cs_` session id, positive amount, public currency, and matching job-bound metadata.
  Stripe Checkout metadata is now exact-shape only at both the live receipt and hosted job persistence layers, so unexpected Stripe metadata keys cannot be silently dropped or used as hidden side-channel state in paid managed-run proof.
  Stripe Checkout live receipts now also require all public reconciliation metadata hashes to be exact `sha256:` labels before receipt serialization, so malformed source, plan, price, or label bindings cannot be echoed as paid-lane proof.
  Stripe managed-run Product and Price setup/verification now require exact FuseKit metadata, so shared-account objects with extra Snowman/MailPilot sidecar metadata cannot be reused or verified as managed-run billing resources.
  Hosted payment receipt persistence is now exact-shape at the top level too, so Stripe payment method ids, customer fields, or other unexpected receipt keys cannot be silently discarded into an apparently valid paid managed-run proof.
  Durable hosted payment receipts now require all Checkout reconciliation hash metadata to be exact `sha256:` labels before a job can persist or decode as paid, so malformed Stripe price/source/plan/label hashes cannot ride inside signed job state as paid-run proof.
  Hosted job payment state now uses the same paid Checkout shape below the server-return path: a `paid: true` stub or malformed public payment payload can persist only as pending, and decoded public jobs cannot claim `paid` without a complete Checkout receipt shape.
  Hosted job payment receipts now normalize any pending or malformed `paid: true` stub back to `paid: false` unless the receipt has the complete paid Stripe Checkout shape, so public job/status payloads cannot display paid proof before server-verified Checkout authorization.
  Stripe return handling now requires the complete Checkout metadata key set before a receipt can be treated as paid for the managed lane, so server return verification, worker dispatch, and decoded hosted job state share the same job/source/plan/price/label binding shape.
  Stripe Checkout receipt metadata now rejects malformed allowed values instead of silently dropping them: job/lane/hash fields must be non-empty strings without padding, and hash fields must remain exact `sha256:` labels before any managed-run receipt can be persisted.
  Live Stripe Checkout receipt normalization now applies that same metadata exactness before server return handling: present job/lane/hash metadata values must be public non-empty strings without padding, and malformed values fail before a receipt can become paid proof.
  Managed-run paid proof now requires exact public Stripe Checkout session ids at both durable job-state and server-dispatch checks, so padded or malformed `cs_` values cannot satisfy the paid-run gate.
  Stripe Checkout receipt normalization now keeps browser redirect URLs only when the Stripe-hosted `/c/pay/...` path id exactly matches the normalized public Checkout session id, so a valid-looking URL cannot drift from the paid-run reconciliation session.
  Durable hosted job payment receipts now enforce that same Checkout URL/session binding before persistence or token decode, so stored paid-state proof cannot keep arbitrary Stripe URLs, query-bearing client-secret URLs, or mismatched Checkout links.
  Durable and server-side managed-run paid checks now require normalized lower-case 3-letter Stripe currency labels, matching the live receipt normalizer and preventing uppercase or padded currency drift from satisfying paid proof.
  Server-side managed-run payment return and dispatch gates now also require Checkout hash metadata to be exact `sha256:` labels before matching expected job/source/plan/price bindings, aligning live return proof with durable job-state paid proof.
  Public hosted readiness verification now rejects unexpected lane, managed-lane, BYO-lane, payment, payment-cost-control, and payment-operator fields, and it verifies the Stripe cost-control session binding and zero-unverified-spend policy, so the launch button cannot be marked ready while hiding Stripe ids, OCI ids, weakened spend controls, or operator sidecars in readiness JSON.
  Hosted verification now accepts staged managed-run billing setup where a public Stripe price label and price id are configured but managed worker dispatch remains disabled, so operators can prepare Checkout without accidentally opening a public paid lane.
  Public Managed FuseKit runs now require live-mode Stripe billing before launchability; test-mode Stripe keys can stage Checkout configuration, but cannot enable public managed worker dispatch unless an explicit test-mode override is set for non-production tests.
  BYO OCI launch-lane contracts and bootstrap receipts now publish the exact AMD/x86_64 runner profile used in the Cloud Shell launch command, so public proof binds the user-owned worker lane to a non-ARM OCI shape before provider work starts.
  BYO OCI bootstrap JSON now includes a structured handoff preflight and reversibility contract before Cloud Shell handoff, including Oracle billing acknowledgement, no FuseKit fee, AMD shape confirmation, human-owned gate boundaries, required redacted proof return, cleanup delete targets, survivor artifacts, and the workspace detonation receipt requirement.
  BYO OCI bootstrap now also publishes a structured proof manifest with the remote-artifacts bundle root, labeled required survivor files, exact live acceptance gate, completion-evidence names, and a no-secret artifact boundary before the user opens Cloud Shell.
  BYO OCI bootstrap generation now fails closed if the public handoff payload contains GitHub tokens, Stripe keys, private-key PEM material, OCI OCIDs, or AWS access-key markers before the JSON or browser handoff can be returned.
  BYO OCI bootstrap now content-negotiates: browser clicks render a human-readable Cloud Shell handoff page with the same preflight, cost, proof-return, and detonation contract, while `format=json` preserves the redacted machine contract.
  BYO OCI readiness now carries the same user-owned cost boundary and security contract as the launch-lane contract, and the outside-in verifier blocks launchability if the readiness endpoint omits the Oracle-owned spend boundary, AMD-only runner proof, or no-managed-worker secret boundary.
  Shared-account Stripe setup is now repo-native: `fusekit-hosted-stripe-price` dry-runs by default, reads the Stripe secret only from env, requires `--execute --confirm-shared-account` before mutation, creates only FuseKit-scoped Product/Price objects, and prints redacted runtime env values while keeping managed dispatch disabled until live proof passes.
  Hosted payment readiness now publishes and verifies that same Stripe setup contract, including the dry-run default, required mutation flags, shared Snowman AI account boundary, and live-proof gate before `FUSEKIT_MANAGED_RUNS_ENABLED=1`.
  Managed-run price labels now require both a numeric amount and an explicit public currency marker, and the shared Stripe setup docs use single-quoted dollar labels so shell expansion cannot silently turn `$1.00` into an ambiguous `.00` public price.
  Managed-run Stripe setup now rejects mutations when the public price label does not match the configured Stripe amount and currency, so the browser-visible cost disclosure cannot drift from the Checkout charge.
  Managed-run Stripe price verification is now repo-native: `fusekit-hosted-stripe-price-verify` retrieves the configured Price/Product from Stripe, requires the active one-time amount, currency, lookup key, FuseKit metadata, product scope, and public label hash to match, and emits redacted proof before `FUSEKIT_MANAGED_RUNS_ENABLED=1`.
  Managed-run Stripe operator setup now publishes and verifies both installed console scripts and source-checkout `python -m` fallbacks, so shared-account price creation and verification remain explicit even before the package entrypoints are installed.
  Managed-run Stripe setup now performs a lookup-key read before create: reruns reuse an existing active FuseKit-scoped Price/Product without mutation, while a lookup key occupied by non-FuseKit metadata halts setup before touching another Snowman AI product.
  Hosted payment readiness now publishes and the outside-in verifier requires that Stripe lookup-key policy, so the browser-visible contract proves the shared-account read-before-create rule instead of relying on operator memory.
  Live shared-account Stripe setup has a verified FuseKit-scoped Price: `price_1ToydUPZlsTa6iL323anyggA` with public label `Launch validation: $1.00 FuseKit managed run`, lookup key `fusekit_managed_run_usd_100_5ac1e91d12201244`, active one-time USD 100-cent amount, matching FuseKit metadata, and Product `prod_Uobr8h5dUxv5vr`; managed dispatch remains disabled until live Checkout proof and worker-dispatch acceptance pass.
  The outside-in hosted verifier now validates lane readiness, including the recommended lane and launchable lane list, so public deployments fail verification if the BYO recommendation or paid-lane blocker state drifts.
  Release proof can now require an exact deployed commit with `fusekit-hosted-verify --expected-commit-sha`, so a healthy public OCI launcher still fails verification when it is serving stale source provenance.
  OCI host posture now preserves the redacted hosted verifier blocker ids, including `hosted.expected_commit`, so stale release provenance is visible in posture evidence instead of collapsing into a generic web-verification failure.
  OCI inventory evidence now publishes read-only running-instance counts, target-match counts, non-target running-instance counts, the exact hosted-launcher tag selector, and a no-remediation cost-visibility policy so operator cleanup/cost review can see when unrelated running compute exists without exposing OCIDs or mutating OCI.
  OCI inventory collection-failure rows now publish only allowlisted read-only operation labels, so malformed SDK failure metadata cannot leak token-like operation names into cost/posture reports.
  OCI inventory and replacement planning now reject boolean running-instance and target-match counts, so `true` cannot prove exactly one FuseKit-tagged host or one running instance during hosted posture/cost evidence.
  OCI host posture now rejects sidecars under DNS propagation and rollback metadata proof, so Cloudflare zone ids, raw DNS output, provider inventories, or operator notes cannot hide inside launch-readiness evidence.
  OCI host posture now redacts secret-shaped unexpected field names before publishing shape blockers, so malformed evidence cannot leak token-like keys while being rejected.
  OCI host posture now requires DNS propagation proof to include explicit literal `propagated: true` or `ready: true` evidence for `fusekit.snowmanai.org`; a status-only `ok`/`pass` DNS stub no longer satisfies live launch readiness.
  OCI host posture now binds DNS propagation proof to both the canonical hosted origin and exact `fusekit.snowmanai.org` hostname, so origin-only or hostname-only DNS reports cannot satisfy launch readiness.
  OCI host posture now requires rollback metadata provider actions to target exact `fusekit.snowmanai.org`, so unrelated Cloudflare/GitHub/Vercel/Resend rollback plans cannot satisfy reversible-setup proof.
  OCI host posture now requires the collection boundary to explicitly say the collector does not request OCI credentials, so wording that mentions or requests OCI credentials cannot pass as non-mutating local-host evidence.
  OCI hosted redeploy access is now plan-only and evidence-backed: `fusekit-hosted-oci-access-plan` checks the FuseKit-tagged AMD launcher instance, public VNIC, SSH probe status, OCI Run Command plugin status, and hosted expected-commit report without mutating OCI or host state, surfacing `oci_deploy_access_unavailable` before release claims.
  OCI hosted redeploy access now publishes a deploy-access repair contract that allows only enabling Run Command on the single FuseKit-tagged launcher or installing the approved SSH key for the single `fusekit` host user, while forbidding DNS, MailPilot/AWS, billing, generated-app credentials, tenancy-wide admin broadening, and ARM shape changes.
  OCI hosted redeploy access now accepts available-plugin evidence too, distinguishing missing Run Command evidence from an image where Run Command is not available, so repair guidance switches to the approved SSH key or a replacement FuseKit-tagged AMD host instead of recommending an impossible plugin enablement.
  OCI hosted redeploy access now emits a redacted `release_action` receipt that names the live commit, expected commit, stale/current state, allowed deploy paths, safe next action, and post-deploy verifier command before any host mutation.
  OCI hosted inventory is now repo-native: `fusekit-hosted-oci-inventory` uses read-only OCI SDK APIs to find exactly one FuseKit-tagged AMD launcher, redacts OCIDs and credential metadata, collects public VNIC/image/instance-agent plugin evidence, embeds the non-mutating access plan, and fails closed if the target is missing or ambiguous.
  OCI hosted replacement planning is now repo-native and non-mutating: `fusekit-hosted-oci-replacement-plan` consumes the redacted inventory/access evidence, requires a current deploy-access blocker before replacement can be requested, enforces AMD/x86_64 and supported Ubuntu candidates, requires replacement Run Command or approved SSH access, keeps the old host and DNS in place until verifier/posture/release/rollback proof pass, and forbids MailPilot/AWS, Stripe, generated-app/provider credential, tenancy-wide policy, and ARM repairs.
  OCI host posture now requires the embedded hosted-verifier proof to carry the official verification schema and public-only secret boundary before live URL readiness can pass, so a hand-written `ready: true` status stub cannot satisfy the host gate.
  OCI host posture now requires hosted-verifier readiness consistency too: `ready: true` must come with empty blocking checks, a launchable readiness summary, zero literal blocker count, no blocker rows, and no next actions before live URL proof can pass.
  OCI host posture now requires hosted-verifier check-row consistency as well: the embedded verifier proof must include at least one public check row and every row must be `status: ok`, so a summary-only or failed-endpoint report cannot pass live URL posture.
  OCI host posture now also requires the embedded hosted-verifier proof to include the full public launch check set: hosted DNS, homepage, health, readiness, deployment contract, exact commit, GitHub intake, worker-dispatch DNS, worker-dispatch health, and worker-dispatch readiness, and rejects duplicate check ids so a shallow health-only excerpt cannot claim launch readiness.
  OCI host posture now rejects sidecars under the embedded hosted-verifier envelope, readiness summary, check rows, and compact source-provenance fallback, so raw deployment payloads, HTTP/header dumps, response bodies, summary diagnostics, or provenance env data cannot hide inside live URL readiness proof.
  Hosted runtime secret-file readiness is now repo-native and redacted: `fusekit-hosted-runtime-secret-plan` validates the env names needed for `/etc/fusekit/hosted-secrets.env`, dry-runs by default, writes only with `--execute`, allows explicitly generated hosted state/worker secrets without emitting them, reports live Stripe staging readiness from public Price id/label only, and verifies an already-written secret file by metadata and key inventory without emitting values; the OCI replacement plan keeps `ready_for_dns_cutover=false` until a ready `--verify-file` report is attached, so an install receipt alone is no longer cutover proof.
  Hosted runtime secret verification now treats unexpected EnvironmentFile keys as blockers, so a hand-edited host file cannot be considered ready while carrying extra provider credentials, OCI material, or behavior-changing env values outside the hosted allowlist.
  Hosted runtime secret verification now redacts secret-shaped unexpected EnvironmentFile key names before publishing blockers or key inventory, so malformed host files cannot leak token-like env names while being rejected.
  OCI host posture now verifies runtime-secret key inventory counts as literal integers aligned to the allowlisted hosted env set, so boolean or mismatched count proof cannot satisfy DNS cutover or payment-staging readiness.
  OCI host posture now also requires every allowlisted hosted runtime env name to carry literal `present: true` proof, so matching inventory totals cannot hide a missing GitHub, Stripe, state, worker, or origin readiness row.
  OCI host posture now rejects runtime-secret verifier sidecars under key inventory or per-env rows, so public host proof cannot carry raw env lines, extra provider keys, or hand-edited diagnostics outside the redacted contract.
  OCI host posture now verifies redacted Stripe runtime env rows for live secret-key mode, public Price id/label proof, and disabled managed-run state, and rejects sidecars under those rows so paid-lane staging proof cannot carry raw Stripe values or hidden webhook/provider keys.
  OCI host posture now rejects sidecars under the runtime-secret verifier's public secret-file metadata too, so raw `stat` output, uid/gid diagnostics, local paths, or host-specific debug notes cannot be hidden inside DNS-cutover proof.
  OCI host posture now validates that the runtime-secret verifier's own public file metadata matches the production secret file, regular non-symlink status, `0600` owner-only mode, root ownership, and private parent directory before DNS cutover can pass.
  OCI host posture now requires that redacted hosted runtime secret verifier report too, and blocks readiness if the verifier schema is missing, not ready, has blockers, missing required keys, unexpected keys, payment-staging drift, or a weak secret-boundary statement.
  OCI host posture now requires dedicated `rkhunter` or `chkrootkit` rootkit proof instead of allowing the CIS/Lynis baseline summary to double-count as rootkit evidence.
  OCI host posture now rejects sidecar fields in CIS baseline and rootkit scanner summaries, so public posture proof can carry only scanner, status, and high/critical finding counts instead of raw scanner logs.
  OCI host posture now requires literal zero high/critical CIS finding counts, so missing counts or boolean values cannot satisfy hosted host-hardening proof.
  OCI hosted release templates now use `/opt/fusekit/current` as the rollback symlink, publish a narrow exact-commit release script, update only non-secret provenance outside the secret env file, restart only the two FuseKit services, and emit a redacted release receipt for post-deploy verification.
  OCI host posture now validates that redacted release receipt, including the allowed mutated paths, restarted FuseKit services, rollback mode, public commit, release directory, secret boundary, and post-deploy verifier command, and blocks readiness if the receipt commit does not match the hosted verifier's deployed commit proof.
  Hosted deployment provider selection now fails closed instead of silently defaulting to Vercel: Vercel is inferred only from Vercel system metadata, OCI/AWS remain explicit, and missing or unsupported providers publish neutral setup/provenance blockers without leaking Vercel-specific DNS/runtime instructions.
  The outside-in hosted verifier now mirrors that provider-neutral contract: unknown provider deployments expect only the generic setup step and provider-selection provenance env, fail on `source_provenance_provider_mismatch`, and do not fall back to Vercel runtime, operator, or source-env expectations.
  Hosted lane selection now fails closed below the route layer too: invalid lane ids cannot silently fall back to the paid Managed FuseKit lane when building jobs, worker contracts, or decoding public job payloads.
- [x] Security invariants are enforced by tests.
- [x] CLI defaults to real-provider execution; incomplete local rehearsals require explicit opt-in.
- [x] FuseKit bootstraps its own runtime components instead of assuming Codex/OpenClaw is preinstalled.
- [x] FuseKit captures provider-agnostic LLM configuration, defaulting to OpenAI-compatible `gpt-5.5`.
- [x] Add one-click `install.sh` that creates a local FuseKit environment and app launch entrypoint.
- [x] Runtime bootstrap downloads OpenClaw installer through Python, not a preinstalled `curl`.
- [x] Runtime bootstrap verifies downloaded components with OpenClaw version and doctor checks before launch continues.
- [x] Human gates are resumable wait checkpoints with retry/re-handoff loops instead of terminal failures.
- [x] OpenAI/OpenClaw authorization is the default fallback LLM lane when no API key is supplied.
- [x] OCI clean-room runner lane has a concrete implementation design.
- [x] Runner broker, resumable job state, and local control-room artifact exist.
- [x] Live control-room server, leak scanner, rollback/start-over, and remote runner loop exist.
- [x] Control-room UI is polished with progress, current/next focus, human-gate emphasis, live refresh, and artifact copy actions.
- [x] SnowmanAI branding and state-specific snowman mascot animations are applied to the control room.
- [x] `launch --runner auto` enters OCI authorization inline instead of requiring a separate runner authorization command.
- [x] `launch --runner auto` now defaults to OCI Cloud Shell deeplink when no local OCI profile/config exists.
- [x] FuseKit prompt gates are opt-in through `--fusekit-gates explicit`; default launch uses service-created gates only.
- [x] Provider capability packs can be synthesized, validated, authorized, and planned for detected services without built-in adapters.
- [x] Provider capability packs now carry executable verification recipes for env, HTTP JSON, DNS, and live URL health checks.
- [x] Provider capability packs now carry setup recipes, with GitHub, Vercel, and Cloudflare routed through the capability recipe runtime.
- [x] Acceptance harness exists: redacted ledger, content-addressed artifact snapshots, and rehearsal/live launch-readiness reports.
- [x] Public acceptance target exists: `examples/moonlite-rsvp` is a party RSVP app that activates GitHub, Vercel, Cloudflare DNS, Resend, webhook secret, vault, verification, and detonation proof.
- [x] Public acceptance target has a polished RSVP surface for launch-recording use across desktop and mobile.
- [x] P0 North Star audit fixes: pack secret fan-out is route-limited, explicit app-env secret routes reject provider/runner/LLM auth material, secret-bearing HTTP verification targets are provider-domain constrained, and provider pack synthesis now has an LLM-capable intelligence loop with validation/repair/cache.
- [x] P1 North Star audit fixes: provider-pack setup execution uses a handler registry, unknown required verification/setup recipes fail instead of silently skipping, OCI remote launches replay non-secret launch/app context, and verification polling can return pending.
- [x] P2 North Star audit fixes: service gates have durable persisted state, provider-pack rollback intent can be planned directly, verification retry knobs are exposed in CLI/remote launch, and OpenClaw bootstrap supports version/hash pinning.
- [x] Post-audit bug pass: OCI VM bootstrap now installs OpenClaw on the executable PATH, OpenClaw wait snapshots use the correct command shape, provider verification treats pending live checks as incomplete unless explicitly allowed, and launch-readiness artifacts use product language while preserving compatibility aliases.
- [x] Product-surface hardening pass: public code/docs now use launch-ready capability wording, provider intelligence refuses silent vault downgrade, capability packs no longer duplicate schema fields, and OCI detonation reports provider delete failures instead of swallowing them.
- [x] Live-run readiness bug pass: remote uploads exclude additional secret-bearing config/key files, artifact retrieval fails loudly on missing/invalid archives, failed OCI remote launches still attempt workspace detonation, runner env overrides reject unknown lanes, and job status preserves failed state after cleanup.
- [x] Vercel deployment hardening: project creation can connect the GitHub repository from normal `owner/repo` input, and production deployments can trigger from GitHub org/repo when Vercel's internal repo id is not available.
- [x] Zero-knowledge launch defaults: FuseKit derives GitHub repo slug, Vercel project name, live URL, and DNS zone from the app source, manifest, or git remote so users do not need provider-specific vocabulary.
- [x] Human-gate UX hardening: terminal handoff and control-room waiting states now show plain-language provider gate cards with exact human actions, reassurance that FuseKit keeps waiting, and no raw secret material.
- [x] Live control-room gate hardening: served control-room payload now includes durable `gates.json` records so active provider gates surface even when the broader setup step is still running.
- [x] Gate-state loss hardening: corrupt gate state now surfaces as a repairable control-room error, and remote artifact retrieval preserves `.fusekit/gates.json` alongside receipts, vault, audit, and job state.
- [x] Comfort/noise hardening: control-room refresh and clipboard failures now surface as calm, repairable UI states instead of silent no-ops, and the launcher validates public GitHub HTTPS app sources while using Git-free package/app download fallbacks where possible.
- [x] Computer-use action-plane hardening: OpenClaw now uses interactive compact JSON snapshots and real wait commands, inferred navigation records before/after observations, starts/stops traces when supported, rejects unsafe navigation/key presses, resurfaces provider gates, and writes redacted recovery events instead of failing silently.
- [x] Verification repair loop: failed or pending provider-pack verification can now trigger a bounded inferred UI repair pass, feed redacted verification errors into the computer-use goal, rerun verification afterward, and record repair evidence in audit/receipt artifacts.
- [x] Human-attention guidance: inferred service gates can now highlight the provider-screen target that needs human attention, and the control room surfaces provider resume links plus resurfacing counts for waiting gates.
- [x] Static control-room hardening: saved control-room HTML now embeds durable gate state and corrupt-gate errors just like the live server, so provider waits remain visible even when opened as a file artifact.
- [x] OCI launcher copy hardening: Cloud Shell launcher clipboard failures now select the bootstrap command and explain the manual copy fallback instead of failing silently.
- [x] OCI/runtime bootstrap hardening: `doctor` now verifies OpenClaw version, doctor, and browser status instead of accepting a merely present binary; Cloud Shell bootstrap retries transient installs and can download public GitHub app sources without Git; OCI VMs use a dedicated Python venv, persistent FuseKit/OpenClaw PATH exports, and clearer remote command failures.
- [x] OpenClaw capability hardening: browser snapshots now use efficient JSON payloads by default, optional label overlays are available for human targeting, diagnostics use fresh cleared errors/requests, inferred UI navigation can use rich wait conditions, provider gates scroll targets into view before highlighting, and FuseKit-owned OpenClaw state disables page evaluate by default.
- [x] Privacy reassurance UX: the control-room Snowman now switches into an eyes-covered privacy pose for secret-bearing gates and running steps involving passphrases, tokens, API keys, private keys, payment/MFA checks, non-echoing CLI prompts, or vault operations.
- [x] Private GitHub source lane: Cloud Shell bootstrap now prompts for the vault passphrase before app retrieval, uses `fusekit source fetch` to download public or private GitHub repos through HTTPS archives, filters local state and credential-looking artifacts from fetched archives case-insensitively, captures approved GitHub App/PAT tokens into the encrypted vault, avoids SSH setup or token-in-URL cloning, and runs a GitHub-specific OpenClaw guidance/highlight goal for private repo approval.
- [x] OCI launcher/installer/VM smoothness pass: source fetching now discovers public GitHub default branches before archive download, launcher/bootstrap source-fetch commands avoid duplicate OpenClaw flags, OCI VM bootstrap retries transient package/browser/OpenClaw installation failures, direct VM runs wait for SSH plus cloud-init plus FuseKit/OpenClaw/browser verification with a longer setup timeout before app upload, and failed partial OCI provisioning attempts detonate created resources.
- [x] Security hardening pass: outbound URL fetches now enforce HTTPS with loopback-only HTTP exceptions, provider HTTP/LLM error bodies no longer echo into public errors, control-room serving is local-only by default with no-store/security headers, dynamic control-room HTML uses attribute/class sanitizers, browser spine result serialization redacts typed text and raw browser output, remote artifact extraction avoids `tar.extractall`, vault writes are atomic owner-only files, the runtime floor is Python 3.10, and local/Cloud Shell bootstrap paths can self-provision an isolated Python 3.12 runtime when the default Python is older.
- [x] Cross-repo bug pass: GitHub source URLs now reject non-root repo pages, failed app archive extraction preserves the previous destination, and remote artifact retrieval fails on empty or unsafe archives instead of silently continuing.
- [x] Alive workflow hardening pass: runner jobs now write durable `checkpoints.json` recovery state with next actions/resume hints, the control room renders Snowman-backed recovery cards for active phases, static control-room artifacts refresh after launch state changes, and remote detonation is guarded by encrypted/redacted artifact completeness checks.
- [x] Trust verification hardening pass: `apply` now writes a redacted `verification_report.json` even when verification needs repair, OCI artifact retrieval requires it before detonation can be trusted, and the control room renders Snowman-backed trust cards for passed, pending, repairing, and failed provider/live-app checks.
- [x] Provider verification/deployment preflight hardening: GitHub, Vercel, Cloudflare/DNS, Resend, Plaid, webhook, and live-app checks now have provider-specific recipe/card semantics, redacted repair guidance, pending-safe DNS/domain/deploy handling, rollback metadata artifacts, and detonation preflight checks before worker destruction.
- [x] Provider UI stump hardening: inferred computer-use now classifies login, MFA, CAPTCHA, consent, missing token, changed navigation, API error, page loading, billing, and unknown UI drift; writes follow-me steps into durable gates; lets the human take over and click resume; falls back to provider-native setup when tokens exist; saves redacted successful provider memory; and keeps verification as the source of truth.
- [x] Provider UI security hardening: snapshots sent to the LLM are redacted, persisted target hints avoid secret-looking text, provider-memory/gate files are private atomic writes, control-room gate resume requires a same-origin custom header, UI repair waits are not treated as success, and provider verification accepts only passed or pending-safe checks before detonation can proceed.
- [x] Guided-gate UX hardening: provider friction copy now leads the user through highlighted provider prompts or autonomous retries instead of asking them to interpret provider screens, figure out navigation, or manually recover setup.
- [x] Magic-lane reliability split: CLI command entrypoints now route through focused command modules, and the control room is split into views, durable state, cards, snowman mascot logic, browser events, CSS assets, and server modules while preserving compatibility imports.
- [x] Canonical launch run-state contract: launches now write `.fusekit/run_state.json` with app repo, runner, OCI, browser, provider session, vault, secret capture, provider-check, receipt, and detonation-safety readiness fields for resume and audit.
- [x] Central Run Record: launches now write `.fusekit/run_record.json` as the non-secret product object tying together run state, checkpoints, provider gates, verifier status, vault record metadata, artifacts, approvals, errors, and workspace detonation proof.
- [x] Control-room review hardening: run-state readiness now renders as a launch-contract panel, Snowman/brand markup matches its CSS assets after the modular split, and gate-resume POSTs reject untrusted browser origins.
- [x] Resume/idempotence hardening: OCI/Cloud Shell launches reuse compatible waiting job state, preserve the same job id on resume, attach run-state artifacts to the control room, recover from corrupt run-state files with a redacted rebuild note, parse persisted readiness booleans strictly, reuse existing GitHub deploy-key vault records, create Vercel env vars before deleting old values unless provider repair requires replacement, and continue through provider verification/repair/fallback as the source of truth.
- [x] Whole-lane failure-mode tests: run-state detonation readiness/redaction, corrupt-state recovery, strict false-value parsing, OCI-ready/browser-missing state, provider-gate waits, token-backed API fallback, DNS pending-safe verification, detonation preflight blocking, interrupted Cloud Shell resume, control-room launch-contract rendering, and Vercel env repair are covered by local tests.
- [x] Precision-alignment pass: remote artifact completeness now matches detonation preflight survivors, provider-check run-state readiness and the remote runner loop are driven by the redacted verification report instead of optimistic completion, durable gates render the same guided follow-me UI in static and live control rooms, and launch-contract cards now explain the exact next condition for each phase.
- [x] Truth/user-surface alignment pass: local launches now write the same durable job, checkpoint, run-state, and optional control-room artifacts as OCI launches; `fusekit detonate` uses the split command module correctly; and live acceptance readiness checks the verification report plus rollback metadata directly instead of trusting receipt presence alone.
- [x] Live OCI provisioning hardening: NSG security rules now use the OCI SDK request-details wrapper expected by the live API, and OCI HTTP debug/wire logging is suppressed before provisioning so delegated auth material cannot spill into terminals or run logs.
- [x] Route/action security sweep: accessible HTTP routes and state-changing actions are mapped, control-room POSTs reject CORS preflight and browser-declared cross-site fetches, and OpenClaw installer bootstrap no longer shells env-controlled args through `bash -lc`.
- [x] Model/inference contract surface: launches now write a non-secret `llm_contract.json`, the Run Record summarizes the active model lane, and the control room explains OpenAI/OpenClaw fallback versus API-key-only custom providers before provider gates begin.
- [x] Model/inference acceptance hardening: Run Record recording readiness and live acceptance now fail closed unless model inference is proven by an encrypted API key or encrypted OpenClaw authorization profile, with the public contract and summary kept consistent.
- [x] Model/inference survivor hardening: `llm_contract.json` is a durable survivor, retrieved from remote OCI workers, restored during worker replacement, validated in artifact bundles, and checked by detonation preflight before launch trust.
- [x] Model/inference blocker UX: acceptance reports and live/static control-room fallback cards now turn missing `model_inference` or `llm_contract.json` proof into the exact next visible action: capture `OPENAI_API_KEY` in the VM or complete the OpenClaw OpenAI authorization gate.
- [x] Model/inference detonation preflight: before plaintext worker destruction is trusted, the central Run Record must prove a ready encrypted API-key or encrypted OpenClaw model lane with secret-boundary wording.
- [x] Model/inference proof consistency: acceptance and detonation preflight now require the embedded `llm_contract` in the Run Record, validate its encrypted-lane/security shape, and reject drift between the contract and `model_inference` summary.
- [x] Model/inference shape authority: the non-secret LLM contract module now owns the model summary, contract, security, and auth-lane key sets consumed by Run Record writing, live acceptance, and detonation preflight, so the explicit inference contract cannot drift across launch gates.
- [x] Model/inference lane-shape hardening: live acceptance now requires shaped LLM auth lanes with ids, labels, boolean availability/action flags, public-safe descriptions, default-lane consistency, and matching `model_inference.lane_count`.
- [x] Model/inference recording-contract hardening: recording readiness now requires the same paired `model_inference`/`llm_contract` proof, so the recording checklist cannot turn green from a summary without shaped auth lanes and matching lane counts.
- [x] Public model/inference readiness docs: README, launch runbook, public readiness, security map, and friction log now require matching `model_inference`/`llm_contract` proof as part of the public recording bar.
- [x] Model/inference artifact drift check: live acceptance now rejects missing or stale standalone `llm_contract.json` when it does not match the Run Record's embedded contract.
- [x] Model/inference detonation survivor check: detonation preflight now requires the standalone `llm_contract.json` survivor and rejects drift against the Run Record before cleanup is trusted.
- [x] Remote artifact inventory fail-closed: OCI artifact retrieval and bundle validation require the pre-detonation Run Record durable-source list plus audit proof, successful OCI cleanup copies `workspace_detonation.json` into the retrieved bundle, and live acceptance fails closed until the complete post-detonation survivor set is present.
- [x] Remote artifact blocker UX: static and live control rooms now turn failed `remote_artifacts.loaded` checks into a named Remote artifacts launch blocker even when older acceptance reports omit structured `blockers[]` or `missing[]`.
- [x] Rehearsal-review blocker UX: final recording-contract cards now explain the specific proof each section waits on, including a clean rehearsal review that matches every human action to visible launcher/VNC instructions with no host browser, terminal, side channel, or user interpretation.
- [x] Rehearsal-review acceptance recovery: Run Record validation failures for `rehearsal_review` now become named Rehearsal review blockers in acceptance reports and static/live control-room fallbacks, instead of generic Run Record repair copy.
- [x] Worker-replacement acceptance recovery: Run Record validation failures for `worker_replacement` now become named Worker replacement blockers that keep the user in the live room while FuseKit proves destroy/restore/reopen/resume without host-machine state.
- [x] Worker-replacement Run Record preflight alignment: cleanup preflight now requires the central Run Record to embed passed `worker_replacement_drill` proof, not just durable replacement intent or a standalone drill survivor, before worker destruction is trusted.
- [x] Worker-replacement drill exactness: cleanup preflight now rejects standalone or embedded worker-replacement drill sidecar fields, padded schema/status/restored-source/statement text, and loose restore-source proof before kill/recreate evidence can support worker cleanup.
- [x] Live Run Record worker-replacement exactness: live acceptance now rejects sidecar or padded embedded `worker_replacement_drill` proof before kill/recreate readiness can support public launch or recording trust.
- [x] Recording worker-replacement artifact exactness: final Run Record recording readiness now fails closed when raw or embedded worker-replacement drill proof has sidecar fields, padded generated text, callback URLs, credential-looking text, or loose restore-source rows before the public proof object can turn green.
- [x] Worker-replacement drill shape authority: the shared worker-replacement contract now owns the immutable drill key set consumed by Run Record generation and live acceptance for both embedded and remote-survivor proof, so kill/recreate evidence cannot drift between launch gates.
- [x] Workspace-detonation acceptance recovery: failed `detonation.workspace_receipt` checks now become named Workspace detonation blockers in acceptance reports and static/live control-room fallbacks, keeping the user in the launcher until VM, boot-volume, public-IP, network-resource, and remote-worker cleanup proof exists.
- [x] Runner-readiness acceptance recovery: failed or missing `runner_readiness.prepared` proof now becomes a named Runner readiness blocker across acceptance and static/live control-room fallbacks, keeping the launcher focused on x86_64/OpenClaw/Playwright/noVNC/shared-browser/vault-access proof before provider gates continue.
- [x] Visual-session acceptance recovery: failed `visual_state.safe` checks now become named Visual session blockers across acceptance and static/live control-room fallbacks, keeping noVNC/control-room URLs and noVNC password metadata safe before the provider browser is trusted.
- [x] Visual-state shape authority: a shared visual-state contract now owns the generated `visual.json` field set, visual transport fields, runner/status/display values, and fixed VM-browser guidance notes consumed by live acceptance and detonation preflight, so VM/browser handoff proof cannot drift across launch gates.
- [x] Provider-pack acceptance recovery: failed or missing `provider_packs.validated` checks now become named Provider packs blockers across acceptance and static/live control-room fallbacks, keeping setup, route planning, and verification paused until capability packs validate for the manifest services.
- [x] Verification/rollback acceptance recovery: failed `verification_report.*` and `rollback_metadata.*` checks now become named Verification and Rollback blockers across static/live control-room fallbacks, keeping provider verification and rollback-action proof launcher-actionable even for older check-only reports.
- [x] Rollback proof hardening: live acceptance and detonation preflight now count only `planned` or `done` provider rollback rows as proof, so skipped rollback placeholders cannot satisfy provider rollback coverage or cleanup trust.
- [x] Rollback metadata exactness: live acceptance and detonation preflight now reject sidecar or padded rollback metadata rows before provider rollback proof can support launch readiness or cleanup trust.
- [x] Rollback-proof shape authority: a shared rollback-proof contract now owns the rollback metadata envelope, action row fields, public action text fields, and accepted proof statuses consumed by live acceptance and detonation preflight, so provider rollback coverage proof cannot drift across launch gates.
- [x] Setup-receipt exactness: live acceptance and detonation preflight now reject sidecar or padded setup-receipt envelopes before zero-secret receipt proof can support launch readiness or cleanup trust.
- [x] Setup-receipt shape authority: a shared setup-receipt contract now owns the receipt envelope, raw-secret counter field, public text fields, action row fields, and required action text fields consumed by Run Record audit generation, live acceptance, and detonation preflight, so zero-secret receipt proof cannot drift across launch gates.
- [x] Detonation preflight verifier drift alignment: cleanup preflight now compares the central Run Record verifier summary against `verification_report.json`, including pending-safe normalization, before worker destruction is trusted.
- [x] Live acceptance embedded-verification drift alignment: live acceptance now compares both Run Record verifier views against `verification_report.json`, so stale embedded provider-check rows cannot pass public readiness after the summary was refreshed.
- [x] Run Record state/verification preflight alignment: cleanup preflight now requires a truthful embedded launch `state`, an embedded `verification` object, and verifier-report drift checks against both Run Record verifier views before worker destruction is trusted.
- [x] Run Record detonation preflight-summary alignment: cleanup preflight now requires the central `detonation` section to prove literal `preflight_safe` readiness and an explicit `workspace_detonated` boolean before worker destruction is trusted.
- [x] Run Record detonation envelope exactness: recording readiness, live acceptance, and cleanup preflight now reject sidecar top-level detonation fields before no-trace cleanup proof can support public recording or worker destruction.
- [x] Detonation preflight provider-route survivor alignment: cleanup preflight now requires `provider_strategies.json` and compares the central Run Record provider strategy/playbook proof against that survivor before worker destruction is trusted.
- [x] Detonation preflight runner-readiness survivor alignment: cleanup preflight now requires `runner_readiness.json`, validates the VM/browser runner contract, and rejects drift against the Run Record `runner_profile` before worker destruction is trusted.
- [x] Detonation preflight gate/wake survivor alignment: cleanup preflight now requires `gates.json` and `gate_events.jsonl`, safety-checks their public contents, and rejects drift against Run Record `provider_gates` and `wake_events` before worker destruction is trusted.
- [x] Core acceptance fallback recovery: stale check-only reports for gates, provider strategies, vault, receipts, worker-state detonation, and leak scan now render named launcher-actionable blockers in static/live control rooms instead of generic launch-evidence repair copy.
- [x] Audit/provider-pack snapshot acceptance recovery: stale check-only reports for `audit.*` and per-provider `provider_pack.*` rows now render named Audit log and Provider packs blockers across acceptance and static/live control rooms, keeping launch proof recovery focused on redacted audit evidence and validated capability packs.
- [x] Manifest/plan acceptance fallback recovery: stale check-only reports for `manifest.*` and `plan.generated` now render named Manifest and Setup plan blockers across acceptance and static/live control rooms, keeping early setup-proof repair tied to app scanning, manifest snapshots, and the visible Approve setup plan control.
- [x] Rehearsal-review proof ledger: Run Records now embed `rehearsal_review.reviewed_actions` for each human action, acceptance rejects drift against `human_actions`, and static/live control rooms show the non-secret gate/gate-event proof source for matched actions.
- [x] Report-level recording contract: live acceptance reports now include a redacted `recording_contract` summary from the Run Record, so public demo review can see every recording-readiness section check and blocker status directly in `.fusekit/acceptance/report.json`.
- [x] Recording-contract section proof alignment: live acceptance now rejects green `recording_contract` checks unless each check has matching Run Record proof sections, matching detonation preflight's anti-placeholder guard.
- [x] Recording-contract vault exactness: final recording readiness now rejects loose vault metadata, non-integer record counts, padded id/kind/provider/label values, and sidecar vault fields before `recording_contract.checks.vault` can turn green.
- [x] Acceptance recording-contract visibility: static and live control rooms now render the acceptance report's redacted `recording_contract` section checks inside the launch-blocker panel, so refreshed demo screens show which recording-proof sections passed or are still pending.
- [x] Acceptance ledger recording-proof visibility: final acceptance ledger events now include `recording_proof_ready`, `recording_ready`, and a redacted recording-contract count/blocker summary, so the append-only proof trail matches the public report.
- [x] Acceptance CLI recording-proof visibility: `fusekit acceptance run` now prints both `Recording proof ready` and `Recording ready`, matching the JSON report and ledger so operators can tell from the terminal whether the live run is recordable.
- [x] Acceptance CLI recordability gate: public walkthrough commands now use `--require-recording`, which preserves normal launch-readiness exit behavior by default but exits nonzero when a live run lacks `recording_ready` proof.
- [x] Acceptance report reader hardening: static and live control-room payloads now reshape and redact `report.json` at read time, dropping unexpected fields and preserving only launch flags, redacted blockers/checks/missing proof, public paths, and the recording-contract summary.
- [x] Control-room recordability formula hardening: static payloads, server-rendered cards, and live refresh JavaScript now require `recording_ready`, `recording_proof_ready`, live public launch readiness, and `recording_contract.recording_ready` to agree before showing the public demo as recordable.
- [x] Acceptance report recordability formula hardening: public report serialization now requires a green recording contract before emitting `recording_proof_ready` or `recording_ready`, so a bare Run Record check cannot make the demo artifact recordable.
- [x] Hollow recording-contract hardening: report serialization and control-room report loading now require a shaped, full-check, all-true, blocker-free `recording_contract` before any public surface can treat recording proof as ready.
- [x] Recording-contract card priority: static and live control rooms now render recording-proof cards in public-demo order, leading with worker replacement, rehearsal review, model inference, provider playbook, verifiers, and detonation instead of arbitrary key order.
- [x] Acceptance CLI recordability formula hardening: terminal output now prints the effective contract-backed `recording_proof_ready` flag, matching JSON reports, the control room, and `--require-recording`.
- [x] Control-room capture reliability hardening: JSON responses now tolerate browser/client disconnects without noisy tracebacks, and vault-backed clipboard-capture tests use a realistic timeout for encrypted writes under full-suite load.
- [x] Acceptance recording-contract summary hardening: public reports now render section checks as true only when the Run Record stored literal JSON `true`, so truthy strings or numbers cannot make malformed proof look green.
- [x] Receipt redaction counter hardening: live acceptance now requires `raw_secrets_exposed` to be literal JSON `0`, so malformed strings, booleans, or floats cannot crash the harness or truncate into false zero-secret proof.
- [x] Detonation preflight receipt-counter alignment: cleanup preflight now requires the standalone setup receipt to carry `raw_secrets_exposed` as literal JSON `0`, so stale or malformed receipt survivors cannot support worker destruction.
- [x] Run Record timeline identity hardening: recording readiness and live acceptance now reject duplicate step/checkpoint ids, so stale progress rows cannot overstate launch completion.
- [x] Run Record durable-state identity hardening: recording readiness and live acceptance now reject duplicate durable source, survivor, detonation-scope, resume-source, volatile-surface, and worker-restore rows, so restoration proof cannot be inflated by repeated entries.
- [x] Run Record detonation identity hardening: recording readiness and live acceptance now reject duplicate deleted-resource, network-resource, survivor, remote-worker process, and cleanup-path rows, so no-trace cleanup proof remains one unambiguous receipt.
- [x] Automation/control-room identity hardening: recording readiness and live acceptance now reject duplicate VNC allowance, automation route, post-gate route, control-room route, and state-changing route rows, so the safety boundary remains unambiguous.
- [x] Live/preflight automation-boundary exactness: live acceptance and cleanup preflight now reject sidecar automation-boundary fields, padded VNC/route/post-gate text, and non-integer ownership counts before post-gate automation proof can support launch readiness or worker cleanup.
- [x] Run Record automation-boundary canonicalization: generated boundaries now trim/redact public route text, drop unsupported or secret-shaped strategy rows from the ready route ledger, dedupe `provider:recipe` route signatures, and derive ownership counts plus post-gate route lists from that canonical ledger.
- [x] Automation-boundary shape authority: a shared automation-boundary contract now owns the public envelope, route fields, count fields, post-gate lists, VNC allow-list, route owners, and route-kind sets consumed by Run Record recording checks, live acceptance, and detonation preflight, so post-gate ownership/no-user-machine-state proof cannot drift across launch gates.
- [x] Run Record verifier-summary canonicalization: generated verifier summaries now trim/redact provider/check/status rows, convert malformed or secret-shaped verifier identities into blocking `unknown` proof, and derive launch-safe counts from the canonical public check rows.
- [x] Verifier-summary shape authority: a shared verifier-summary contract now owns the schema version, public envelope, check-row, and count-key sets consumed by Run Record recording checks, live acceptance, and detonation preflight, so live-verifier proof cannot drift across launch gates.
- [x] Run Record audit-row canonicalization: final audit-trail rows now trim/redact public text, apply generated status/summary fallbacks, drop unsupported categories, and derive counts from the canonical public audit ledger before recording or cleanup validators run.
- [x] Audit-trail shape authority: a shared audit-trail contract now owns the schema version, public envelope, entry-field, and required-category sets consumed by Run Record recording checks, live acceptance, and detonation preflight, so audit proof cannot drift across launch gates.
- [x] Run Record evidence-row canonicalization: final evidence inventory rows now trim/redact path/source text, reject unsafe public-relative paths and secret-shaped sources, collapse duplicate paths, and derive counts from the canonical evidence rows.
- [x] Artifact/evidence shape authority: a shared evidence-inventory contract now owns the schema version, artifact row, evidence envelope, evidence row, and count-key sets consumed by Run Record recording checks, live acceptance, and detonation preflight, so survivor proof cannot drift across launch gates.
- [x] Gate/wake proof shape authority: a shared gate-proof contract now owns provider-gate summary/record and wake-event summary/record key sets consumed by Run Record recording checks, live acceptance, and detonation preflight, so guided-control handoff proof cannot drift across launch gates.
- [x] Rehearsal-proof shape authority: a shared rehearsal-proof contract now owns human-action row fields, known action/count buckets, exact visible controls, reviewed-action fields, schema versions, and proof-source mapping consumed by Run Record recording checks, live acceptance, and detonation preflight, so clean-rehearsal proof cannot drift across launch gates.
- [x] Vault-proof shape authority: a shared vault-proof contract now owns the public vault metadata envelope, record fields, and forbidden raw-secret field names consumed by Run Record recording checks, live acceptance, and detonation preflight, so credential-capture proof cannot drift across launch gates or reintroduce raw values.
- [x] Run Record provider-strategy canonicalization: generated central route proof now trims/redacts provider, strategy, decision, selected-route, evidence, and candidate rows; drops sidecar or secret-shaped route data; dedupes fallback candidates; and reuses the canonical playbook in embedded `provider_strategies`.
- [x] Run Record provider-playbook canonicalization: generated central playbooks now trim/redact step and safety-note text, drop malformed or secret-shaped rows, preserve only generated step fields, derive step counts from canonical rows, and reuse that clean playbook inside embedded `provider_strategies` proof.
- [x] Provider-strategy shape authority: a shared provider-strategy contract now owns the artifact schema version, public envelope, provider rows, strategy rows, decision fields, selected-route fields, fallback-candidate fields, and ordered writer field lists consumed by the CLI artifact writer, Run Record generation, live acceptance, and detonation preflight, so deterministic route proof cannot drift across launch gates.
- [x] Provider-playbook shape authority: a shared provider-playbook contract now owns the public provider families and exact step fields used by Run Record generation, live acceptance, and detonation preflight, so deterministic setup ordering cannot drift across launch gates.
- [x] Run Record runner-profile canonicalization: generated runner summaries now keep only the shared runner-readiness contract fields, trim/redact public VM/browser text, relabel provider-profile/cache/binary paths, drop malformed checks and sidecar rows, and leave incomplete runner proof blocking rather than noisy.
- [x] Runner-readiness shape authority: the shared runner-readiness contract now owns the readiness/profile/browser/observed/binary key sets plus schema, status, and expected runner-profile constants consumed by Run Record generation, live acceptance, and detonation preflight, so VM/browser handoff proof cannot drift across launch gates.
- [x] Run Record workspace-detonation canonicalization: generated detonation summaries now keep only the workspace cleanup receipt contract, trim/redact public cleanup text, dedupe cleanup resource lists, relabel unsafe failure details, and drop sidecar receipt/resource/remote-worker rows before recording or cleanup validators run.
- [x] Detonation-proof shape authority: a shared detonation-proof contract now owns the Run Record detonation envelope, workspace receipt fields, resource-summary fields, remote-worker cleanup fields, and cleanup text/list/boolean groups consumed by Run Record generation, live acceptance, and detonation preflight, so no-trace cleanup proof cannot drift across launch gates.
- [x] Recording-contract vocabulary alignment: a shared recording-contract module now owns the schema version, ordered checklist, field set, and proof-section mapping emitted by the Run Record writer and consumed by the control room, live acceptance, and detonation preflight, so launch gates cannot drift onto separate hand-maintained readiness vocabularies.
- [x] Detonation preflight identity alignment: cleanup preflight and CLI workspace-detonation readiness now reject duplicate control-room mutation routes plus duplicate remote-worker process/path cleanup rows before worker destruction can be trusted.
- [x] Detonation preflight control-room route-count alignment: cleanup preflight now requires integer `route_count` and `state_changing_route_count` proof matching the protected route inventory, so float or hand-edited control-room security counters cannot support worker destruction.
- [x] Recording-contract control-room security exactness: final recording readiness now rejects padded route labels, padded methods/protection strings, padded state-changing route labels, and unexpected sidecar control-room security fields before `recording_contract.checks.control_room_security` can turn green.
- [x] Live/preflight control-room security exactness: live acceptance and cleanup preflight now reject sidecar control-room security fields plus padded route, method, protection, and state-changing route text before protected mutation proof can support launch or cleanup trust.
- [x] Control-room security shape authority: a shared control-room security contract now owns the route inventory, public envelope, route fields, protected mutation routes, POST-protection terms, unknown-route protection, and statement terms consumed by the public route-surface writer, Run Record recording checks, live acceptance, and detonation preflight, so browser mutation security proof cannot drift across launch gates.
- [x] Worker-replacement preflight identity alignment: the shared drill validator now rejects duplicate restored-source rows, so kill/recreate proof cannot pass cleanup preflight with repeated survivor labels.
- [x] Detonation preflight provider-gate exactness: cleanup preflight now rejects loose provider-gate summaries, non-integer totals/status counts, padded provider/status/target values, malformed attempt/timestamp rows, and sidecar gate fields before worker destruction is trusted.
- [x] Wake-event recording/cleanup exactness: final recording readiness and cleanup preflight now reject loose wake-event summaries, non-integer totals/event counts, padded event/gate/target values, malformed target-count/timestamp rows, and sidecar wake-event fields before wake proof can support recording or worker destruction.
- [x] Live Run Record wake-event exactness: live acceptance now applies the shared wake-event summary and row shape to central `wake_events` proof before Run Record readiness, so sidecar summary fields, padded event names, and ignored row metadata cannot hide behind normalized `gate_events.jsonl` signatures.
- [x] Run Record envelope exactness: the writer now owns the generated top-level Run Record field set, and live acceptance plus cleanup preflight reject sidecar top-level fields or malformed timestamp proof before the central proof object can support public launch or worker destruction.
- [x] Cleanup Run Record hollow-proof guard: detonation preflight now requires generated proof sections such as vault, playbook, runner profile, worker replacement, artifacts, verifiers, evidence, human actions, and provider strategy summaries to be non-empty before section validators run, so an empty top-level placeholder cannot satisfy the central proof object.
- [x] Gate-event survivor exactness: live acceptance and detonation preflight now validate raw `gate_events.jsonl` rows against the generated wake-event shape before signature comparison, so sidecar survivor fields, padded row values, malformed target counts, and bad event timestamps cannot support launch readiness or cleanup.
- [x] Provider-gate survivor exactness: live acceptance and detonation preflight now validate raw `gates.json` rows against the generated provider-gate shape before readiness or signature comparison, so sidecar survivor fields, padded gate values, malformed target lists, and bad attempt/timestamp proof cannot support launch readiness or cleanup.
- [x] Provider-gate survivor artifact authority: the shared gate-proof contract now owns the raw `gates.json` envelope consumed by `GateService` serialization and detonation preflight, so guided-control survivor proof cannot drift between writer and cleanup gate.
- [x] Moonlite rehearsal fixture proof: the checked-in public acceptance target now has a regression that copies `examples/moonlite-rsvp` as clean source without local `.fusekit`, `node_modules`, `dist`, or build-info output, runs rehearsal acceptance, requires a clean local `launch_ready` report with GitHub/Vercel/Resend/Cloudflare provider-pack proof and leak-scan proof, and still keeps public/recording readiness false until live evidence exists.
- [x] Refreshed recording-contract parity: live `/api/job` acceptance refreshes now preserve `data-acceptance-recording-check` on recording-contract cards, keeping dynamic demo screens inspectable by the same section ids as static control-room HTML.
- [x] Run Record browser-payload hardening: control-room Run Record loading now recursively redacts public text and local paths before static HTML or `/api/job` can expose stale or hand-edited token-shaped statement, error, blocker, or extra-field values.
- [x] Verifier/provider-route payload hardening: verification reports and provider strategy/playbook artifacts now pass through the same public-payload redaction before static HTML or `/api/job`, preserving structural provider/check/route ids while scrubbing stale token-shaped repair, callback, proof, or extra-field values.
- [x] Base control-room payload hardening: job steps, checkpoints, artifact paths, run-state notes, and standalone `llm_contract.json` are now context-redacted before browser delivery while preserving the live action token and structural launch ids.
- [x] Control-room action response hardening: every browser-visible JSON response now passes through the same context-aware public redaction, preserving gate ids/status/wake metadata while scrubbing poisoned durable gate hints or provider errors before the page displays them.
- [x] Public path-field hardening: control-room browser payload redaction now treats suffix-shaped path keys such as `trace_path`, `log_file`, `workspace_dir`, and `bundle_root` as public artifact paths, keeping proof useful without leaking local runner/workspace prefixes.
- [x] Visual-state browser-payload hardening: `visual.json` now preserves only validated noVNC/control-room fields for the live VM iframe while redacting extra notes, token-shaped text, and path-shaped visual metadata before `/api/job` or static HTML can expose them.
- [x] Durable gate browser-payload hardening: `gates.json` records now context-redact reasons, resume URLs, follow steps, success/avoid criteria, next actions, and resume hints before browser delivery while preserving gate ids, status, env targets, and the Open/Capture controls.
- [x] Run Record structural gate hardening: central records now public-redact and canonicalize gate ids, providers, statuses, classifications, targets, guidance lists, timestamps, wake-event fields, and approval summaries before writing provider-gate proof.
- [x] Run Record wake-event canonicalization: central records now trim/redact wake-event rows, normalize malformed target counts and timestamps, skip token/password/secret marker text, and collapse duplicate event ids/proofs before writing evented resume proof.
- [x] Provider-gate proof identity hardening: recording readiness and live acceptance now reject duplicate provider-gate ids plus drifted gate status/provider summaries, so stale gate rows cannot inflate or blur launch proof.
- [x] Recording-contract provider-gate exactness: final recording readiness now rejects padded provider-gate fields, padded captured targets, padded provider summaries, and unexpected sidecar gate fields before `recording_contract.checks.provider_gates` can turn green.
- [x] Approval proof hardening: live acceptance now requires Run Record approvals to be shaped, public-safe, matched to durable gates, anchored to matching resume wake events, and unique per approval gate, while Run Record writing deduplicates approval summaries by gate id.
- [x] Approval row exactness hardening: live acceptance and cleanup preflight now reject padded approval fields and unexpected sidecar fields before protected approval proof can support public readiness or cleanup trust.
- [x] Approval-summary shape authority: a shared approval-summary contract now owns approval row fields, public text fields, timestamp field, and accepted statuses consumed by Run Record generation, live acceptance, and detonation preflight, so protected approval proof cannot drift across launch gates.
- [x] Canonicalize generated approval proof: Run Record writing now trims/redacts approval id/provider/status/reason fields, fills public provider/reason fallbacks, collapses duplicate approval ids, and normalizes malformed approval timestamps before acceptance or cleanup preflight sees them.
- [x] Detonation preflight approval-proof alignment: cleanup preflight now requires the Run Record approval list and validates approval metadata, unique ids, public-safe text, durable gate id/status matches, and resume wake-event anchors before worker destruction is trusted.
- [x] Human-action proof identity hardening: recording readiness and live acceptance now reject duplicate human-action proof rows, so repeated Capture/approval/open rows cannot satisfy a clean rehearsal or public recording contract.
- [x] Generated human-action count exactness: Run Records now emit explicit zero-count buckets for absent visible controls and ignore diagnostic/non-visible wake rows when building `human_actions`, so only actual launcher actions feed rehearsal proof.
- [x] Wake-event proof identity hardening: recording readiness and live acceptance now reject duplicate wake-event ids and duplicate event/gate/target proof rows, so stale or replayed gate wake events cannot inflate public launch proof.
- [x] Run Record evidence-path hardening: artifact paths are written as public labels, and the evidence inventory includes only files that resolve inside the `.fusekit` bundle, preventing outside worker/host paths from becoming post-detonation proof.
- [x] Evidence inventory identity hardening: recording readiness and live acceptance now reject duplicate evidence rows and drifted evidence counters, so repeated logs/screenshots/receipts cannot inflate public proof.
- [x] Recording-contract artifact exactness: final recording readiness now rejects sidecar artifact fields, padded artifact names or paths, unsafe artifact paths, credential-query artifact text, and non-boolean existence proof before `recording_contract.checks.artifacts` can turn green.
- [x] Recording-contract evidence exactness: final recording readiness now rejects sidecar evidence fields, padded evidence paths or sources, unsafe evidence paths, incomplete inventory statements, and loose schema/count proof before `recording_contract.checks.evidence` can turn green.
- [x] Remote upload hygiene hardening: OCI app archives now exclude TypeScript build-info files, frontend cache/coverage folders, `.env`, `.fusekit`, dependency folders, and case variants of those local-state names, with tests inspecting the transient upload tarball before it leaves the host.
- [x] Run Record artifact-list hardening: live acceptance now validates each artifact row has a public-safe name, public-relative non-traversing path, no credential query text, and a boolean existence flag.
- [x] Artifact-list identity hardening: recording readiness and live acceptance now reject duplicate artifact names/paths and unsafe artifact rows before artifact inventory proof can be public-ready.
- [x] Canonicalize generated artifact proof: Run Record writing now trims/redacts artifact names, skips credential-query or secret-looking artifact labels/paths, and collapses duplicate generated artifact names or paths before evidence inventory is derived.
- [x] Durable-source artifact truth hardening: Run Record durable-state summaries now treat artifact existence as present only when the source row stores literal JSON `true`, so malformed truthy values cannot inflate survivor proof.
- [x] Harden detonation preflight against placeholder gate/wake proof: provider-gate totals, provider/status summaries, wake-event counts, and captured-target wake evidence must match before cleanup.
- [x] Human-action gate binding hardening: recording readiness now requires each guided human-action proof row to match a durable provider gate id, and copy-once Capture actions must match that gate's declared env target before `human_actions` can support the final recording contract.
- [x] Run Record numeric-proof hardening: live acceptance now rejects JSON booleans in gate, wake-event, evidence, human-action, rehearsal, automation-boundary, verifier, and audit counters instead of letting truthy values satisfy launch proof.
- [x] Live acceptance/Run Record/preflight counter hardening: proof counters now reject floats instead of truncating them, so `1.9` cannot satisfy a one-row total in durable, gate, wake, verifier, evidence, audit, model-lane, automation-boundary, or human-action proof.
- [x] Run Record provider-strategy hardening: embedded provider strategy and playbook proof is redacted before summary/record construction, preserving route decision signatures while removing callback URLs and token-shaped instruction text from the central artifact.
- [x] Run Record provider-playbook identity hardening: recording readiness and live acceptance now reject duplicate provider playbook step ids, so ordered launcher proof cannot be made ambiguous by stale or hand-edited duplicate rows.
- [x] Run Record verifier hardening: embedded verification reports are redacted before the central record is written, preserving provider/check/status signatures while removing callback URLs and token-shaped repair details from public proof.
- [x] Verifier identity hardening: recording readiness, live acceptance, and detonation preflight now reject duplicate provider/check verifier rows, so public trust proof has one unambiguous status per verifier identity.
- [x] Recording-contract verifier exactness: final recording readiness now requires the generated verifier summary schema, exact fields, trimmed provider/check/status rows, literal integer counters, boolean pending-safe proof, and live-verifier guidance before `recording_contract.checks.verifiers` can turn green.
- [x] Run Record LLM-contract hardening: embedded `llm_contract` proof is redacted before the central record is written, and live acceptance rejects standalone `llm_contract.json` survivors that contain credential-looking text.
- [x] Run Record LLM-lane hardening: live acceptance now rejects hollow or malformed `llm_contract.lanes` and requires the model summary lane count to match the embedded contract.
- [x] Run Record detonation-receipt hardening: embedded workspace detonation proof is redacted before the central record is written, and live acceptance rejects standalone `workspace_detonation.json` survivors that contain credential-looking text.
- [x] Workspace-detonation receipt exactness: live acceptance and Run Record validation now reject sidecar or padded workspace cleanup receipts before no-trace OCI cleanup proof can support launch readiness.
- [x] Recording-contract workspace-detonation exactness: final recording readiness now rejects loose workspace cleanup receipts, resource summaries, and remote-worker cleanup rows before `recording_contract.checks.detonation` can turn green.
- [x] Run Record setup-receipt hardening: setup receipt proof is redacted before receipt-derived audit entries are written, and live acceptance rejects standalone `setup_receipt.json` survivors that contain credential-looking text.
- [x] Standalone survivor callback hardening: live acceptance now rejects unredacted callback URLs in `provider_strategies.json`, `verification_report.json`, `rollback_plan.json`, `setup_receipt.json`, `llm_contract.json`, and `workspace_detonation.json` even when they do not contain obvious token-shaped query values.
- [x] Gate/wake survivor callback hardening: live acceptance now rejects unredacted callback URLs and credential-looking text directly in durable `gates.json` and `gate_events.jsonl` proof before gate resolution, audit, or Run Record wake consistency can pass.
- [x] Provider-pack public-proof hardening: per-provider capability-pack acceptance now rejects callback URLs or credential-looking text after schema validation, and the aggregate `provider_packs.validated` gate fails if any required provider pack fails.
- [x] Visual-state survivor hardening: live acceptance now rejects callback URLs or credential-looking text in extra `visual.json` survivor fields and callback-shaped noVNC/control-room URLs while still allowing the validated VM iframe/control-room URL contract.
- [x] Visual-state survivor exactness: live acceptance now rejects sidecar fields, padded generated strings, wrong display proof, and drifted visual guidance notes in raw `visual.json` before noVNC/control-room state can support launch readiness.
- [x] Worker-replacement proof hardening: Run Records recursively redact embedded worker-replacement drill proof, and acceptance/preflight require `restored_from` to match the durable replacement source contract exactly.
- [x] Runner-readiness public-summary hardening: Run Records and acceptance ledger snapshots now label runner profile/cache/binary paths, live acceptance compares that public summary against raw `runner_readiness.json`, and standalone readiness proof fails closed on callback URLs or credential-looking text.
- [x] Runner-readiness survivor exactness: shared runner-readiness validation now rejects sidecar or padded VM/browser profile proof before runner readiness can support launch readiness or cleanup trust.
- [x] Base Run Record path hardening: central records now write `app_path` as a public label, and live acceptance rejects absolute app paths before public launch proof can pass.
- [x] Run-state proof hardening: central records now public-redact embedded run-state notes before writing launch state into `run_record.json`.
- [x] Remote durable-survivor hardening: live remote artifact inventory now rejects callback URLs or credential-looking text in standalone `job.json`, `run_state.json`, `checkpoints.json`, and `worker_replacement_drill.json` before treating retrieved OCI artifacts as public proof.
- [x] Remote run-state survivor exactness: live remote artifact inventory now rejects sidecar fields, stringy booleans, padded notes, and unknown `missing_for_detonation` rows in raw `run_state.json` before retrieved OCI artifacts can support launch readiness.
- [x] Embedded run-state exactness: live Run Record validation now applies the generated launch-state contract to central `state`, so sidecar fields, stringy booleans, padded notes, and unknown detonation-missing rows cannot support public readiness even when raw `run_state.json` is clean.
- [x] Cleanup embedded run-state exactness: detonation preflight now applies the same generated launch-state contract to central `state`, so worker destruction cannot trust a looser embedded run-state proof than live readiness accepts.
- [x] Remote job/checkpoint survivor exactness: live remote artifact inventory now rejects sidecar or padded `job.json` and generated `checkpoints.json` recovery proof before retrieved OCI artifacts can support launch readiness.
- [x] Remote survivor shape authority: a shared remote-survivor contract now owns the job, step, checkpoint, checkpoint-file, run-state, and public survivor label fields consumed by runner job serialization and live acceptance, so retrieved OCI proof cannot drift from the writer that generated it.
- [x] Remote worker-replacement survivor exactness: live remote artifact inventory now rejects pending, sidecar, padded, duplicated, or host-machine-dependent `worker_replacement_drill.json` proof before a retrieved OCI bundle can support public recording readiness.
- [x] Durable-source path hardening: live acceptance and recording-readiness checks now require every Run Record `durable_state.sources` id to point at its canonical survivor filename, so a hand-edited source row cannot claim `run_state` or `gates` survived through a decoy relative path.
- [x] Detonation preflight durable-source path alignment: cleanup preflight now validates the same canonical `durable_state.sources` filename contract before worker destruction is trusted.
- [x] Durable-state proof shape authority: a shared durable-state contract now owns durable-state and detonation-scope schema versions, the worker-and-OCI detonation scope mode, no-trace/durable-worker statement terms, and replacement state-owner wording consumed by Run Record generation, live acceptance, automation-boundary proof, and cleanup preflight.
- [x] Detonation preflight worker-replacement contract alignment: cleanup preflight now requires the same durable-worker statements, no-trace OCI wording, runner profile, state owner, and volatile-surface coverage that recording readiness and live acceptance require.
- [x] Detonation preflight evidence-survivor alignment: cleanup preflight now resolves Run Record evidence-inventory paths under the `.fusekit` survivor bundle and rejects invented log, screenshot, visual, or receipt paths before worker destruction is trusted.
- [x] Detonation preflight visual-survivor alignment: cleanup preflight now requires `visual.json` and validates the same generated noVNC/control-room launch-proof envelope before worker destruction can be trusted.
- [x] Detonation preflight artifact-survivor alignment: cleanup preflight now also resolves Run Record artifact rows marked `exists: true` under the `.fusekit` survivor bundle before trusting cleanup.
- [x] Live acceptance artifact-survivor alignment: live Run Record acceptance now resolves artifact rows marked `exists: true` under the retrieved `.fusekit` bundle, matching detonation preflight's cleanup trust rule.
- [x] Live/preflight artifact-evidence exactness: live acceptance and cleanup preflight now reject sidecar artifact/evidence fields, padded artifact paths, padded evidence path/kind/source text, and non-integer evidence counts before survivor inventory proof can support launch readiness or worker cleanup.
- [x] Detonation preflight audit-trail alignment: cleanup preflight now validates the Run Record audit trail schema, counts, wake-event ids, receipt/audit indexes, required categories, and source anchors before worker destruction is trusted.
- [x] Audit-trail row exactness hardening: live acceptance and cleanup preflight now reject padded audit-entry fields and unexpected sidecar fields before audit proof can support public readiness or cleanup trust.
- [x] Recording-contract audit-trail exactness: final recording readiness now rejects loose audit rows, missing receipt/audit indexes, secret-looking audit text, and missing or stale wake-event anchors before `recording_contract.checks.audit_trail` can turn green.
- [x] Recording-contract audit envelope hardening: final recording readiness now requires the generated audit-trail envelope, audit-first statement, literal integer entry/category counts, and integer receipt/audit indexes before audit proof can be marked recordable.
- [x] Detonation preflight automation-boundary alignment: cleanup preflight now validates the Run Record automation-boundary schema, ready/no-user-machine state, complete VNC allow-list, route ownership, zero-blocked route counts, and post-gate API/CLI versus human-gate route lists before worker destruction is trusted.
- [x] Recording-contract automation-boundary exactness: final recording readiness now rejects padded VNC allow-list values, padded automation route fields, padded post-gate route labels, and unexpected sidecar automation fields before `recording_contract.checks.automation_boundary` can turn green.
- [x] Automation-boundary status/scope authority: the shared automation-boundary contract now owns ready/repair statuses, the worker-and-OCI detonation scope label, and required statement terms consumed by Run Record generation, live acceptance, and cleanup preflight, so post-gate automation proof cannot drift across launch gates.
- [x] Detonation preflight survivor hardening: cleanup preflight now rejects callback URLs or credential-looking text in standalone public JSON survivors, including setup receipts, verifier reports, rollback metadata, `llm_contract.json`, `worker_replacement_drill.json`, and the central Run Record.
- [x] Detonation preflight audit hardening: cleanup preflight now parses `audit.jsonl` as public JSONL proof and rejects malformed rows, callback URLs, or credential-looking text before worker destruction is trusted.
- [x] Detonation preflight vault hardening: cleanup preflight now rejects vault bundles that contain plaintext or credential-looking markers before worker destruction is trusted.
- [x] Detonation preflight vault-summary alignment: cleanup preflight now validates Run Record vault metadata counts, unique ids, required id/kind/provider/label fields, and recursive raw value/passphrase/private-key/password field exclusion before worker destruction is trusted.
- [x] Detonation preflight vault exactness: cleanup preflight now rejects loose Run Record vault metadata, non-integer record counts, padded id/kind/provider/label values, credential-looking vault record text, and sidecar vault fields before worker destruction is trusted.
- [x] Detonation preflight timeline alignment: cleanup preflight now validates Run Record step/checkpoint id, label, status, uniqueness, and public-safe detail/next-action/resume-hint text before worker destruction is trusted.
- [x] Run Record timeline row hardening: live acceptance and cleanup preflight now reject padded step/checkpoint fields and unexpected sidecar fields before timeline proof can support public readiness or cleanup trust.
- [x] Recording-contract timeline exactness: final recording readiness now rejects padded step/checkpoint fields, unexpected sidecar fields, malformed timestamps, and secret-looking timeline text before `recording_contract.checks.timeline` can turn green.
- [x] Central Run Record callback hardening: live acceptance now rejects bare provider callback URLs anywhere in `run_record.json`, even when the URL has no obvious token query string.
- [x] Vault acceptance hardening: live acceptance now treats credential-looking plaintext in vault bundles as a ciphertext-boundary failure and turns corrupt or modified vault bundles into `vault.unlock` failures instead of crashing the harness.
- [x] Vault metadata proof hardening: Run Records now drop raw value-style fields and public-redact remaining vault metadata before writing credential-capture proof, and live acceptance rejects stale records that reintroduce those fields.
- [x] Vault proof identity hardening: recording readiness and live acceptance now reject duplicate vault metadata ids, mismatched vault counts, and raw secret metadata fields before credential-capture proof can be public-ready.
- [x] Audit-log proof hardening: Run Records now redact raw `audit.jsonl` event names before writing central audit actions or summaries.
- [x] Audit-trail identity hardening: recording readiness and live acceptance now reject duplicate public audit proof rows, so repeated receipt, wake, audit-log, or detonation entries cannot inflate the launch proof chain.
- [x] Raw audit survivor hardening: live acceptance now parses `audit.jsonl` as JSONL and rejects malformed rows, callback URLs, or credential-looking text before the redacted audit log can count as public proof.
- [x] Raw audit survivor exactness: live acceptance and cleanup preflight now reject sidecar top-level audit fields, padded or missing event names, non-object `data`, and non-string timestamps before raw `audit.jsonl` can support launch readiness or cleanup trust.
- [x] Raw audit survivor shape authority: a shared audit-log survivor contract now owns standalone audit row fields and the generated event/data/timestamp field names consumed by live acceptance and detonation preflight, so raw audit proof cannot drift across launch gates.
- [x] Acceptance-summary proof hardening: Run Records now redact embedded acceptance blockers and errors before writing central launch-readiness proof.
- [x] Failed-run resume hygiene: launches now resume only active `created`, `running`, or `waiting` jobs with no failed steps, leaving failed attempts as audit evidence while fresh retries start with clean job ids and checkpoints.
- [x] Public recording-gate proof-source hardening: `fusekit acceptance run --require-recording` now requires `--mode live` plus `--remote-artifacts`, tying recordability to retrieved OCI survivor evidence instead of local `.fusekit` scratch state.
- [x] Public recording-gate UX alignment: static and live control-room acceptance cards now tell launchers to rerun live acceptance with both `--remote-artifacts` and `--require-recording`, matching the CLI's survivor-proof gate.
- [x] Public report recordability proof-source hardening: acceptance reports and control-room report readers now require `remote_artifacts.loaded: ok` before `recording_proof_ready` or `recording_ready` can go green.

## Milestone 1: Repo Skeleton And CLI

- [x] Add `LICENSE`.
- [x] Add package modules: `manifest`, `scanner`, `planner`, `policy`, `approvals`, `audit`, and `errors`.
- [x] Add package folders: `crypto`, `vault`, `providers`, `capabilities`, `detonation`, and `schemas`.
- [x] Add CLI command shell for `scan`, `validate`, `plan`, `authorize`, `apply`, `verify`, `receipt`, `unlock`, `request`, and `detonate`.
- [x] Add `install`, `setup`, and `launch` commands for one-click app integration and guided setup.
- [x] Make OpenClaw the default supervised computer-use spine for provider authorization.
- [x] Add `bootstrap` and `doctor` commands for self-contained runtime setup.
- [x] Add `--llm-auth-mode auto|api-key|openclaw` and OpenClaw device-code support.
- [x] Add basic validation tests.

## Milestone 2: App Scanner And Manifest

- [x] Implement repo scanner for common website stacks.
- [x] Detect environment variable usage.
- [x] Detect provider SDK usage.
- [x] Detect Plaid dependencies/env and emit a provider-pack service with a generated pack path.
- [x] Detect DNS/domain and webhook needs where possible.
- [x] Generate `fusekit.yaml` with required services, capabilities, approvals, and user-required steps.

## Milestone 3: Planning Engine

- [x] Represent setup actions as automatic, user-required, or approval-required.
- [x] Generate plain-English and JSON setup plans.
- [x] Fail closed on unknown high-risk setup actions.
- [x] Add tests for plan generation and risk classification.

## Milestone 4: Cipher Vault Bundle

- [x] Implement memory-hard KDF.
- [x] Implement authenticated encryption.
- [x] Define credential bundle schema.
- [x] Store generated passwords, account credentials, provider tokens, API keys, DNS settings, SSH keys, webhook secrets, and sensitive setup settings as encrypted records.
- [x] Ensure someone who opens the encrypted vault file without the passphrase cannot understand its contents.
- [x] Add wrong-passphrase and no-plaintext tests.

## Milestone 5: Vault Runtime And Sessions

- [x] Unlock bundle into memory only.
- [x] Create short-lived local vault session tokens.
- [x] Implement secret broker.
- [x] Implement capability request and response schemas.
- [x] Deny raw-secret export attempts.
- [x] Ensure tokens are not written to logs or receipts.

## Milestone 6: Policy And Approvals

- [x] Implement default-deny policy.
- [x] Implement allow, deny, and require-approval matching.
- [x] Require approval for production DNS, billing, payment, destructive infra, and arbitrary SSH commands.
- [x] Add approval CLI or local approval UI. V1 uses explicit CLI approval flags.
- [x] Add fail-closed tests.

## Milestone 7: Working Real Provider Adapters

- [x] Define provider authorization contracts.
- [x] Implement GitHub repo secret and deploy key adapter.
- [x] Implement Vercel project and environment variable adapter.
- [x] Implement DNS propose/apply adapter for one provider.
- [x] Implement webhook create/verify utilities.
- [x] Declare account creation support in provider packs. API account creation is fail-closed unless the pack names a real setup recipe; current public catalog packs use supervised account gates.
- [x] Use supervised user handoff when provider APIs require browser login, CAPTCHA, MFA, billing, or identity checks, then capture approved resulting credentials into the encrypted vault.
- [x] Open provider signup/token/project pages for GitHub, Vercel, and Cloudflare during authorization handoff.
- [x] Add OpenClaw browser spine adapter and provider authorization playbooks.
- [x] Add Playwright internal/dev fallback adapter and provider UI playbooks for GitHub, Vercel, Cloudflare, and Resend.
- [x] Add bounded inferred UI navigation loop with LLM-planned actions and service-gate stops.
- [x] Allow the inferred UI navigation loop to run through OpenClaw by default, with Playwright as a fallback adapter.
- [x] Make inferred UI service gates durable waits that retry/resnapshot instead of terminal stops.
- [x] Add action-plane evidence around inferred navigation: observation snapshots, trace hooks, after-action snapshots, safe-key/HTTPS validation, and redacted blocked-action recovery.
- [x] Harden OpenClaw browser action failures so subprocess stderr follows the same browser-output redaction path as structured spine results before reaching ProviderError messages.
- [x] Feed provider verification failures back into the inferred UI loop for targeted repair before final failure.
- [x] Use browser highlight primitives to guide the human at service-created gates without bypassing provider controls.
- [x] Detect Resend email dependency and add Resend provider handoff metadata.
- [x] Add stump classification, follow-me guidance, human takeover/resume, provider-memory hints, and token-backed API fallback to the inferred provider UI loop.
- [x] Add OpenClaw OpenAI auth fallback that records OpenClaw auth-profile state into the encrypted vault when present.
- [x] Add validated provider capability-pack synthesis with a real-capable Plaid setup recipe.
- [x] Add `fusekit provider synthesize`, `provider validate`, and `provider list`.
- [x] Add `fusekit provider verify` and capability-pack verification integrated into `apply`.
- [x] Add Resend and Plaid executable pack verification recipes.
- [x] Move `apply` off provider-specific CLI branches and onto provider-pack setup execution.
- [x] Add secret-routing safeguards so wildcard setup recipes only route app/runtime env secrets, not provider auth tokens or runner/LLM credentials.
- [x] Add setup recipe validation so explicit app env-store recipes cannot route provider auth tokens into GitHub/Vercel secrets.
- [x] Add validation that rejects LLM-generated HTTP verification recipes that send secrets to domains outside provider-owned/documented hosts.
- [x] Add provider intelligence loop that collects app evidence, drafts a pack through a heuristic or OpenAI-compatible LLM source, validates, repairs, and writes the cached pack.

## Milestone 8: Real Website Setup Path

- [x] Pick one initial website stack.
- [x] Scan app and generate setup manifest.
- [x] Provide one-command setup orchestration for scan, plan, authorization, apply, verify, receipt, and detonation.
- [x] Connect GitHub repo, including private source retrieval through supervised GitHub App/PAT authorization without local SSH setup.
- [x] Connect deployment target adapter path. Live proof still depends on supervised provider authorization.
- [x] Configure environment variables through provider-pack setup. Live proof still depends on supervised provider authorization.
- [x] Propose and optionally apply DNS.
- [x] Verify deployed website health.
- [x] Store all sensitive setup material in encrypted vault bundle.
- [ ] Document the real acceptance run in README. Reproduction steps are documented; real run log is pending supervised authorization.

## Milestone 9: Audit, Receipt, And Detonation

- [x] Implement redaction utilities.
- [x] Implement JSONL audit log.
- [x] Implement non-secret setup receipt.
- [x] Implement detonation cleanup.
- [x] Preserve encrypted bundle, redacted audit log, and redacted setup receipt.
- [x] Detonate FuseKit-owned OpenClaw state after launches that used OpenClaw auth or provider handoff so OAuth/profile state does not remain as plaintext worker access.
- [x] Add no-secret scanning tests for logs, receipts, terminal summaries, and temp files.

## Milestone 10: Launch Hardening

- [x] Rewrite README around the real setup flow.
- [x] Add safety model diagram.
- [x] Add local control-room HTML artifact for runner/job visibility.
- [x] Add live control-room server for runner/job visibility.
- [x] Add plaintext secret-leak scanner for repos and artifacts.
- [x] Add rollback planning and start-over cleanup commands.
- [x] Add rollback planning from provider capability-pack rollback metadata.
- [x] Add provider authorization guide.
- [x] Add launch acceptance harness with redacted proof ledger and launch-readiness report.
- [x] Add public acceptance app/runbook for the launch recording path.
- [x] Add threat model.
- [x] Run full tests.
- [x] Verify package install from fresh checkout.
- [x] Verify `.gitignore` blocks vault bundles, logs, receipts, and worker temp files.

## Milestone 11: OCI Clean-Room Runner

- [x] Design the OCI runner lane in `docs/oci-runner-lane.md`.
- [x] Add runner selection to `launch`: `--runner auto|local|oci-cloud-shell|oci-free|oci-existing`.
- [x] Add `fusekit runner` command group.
- [x] Add OCI auth planner with modes: `auto`, `existing-config`, `browser-session`, and `api-key-upload`.
- [x] Add supervised OCI signup/login/API-key handoff through OpenClaw.
- [x] Inline OCI browser-session authorization into `launch` with retrying service-gate waits.
- [x] Store generated OCI signing keys and runner config only in the encrypted vault.
- [x] Add OCI SDK dependency.
- [x] Implement compartment, VCN, subnet, route, internet gateway, NSG, and VM provisioning.
- [x] Implement shape/availability-domain retry and capacity-gate loop.
- [x] Implement cloud-init bootstrap artifact for FuseKit, OpenClaw, browser dependencies, and verification.
- [x] Install Playwright Chromium in the OCI VM runner bootstrap.
- [x] Implement SSH upload/exec/download protocol without command-line passphrase or secret leakage.
- [x] Upload the encrypted FuseKit vault, but not `.fusekit` scratch state, into the remote OCI worker.
- [x] Implement OCI workspace detonation: instance, boot volume, networking, generated API key, SSH keys, and remote worker state.
- [x] Add durable remote runner loop entrypoint for OCI VM execution.
- [x] Add OCI Cloud Shell deeplink/fallback bootstrap plan and local launcher HTML.
- [x] Forward provider/domain/live URL/inference launch intent through the OCI Cloud Shell bootstrap into the clean-room worker.
- [x] Allow Cloud Shell and OCI VM bootstrap to install a selected FuseKit package/repo instead of silently defaulting to PyPI.
- [x] Support OCI Cloud Shell delegation-token config loading for SDK calls.
- [x] Add local doubles for OCI API tests.
- [ ] Add one documented supervised real OCI acceptance run.

## Milestone 12: Super-Magic Launch UX

- [x] Add `fusekit control-room --serve` live UI endpoint.
- [x] Upgrade `fusekit control-room` into a responsive user-facing launch monitor with full job refresh.
- [x] Add `fusekit leak-scan`.
- [x] Add `fusekit rollback`.
- [x] Add `fusekit start-over`.
- [x] Add `fusekit-runner-loop` remote VM entrypoint.
- [x] Add OCI Cloud Shell bootstrap lane.
- [x] Persist provider/OCI/DNS/LLM human-gate state in `.fusekit/gates.json` so gates can be resurfaced instead of treated as one-shot failures.
- [x] Forward launch context and verification retry settings into remote OCI worker launches.
- [x] Expose verification polling with `--verify-attempts` and `--verify-retry-seconds`.
- [x] Add OpenClaw-backed provider docs/UI research before provider-pack drafting. Full ref-based provider action playbooks still need broader coverage.
- [x] Add acceptance harness for scan/plan/pack/vault/receipt/leak-scan/detonation proof artifacts.
- [x] Add provider capability catalog for common AI-built app services. Catalog-backed packs now cover Stripe, Supabase, Clerk, Neon, Upstash, OpenAI, Resend, and Plaid paths with conservative verification where provider-native recipes are not yet implemented.
- [x] Catalog live-wiring hardening: provider token detection and env capture now derive from provider-pack handoff metadata, so newly cataloged providers do not need bespoke token-map code before launch/setup can use approved env credentials.
- [x] Add stronger provider-pack provenance, endpoint-purpose validation, and tool-permission binding. Generic provider-pack verification covers env, HTTP JSON, DNS records, and URL health; demo-path provider verification covers GitHub secrets/deploy keys, Vercel project/env/deployment URL, Cloudflare API records plus DNS propagation, Resend domain/API health, app health, and webhook signature secrets.
- [x] Polish the public Moonlite RSVP acceptance surface so the live setup path looks credible in recordings.
- [x] Harden central Run Record provider-gate proof so durable gate guidance keeps provider/status/target/wake shape while redacting callback URLs, provider URLs, tokens, copied gate text, and poisoned structural gate fields before the public proof object is written.
- [x] Harden Run Record approval proof so hand-edited approvals cannot stand in for a protected control-room resume event or drift from durable gate state.
- [x] Harden Run Record artifact/evidence paths so outside-root worker logs can be labeled without leaking local paths or counted as public evidence.
- [x] Harden Run Record artifact-list proof so hand-edited rows cannot carry absolute paths, traversal, token query text, missing names, or non-boolean existence flags.
- [x] Harden Run Record numeric proof so boolean values cannot stand in for gate totals, wake-event counts, evidence counts, verifier counts, or audit counts.
- [x] Harden embedded Run Record provider strategy/playbook proof so route decisions remain comparable to `provider_strategies.json` while unsafe guidance text is stripped from the public record.
- [x] Harden embedded Run Record verifier proof so status summaries still match `verification_report.json` while unsafe verifier repair details are stripped from the public record.
- [x] Harden embedded Run Record verification proof so malformed extra verifier check rows cannot hide behind signature matching in live acceptance.
- [x] Harden embedded Run Record model/inference contract proof so unsafe LLM contract text is stripped from the public record and raw-secret LLM survivors block launch readiness.
- [x] Harden model/inference summary drift so live acceptance and cleanup preflight reject mismatches across provider, model, base URL, API-key env, auth mode, required flags, default lane, and status before inference readiness can pass.
- [x] Harden recording model/inference drift so the final recording checklist uses the same provider, model, base URL, API-key env, auth mode, required flags, default lane, status, and lane-count alignment as live acceptance and cleanup preflight.
- [x] Harden recording model-summary exactness so the final recording checklist rejects sidecar or padded `model_inference` fields before inference proof can be marked recordable.
- [x] Harden recording LLM-contract identity so the final recording checklist requires generated contract base URL, record id, auth mode, and literal `required`/`can_proceed_without_api_key` flags before model inference can be marked recordable.
- [x] Harden recording LLM-contract exactness so the final recording checklist rejects sidecar contract, security, or lane fields and requires the full generated public security block before model inference can be marked recordable.
- [x] Harden embedded Run Record model/inference lane proof so the contract must describe concrete public-safe auth lanes and the model summary must count them accurately.
- [x] Harden model/inference type proof so live acceptance and cleanup preflight reject non-boolean `required`/`can_proceed_without_api_key` flags and non-integer lane counts before inference readiness can pass.
- [x] Harden model/inference public text so live acceptance and cleanup preflight reject padded or unsafe model summary, LLM contract, and contract-security strings before inference proof can support launch readiness.
- [x] Harden recording model/inference public text so the final recording checklist rejects callback URLs or credential-looking model summary, LLM contract, security, and lane-description text before inference proof can be marked recordable.
- [x] Harden recording LLM-lane identity so the final recording checklist rejects padded or credential-looking lane ids and labels before lane proof can be marked recordable.
- [x] Harden model/inference lane identity so live acceptance and cleanup preflight reject padded or duplicate `llm_contract.lanes` ids, labels, and descriptions before lane counts can support launch readiness.
- [x] Harden model/inference selected-lane readiness so live acceptance and cleanup preflight reject default or encrypted-status lanes that are unavailable or still require user action when the contract claims ready auth.
- [x] Harden generated model/inference default-lane proof so encrypted API-key contracts default to the API-key lane unless an encrypted OpenClaw profile actually exists, and recording readiness applies the same selected-lane availability rule.
- [x] Harden model/inference contract exactness so live acceptance and cleanup preflight reject sidecar fields in `model_inference`, `llm_contract`, contract security, or lane rows before inference proof can support launch readiness.
- [x] Harden embedded Run Record workspace detonation proof so cleanup receipts remain comparable to `workspace_detonation.json` while unsafe callback or token-shaped cleanup text is stripped from the public record.
- [x] Harden setup-receipt-derived Run Record audit proof so provider/DNS action rows retain receipt indexes while unsafe action or status text is stripped from the public record.
- [x] Harden standalone survivor artifacts so callback URLs cannot remain in provider strategies, verifier reports, rollback metadata, setup receipts, model/inference contracts, or workspace detonation receipts after redacted Run Record signatures match.
- [x] Harden durable gate and wake-event survivors so raw `gates.json` and `gate_events.jsonl` cannot preserve callback URLs or token-shaped return data while still allowing ordinary provider setup/review URLs.
- [x] Harden provider capability-pack acceptance so structurally valid but unsafe packs cannot preserve callback URLs or token-shaped text in per-provider snapshots, and one failed required pack blocks the aggregate provider-pack readiness gate.
- [x] Harden visual-session survivor proof so expected noVNC/control-room transport fields keep their specialized URL validation while raw `visual.json` still has the exact generated fields, display proof, and guidance notes, with no sidecar metadata or callback/token-shaped return data.
- [x] Harden worker-replacement drill proof so restored source labels must exactly match the durable replacement contract and unsafe drill text is stripped before it reaches the central Run Record.
- [x] Harden embedded and snapshotted runner-readiness proof so the central Run Record keeps the x86/browser contract, acceptance snapshots replace VM-local profile/cache/binary paths with public labels, and raw readiness survivors cannot carry callback URLs or token-shaped return data.
- [x] Harden base Run Record identity fields so app paths are public labels rather than local/VM absolute workspace paths.
- [x] Harden embedded run-state proof so stale callback URLs or token-shaped notes are stripped before the launch state contract enters the central record.
- [x] Harden Run Record vault metadata so credential records prove exact id/kind/provider/label shape without preserving raw values, passphrases, private keys, token-shaped metadata, duplicate ids, blank rows, padded text, or sidecar fields, and make live acceptance enforce that vault proof shape recursively.
- [x] Harden audit-log-derived Run Record proof so poisoned event names cannot preserve callback URLs or token-shaped text in central audit actions or summaries.
- [x] Harden embedded acceptance-summary proof so stale acceptance blockers or errors cannot preserve callback URLs or token-shaped text in the central record.
- [x] Canonicalize embedded acceptance-summary writing: Run Records now trim/de-duplicate missing proof, narrow blocker cards to public recovery fields, add fallback blockers for missing proof, and trim redacted error text before central validation.
- [x] Harden embedded acceptance-summary shape proof so non-empty central summaries must carry literal ready booleans, a blockers list, and string error text before live acceptance can pass.
- [x] Align detonation preflight with embedded acceptance-summary shape proof so cleanup cannot trust a central Run Record with a missing or malformed acceptance section.
- [x] Harden embedded acceptance-summary truth proof: live acceptance and cleanup preflight now reject stale ready flags that contradict launch readiness, blockers, errors, or `recording_contract.recording_ready`.
- [x] Harden embedded acceptance-summary mode proof: central Run Records now preserve report mode, and readiness summaries must declare `live` before `public_launch_ready` or `recording_ready` can support launch or cleanup trust.
- [x] Harden embedded acceptance-summary recording-proof flag: central Run Records now preserve `recording_proof_ready`, and acceptance/preflight require it to be boolean, required by `recording_ready`, and aligned with `recording_contract.recording_ready`.
- [x] Harden embedded acceptance-summary missing-proof list: central Run Records now preserve redacted `missing` strings, and acceptance/preflight require that list to be shaped and empty before ready flags can support launch or cleanup trust.
- [x] Harden embedded acceptance-summary missing-proof identity: live acceptance and cleanup preflight now reject blank or duplicate embedded `missing[]` proof strings so launch-evidence lists cannot be inflated.
- [x] Harden embedded acceptance-summary recovery text normalization: live acceptance and cleanup preflight now reject surrounding whitespace in embedded `missing[]` strings and blocker-card text fields.
- [x] Harden embedded acceptance-summary blocker detail shape: live acceptance and cleanup preflight now reject empty `detail` fields when optional blocker detail is present.
- [x] Harden embedded acceptance-summary missing-to-blocker consistency: live acceptance and cleanup preflight now require every embedded `missing[]` proof string to have a matching blocker `item`.
- [x] Harden embedded acceptance-summary blocker-card shape: live acceptance and cleanup preflight now require every embedded blocker row to carry non-empty `item`, `category`, and `next_action` strings plus optional string `detail`.
- [x] Harden embedded acceptance-summary blocker identity: live acceptance and cleanup preflight now reject duplicate embedded blocker `item` values after trimming so recovery maps cannot be inflated by repeated cards.
- [x] Harden embedded acceptance-summary blocker schema: live acceptance and cleanup preflight now reject unexpected embedded blocker sidecar fields beyond `item`, `category`, `next_action`, and optional `detail`.
- [x] Acceptance-summary shape authority: a shared acceptance-summary contract now owns the embedded acceptance envelope, readiness booleans, blocker-card fields, and unresolved-error fields consumed by Run Record generation, live acceptance, and detonation preflight, so launch recovery proof cannot drift across public surfaces.
- [x] Harden embedded acceptance-summary envelope exactness: live acceptance and cleanup preflight now reject unexpected top-level acceptance sidecar fields before central readiness proof can support launch or worker cleanup.
- [x] Harden embedded acceptance-summary mode string proof: live acceptance and cleanup preflight now require exact `live` or `rehearsal` mode strings instead of trimming whitespace-padded values into proof.
- [x] Harden embedded acceptance-summary readiness formulas: live acceptance and cleanup preflight now recompute `public_launch_ready` and `recording_ready` from mode, launch readiness, and recording-proof readiness instead of trusting hand-edited booleans.
- [x] Harden embedded acceptance-summary error truth: live acceptance and cleanup preflight now reject any non-empty embedded acceptance error whenever launch, public launch, or recording readiness is true.
- [x] Harden embedded acceptance-summary error text shape: live acceptance and cleanup preflight now require embedded acceptance errors to be empty or trimmed non-empty text.
- [x] Harden recording-contract blocker shape: live acceptance now rejects non-string, empty, padded, or duplicate blocker rows before public recording proof can pass.
- [x] Harden pre-detonation recording-contract blockers: cleanup preflight now rejects non-string, empty, padded, or duplicate blocker rows before trusting `detonation` as the lone pending recording section.
- [x] Harden embedded acceptance-summary unresolved-error truth: live acceptance and cleanup preflight now reject any launch, public-launch, or recording-ready embedded acceptance flag while unresolved Run Record errors remain.
- [x] Harden Run Record unresolved-error proof: live acceptance and cleanup preflight now reject padded error source/id/detail fields and duplicate source/id error identities.
- [x] Harden Run Record unresolved-error exactness: live acceptance and cleanup preflight now reject sidecar error fields beyond `source`, `id`, and `detail` before error proof can support launch readiness or cleanup trust.
- [x] Canonicalize generated Run Record error proof: the writer now trims/redacts source, id, and detail fields, fills blank details with public fallback text, and de-duplicates source/id rows before unresolved errors reach public acceptance or cleanup preflight.
- [x] Harden human-action trace row proof: live acceptance and cleanup preflight now reject padded generated fields and unexpected sidecar fields in guided human-action rows.
- [x] Harden rehearsal-review action proof: live acceptance and cleanup preflight now reject padded reviewed-action fields and unexpected sidecar fields before human-action review proof can pass.
- [x] Harden provider-playbook step proof: live acceptance and cleanup preflight now reject padded step fields and unexpected sidecar fields before route-plan proof can support recording or cleanup trust.
- [x] Harden provider-playbook safety-note proof: live acceptance and cleanup preflight now reject padded, duplicated, empty, or object-shaped safety guidance before public recording or cleanup trust can pass.
- [x] Align control-room public recording recovery copy with the strict `fusekit acceptance run --mode live --require-recording` launch gate, so soft launch-ready reports cannot be mistaken for recordable proof.
- [x] Harden detonation preflight against hollow recording contracts: cleanup can proceed only when `detonation` is the lone remaining recording-contract blocker.
- [x] Harden recording-contract exactness: live acceptance and cleanup preflight now reject sidecar top-level or check fields before recordability proof can support public launch or worker cleanup.
- [x] Harden detonation preflight against recording-contract section drift: every true non-detonation recording check must have its matching Run Record proof section present before worker cleanup.
- [x] Harden detonation preflight against scalar recording-section placeholders: recording-contract section proof must be a non-empty object/list, not a string or number.
- [x] Harden detonation preflight against anonymous Run Record identity: cleanup preflight now requires central Run Record id/status/runner and a public-label app path before worker destruction is trusted.
- [x] Harden detonation preflight against `errors_empty` drift: the recording checklist must match the actual Run Record `errors` list before cleanup.
- [x] Harden detonation preflight against malformed error proof: cleanup preflight now requires an explicit Run Record `errors` list and validates unresolved error source/id/detail fields plus public-safe text before worker destruction is trusted.
- [x] Canonicalize generated Run Record timeline proof: the writer now redacts/trims step and checkpoint rows, collapses duplicate ids, preserves generated `updated_at` and checkpoint `mascot_state` fields, and normalizes malformed timestamps before public acceptance or cleanup preflight sees them.
- [x] Timeline proof shape authority: a shared timeline-proof contract now owns Run Record step fields, checkpoint fields, required text fields, optional recovery text fields, and timestamp proof consumed by Run Record generation, live acceptance, and detonation preflight, so recovery timeline proof cannot drift across launch gates.
- [x] Harden detonation preflight against truthy pending-safe verifier proof: cleanup now accepts only literal JSON `true` for nested or top-level `pending_safe` fields.
- [x] Harden standalone and embedded verification-report row proof: shared validation now rejects padded provider/check/status text, empty generated summary or repair text, non-object details, and unexpected sidecar fields before verifier signatures can support public readiness or cleanup trust.
- [x] Verification-report shape authority: the verification report writer now owns the check-row fields, text fields, schema version, status/count vocabulary, safe statuses, and pending-safe check ids consumed by detonation preflight and live acceptance, so verifier proof cannot drift between artifact generation and launch gates.
- [x] Harden detonation preflight against contextless verifier proof: cleanup now requires the central verifier summary statement to explain live provider verifiers, green checks, and pending-safe checks before worker destruction is trusted.
- [x] Harden standalone verification-report identity proof: shared validation now rejects anonymous passed rows missing provider, check, or status before live acceptance or detonation preflight can trust the report.
- [x] Harden Run Record verifier-summary row proof: live acceptance and cleanup preflight now reject padded verifier identities, non-boolean pending-safe flags, and unexpected sidecar fields before green verifier proof can support public recording or cleanup trust.
- [x] Harden public verifier coverage proof: recording readiness, live acceptance, standalone verification coverage, and detonation preflight no longer count skipped rows as GitHub, Resend, Vercel, DNS/Cloudflare, or `live_app` launch proof.
- [x] Harden detonation preflight against hollow LLM auth-lane proof: cleanup now requires shaped `llm_contract.lanes`, required encrypted-auth lane coverage, and matching `model_inference.lane_count`.
- [x] Harden detonation preflight against placeholder public proof: provider-playbook and verifier-summary sections must be shaped and cover GitHub, Resend, Vercel, DNS/Cloudflare, and `live_app` before cleanup.
- [x] Harden detonation preflight against placeholder provider-strategy proof: central route rows must be shaped and cover public provider families before survivor drift comparison can support cleanup.
- [x] Harden provider-strategy fallback proof: live acceptance and cleanup preflight now require every fallback candidate row to carry kind/status evidence before route proof can support readiness or cleanup.
- [x] Harden detonation preflight against thin provider-strategy route proof: selected routes now require deterministic/implemented booleans, route reasons, and follow-me fields for human-gate strategies before cleanup can trust the central Run Record.
- [x] Harden live acceptance central provider-strategy proof: Run Record route rows now require deterministic/implemented/reason evidence, provider-specific Resend route evidence, human-gate follow-me fields, and drift signatures that include route rationale/evidence.
- [x] Harden detonation preflight against misleading Resend API route proof: cleanup now requires selected-route evidence that FuseKit owns Resend domain/audience setup, users do not manually create those resources, and domain setup precedes DNS approval.
- [x] Harden detonation preflight against stale provider-playbook semantics: cleanup now enforces route actor ownership, route-specific controls, proof sources, resume events, unsafe-instruction rejection, safety-note quality, and Resend/Vercel-before-DNS ordering before survivor drift comparison can support cleanup.
- [x] Harden standalone provider-strategy survivor exactness: cleanup preflight now rejects sidecar or padded `provider_strategies.json` top-level, provider, strategy, decision, selected-route, candidate, and embedded playbook-step proof before normalized signatures can support worker destruction.
- [x] Harden live acceptance provider-strategy survivor exactness: live acceptance now rejects sidecar or padded `provider_strategies.json` top-level, provider, strategy, decision, selected-route, and candidate proof before route decisions can support public launch readiness.
- [x] Harden detonation preflight against placeholder human proof: `human_actions` and `rehearsal_review` must be shaped, count-backed, bound to durable provider gates, and matched to visible control-room instructions before cleanup.
- [x] Harden detonation preflight against placeholder artifact/evidence proof: Run Record artifact rows and evidence inventory must be public-relative, unique, count-backed, and non-secret before cleanup.
- [x] Failed-run OCI cleanup recovery: `fusekit runner detonate` can recover from a retained runner by loading `.fusekit/oci_workspace.json` when the encrypted workspace record is unavailable, and verified VM plus boot-volume destruction now satisfies remote-worker cleanup proof while partial cloud deletion still fails closed.
- [x] Harden central acceptance recordability proof: embedded Run Record acceptance summaries now require `remote_artifacts_ready`, and live acceptance plus cleanup preflight recompute recording readiness from public launch, retrieved survivor artifacts, and recording-contract proof.
- [x] Harden clean-rehearsal non-empty proof: provider-gate runs can no longer satisfy live acceptance, recording readiness, or cleanup preflight with empty human-action and rehearsal-review rows.
- [x] Canonicalize generated clean-rehearsal proof: Run Record writing now marks `rehearsal_review` as `needs_review` whenever provider gates or wake events exist but no guided human-action rows were recorded.
- [x] Bind generated clean-rehearsal proof to automation ownership: Run Record writing now also treats automation-boundary `human_gate` routes as requiring guided human actions, so route-led provider handoffs cannot look rehearsed before the control-room action trace exists.
- [x] Canonicalize generated acceptance readiness proof: Run Record writing now recomputes embedded acceptance `launch_ready`, `public_launch_ready`, `recording_proof_ready`, and `recording_ready` from blockers, missing proof, errors, remote artifacts, and the generated recording contract instead of preserving stale report booleans.
- [x] Align generated detonation preflight proof with the recording contract: Run Record writing now keeps `detonation.preflight_safe=false` until every non-detonation recording-contract check is true, so stale run-state cleanup readiness cannot invite worker destruction before launch proof is complete.
- [x] Harden control-room acceptance readiness loading: static/live payloads and refresh cards now downgrade `launch_ready` and `public_launch_ready`, then prioritize actionable blocker cards, when an acceptance report still carries missing proof, blockers, or an error even if stale ready booleans remain.
- [x] Harden protected control-room action bodies: `/pass` and `/open` now reject request bodies after auth/CSRF/action-token checks, keeping resume/open actions limited to pre-existing durable gate ids while `/capture-clipboard` remains the only bounded JSON mutation route.
- [x] Harden remote survivor inventory file proof: live acceptance now requires each retrieved OCI survivor to be a regular file, distinguishing missing artifacts from non-file placeholders before `remote_artifacts.loaded` can support recording readiness.
- [x] Harden remote artifact source proof: live acceptance rejects `--remote-artifacts` when it resolves to the app's active `.fusekit` scratch directory, so public recording proof must come from a retrieved OCI survivor bundle.
- [x] Harden remote artifact directory proof: live acceptance rejects symlinked remote-artifact roots or symlinked `.fusekit` bundle directories before resolving paths, so retrieved survivor evidence cannot be borrowed from host-local directories.
- [x] Harden remote artifact allowlist proof: live acceptance rejects unexpected top-level files or directories in retrieved `.fusekit` bundles, so plaintext leftovers such as dotenvs, logs, browser state, or hand-built sidecars cannot ride along with public survivor evidence.
- [x] Harden remote artifact retrieval allowlist proof: OCI artifact retrieval now uses the shared survivor bundle contract and fails before detonation trust when the downloaded `.fusekit` bundle contains unexpected top-level entries.
- [x] Align remote artifact fetch with VM/browser survivor proof: OCI artifact retrieval now builds the SSH tar fetch list from the shared pre-detonation survivor contract and includes optional `visual.json`, so final live acceptance can prove the noVNC/control-room browser session from retrieved artifacts.
- [x] Harden pre-detonation retrieval phase proof: OCI artifact validation now rejects stale final-only survivors such as `workspace_detonation.json` plus linked or non-file survivor placeholders in reused artifact directories before cleanup proof can be trusted.
- [x] Harden remote artifact extraction freshness: retrieving a new OCI artifact archive now clears the previous local `.fusekit` bundle after confirming the archive is readable, so stale survivor files cannot satisfy validation for a partial fresh download.
- [x] Harden remote artifact extraction failure recovery: unsafe or empty artifact archives are rejected before clearing the previous local `.fusekit` bundle, preserving diagnostic survivor evidence until a readable archive with safe file members replaces it.
- [x] Harden remote artifact replacement atomicity: OCI artifact extraction now stages the downloaded survivor files in a temporary bundle, requires a complete fresh `.fusekit` survivor set, and rolls back to the previous local survivor bundle if copy, validation, or replacement fails.
- [x] Harden remote artifact archive exactness: OCI artifact extraction now rejects archive members outside the pre-detonation survivor contract, linked survivor entries, and duplicate survivor file rows before staging, so retrieval proof cannot depend on ignored leftovers, nested scratch files, or ambiguous last-writer-wins tar behavior.
- [x] Harden final detonation receipt attachment: successful OCI cleanup now atomically copies `workspace_detonation.json` only into an existing regular retrieved `.fusekit` bundle and refuses missing, linked, or non-file survivor targets, so final proof cannot be written through host-local links, half-written, or invented after retrieval.
- [x] Harden final detonation receipt temp handling: stale hidden temp files or symlinks are cleared before exclusive receipt writes, while temp directories fail closed, so the atomic post-cleanup receipt copy cannot follow host-local temp links or overwrite ambiguous placeholders.
- [x] Harden final Run Record detonation refresh: after successful OCI cleanup, FuseKit now syncs the final run-state survivor into retrieved remote artifacts and regenerates remote `run_record.json` so the central proof object agrees with `workspace_detonation.json` before live acceptance.
- [x] Harden final Run Record attachment atomicity: the post-detonation remote Run Record refresh now rejects missing, symlinked, or non-file survivor paths; stages `job.json`, `checkpoints.json`, and `run_record.json` through guarded temporary files; and preserves the previous survivor set if final proof regeneration fails.
- [x] Harden final timeline drift acceptance: live acceptance now compares overlapping timeline ids in retrieved `job.json` and `checkpoints.json` against the central Run Record, so interrupted final proof promotion cannot leave contradictory detonation step/checkpoint status in public survivor evidence.
- [x] Harden remote survivor symlink proof: live acceptance rejects linked survivor paths in retrieved artifact bundles before reading public JSON proof, so host-local files cannot stand in for OCI survivor evidence.
- [x] Harden remote survivor inventory content proof: live acceptance now flags zero-byte required survivor files as empty placeholders before `remote_artifacts.loaded` can pass, while still allowing an empty `gate_events.jsonl` stream for runs with no wake events.
- [x] Harden generated Run Record artifact proof: artifact rows now mark `exists: true` only for regular files, so directory placeholders cannot enter the central product object as survivor evidence.
- [x] Harden Run Record standalone-artifact drift reads: live acceptance now fails central Run Record proof when required `llm_contract.json` is missing, and treats present standalone comparison artifacts such as `provider_strategies.json`, `verification_report.json`, `workspace_detonation.json`, `runner_readiness.json`, or `llm_contract.json` as failed proof if they are non-file, unreadable, or not JSON objects instead of silently skipping drift checks.
- [x] Harden detonation preflight JSON survivor reads: cleanup trust now records labeled failures when required public JSON survivors are unreadable or not JSON objects instead of collapsing them into anonymous empty proof before downstream checks run.
- [x] Harden audit JSONL survivor non-empty proof: live acceptance and cleanup preflight now reject blank `audit.jsonl` survivors with no JSON object rows, so file presence alone cannot stand in for credential-capture, provider-action, approval, or cleanup audit evidence.
- [x] Harden live gate-event survivor exactness: live acceptance now applies the generated wake-event row shape to raw `gate_events.jsonl` before Run Record signature comparison, matching cleanup preflight and rejecting sidecar wake proof before launch readiness can pass.
- [x] Harden live provider-gate survivor exactness: live acceptance now applies the generated provider-gate envelope and row shape to raw `gates.json` before gate readiness, matching cleanup preflight and rejecting sidecar gate proof before launch readiness can pass.
- [ ] Complete supervised public real acceptance run. README now documents the exact supervised run protocol and current status.
- [ ] Ship hosted GitHub intake for `fusekit.snowmanai.org`: sign in, install/select repo, verify signed callback state, scan, visible plan, control room, proof page, rollback, and detonation without terminal/download steps. Current open-core slice covers the signed install link, pre-install trust/open-core/no-terminal/proof/reversal JSON, source repository/license/entrypoint proof, callback receipt, selected-repository listing, source fetch/scan, visible hosted plan with approved action ids, public control-room shell, redacted hosted-worker contract, browser-visible permission boundary, browser-visible provider gate checklist, signed stateless job handoff, browser-facing proof receipt page, redacted hosted readiness contract with config-shape validation and fail-closed GitHub intake routing, redacted job status API, backend-only source preparation with internal installation-token exchange and approved-plan drift rejection, backend-only launch/acceptance invocation with public path redaction, backend-only worker-proof payload assembly from real acceptance/artifact evidence, `fusekit-hosted-worker` one-job claim/run/proof entrypoint, production-required signed worker-dispatch wakeup for the hosted button, HMAC-verified dispatch receiver with readiness and duplicate-dispatch idempotency markers, outside-in hosted verifier with Cloudflare 1000 diagnosis, durable worker-dispatch idempotency checks, homepage/deployment/pre-install intake trust drift detection, homepage reversible-step drift detection, parsed homepage embedded-contract drift detection, canonical `fusekit.snowmanai.org` deployment-contract drift detection, public GitHub token-boundary drift detection, public worker-dispatch contract drift detection, public HTML/JSON credential-text leak detection, and operator setup contract drift detection, public operator setup checklist for the selected hosted provider, Cloudflare CNAME/A-record, and worker dispatch receiver, redacted protected-action receipts surfaced in browser after button clicks, hosted worker maintenance mode for rollback/detonation requests with rollback execution/post-verification and detonation action proof gating, browser-native protected request controls, Vercel/AWS-compatible WSGI entrypoint, and OCI Compute hosted-origin provenance with Cloudflare A-record verifier support; the production service still needs live GitHub App secret installation and live provider rollback/detonation execution proof. The permanent OCI host path uses an explicitly tagged AMD/x86_64 compute origin rather than the disposable OCI runner VM lane.

  The hosted browser UI now also shows the reversal playbook in both the control room and proof receipt so the one-click launcher remains reversible without a terminal workflow.

## North Star Audit Remediation

- [x] P0-1: Prevent wildcard secret routes from copying provider auth tokens into generated app/deploy env stores.
- [x] P0-1b: Prevent explicit app env-store setup recipes from routing provider/runner/LLM auth material.
- [x] P0-2: Reject secret-bearing dynamic HTTP verification recipes unless their destinations match provider documented domains or explicit validated provider hosts.
- [x] P0-3: Introduce a provider intelligence loop that can use an OpenAI-compatible LLM to draft, repair, validate, and cache provider packs.
- [x] P1-1: Replace the setup executor's provider-specific branch chain with a recipe handler registry.
- [x] P1-2: Treat unknown required verification recipes as failed, while preserving optional skips.
- [x] P1-3: Replay launch context when delegating to a remote OCI worker.
- [x] P1-4: Add verification polling/pending semantics and expose retry settings through CLI and remote launch.
- [x] P2-1: Add durable gate records for provider, OCI, DNS, plan, and LLM authorization waits.
- [x] P2-2: Add provider-pack rollback intent planning.
- [x] P2-3: Add optional OpenClaw installer version/hash pinning.
- [x] Fixed gap 1: provider intelligence can browse provider docs/UI through OpenClaw before drafting packs.
- [x] Fixed gap 2: scanner evidence now includes routes, config files, webhook handlers, OAuth callbacks, env syntax variants, and custom domain candidates.
- [x] Fixed gap 3: generated packs now carry non-secret provenance, endpoint purpose declarations, and setup/verify tool-permission bindings.
- [x] Fixed gap 4: rollback can execute provider-native delete/revoke/restore paths for GitHub repo secrets/deploy keys, Vercel env/project resources, and Cloudflare DNS proposal metadata.
- [x] Fixed gap 5: README documents the supervised real-provider acceptance run protocol and marks live execution as pending provider account handoff.
- [x] Fixed gap 6: failed/pending provider verification now feeds redacted errors into a bounded inferred UI repair pass and reruns verification.
- [x] Fixed gap 7: provider packs now expose account-creation mode, so automatic account creation cannot be implied without an API-backed recipe and supervised signup remains visible to users and audits.
- [ ] Remaining: full public acceptance run still requires supervised GitHub/Vercel/Cloudflare authorization and a disposable test domain.
- [x] Remaining: acceptance harness can ingest provider verification results directly from retrieved remote OCI artifacts.
