# Magic Path Friction Log

FuseKit's public promise is that the user logs in, approves real provider gates, and
then watches FuseKit do the work through the launcher/control-room surface. Any step
that requires the user to infer provider setup, paste side-channel commands, debug VM
state, or decide which provider page matters is a product bug until it is automated,
guided, or explicitly verified.

## Product Rules

- Provider gates must open inside the live VM browser session that FuseKit observes.
- Verification failures that require a human must create durable control-room gates
  with exact follow-me steps and the right provider URL.
- DNS changes are allowed only after explicit approval, then FuseKit applies the
  approved record shape and verifies propagation.
- Secrets must be captured only into the encrypted vault or provider-native secret
  stores; no launcher/control-room route may expose raw secret text.
- "I finished this step" must trigger a visible state transition: either reverify,
  surface the next guided gate, or show the exact remaining blocker.
- Every real-run intervention should become an audit event, regression test, or
  tracked backlog item before launch readiness is claimed.

## Logged Friction And Fixes

| Friction observed | Product fix |
| --- | --- |
| OCI launched ARM or too-small shapes that broke Python, uv, Node, OpenClaw, and browser runtime binaries. | Runner provisioning now prefers x86_64 Flex runners sized for visual/browser automation and rejects ARM fallback for the OCI lane. |
| SSH reachability failed on older clients due to unsupported SSH options and image-specific login users. | OCI bootstrap now resolves the image user and avoids brittle SSH algorithm options when the client cannot support them. |
| OCI compartment creation caused permission and cleanup churn. | The launcher can use the tenancy/root compartment path and must avoid unnecessary compartment creation for public runs. |
| Provider pages opened in a local browser instead of the VM browser FuseKit observes. | Control-room gate-open actions launch provider URLs in the shared VM browser profile. |
| noVNC/control-room loops made the user re-enter passwords or stare at a blank display. | Visual runtime readiness is surfaced as control-room state, and gates point users to the same VM browser session. |
| Cloudflare DNS approval and application happened outside the guided launcher path. | DNS approval is treated as a first-class approval gate; after approval FuseKit applies and verifies the exact records. |
| Resend account pages gave no exact instruction for API key, domain, audience ID, or from-email setup. | Verification-time Resend failures now create actionable control-room gates with Resend-specific follow-me steps. |
| "I finished this step" sometimes appeared to do nothing after a provider gate was passed. | Provider verification gates are regenerated from verification results, so the next missing capability becomes visible instead of hiding in the report. |
| Control-room button failures could discard the backend's useful blocker and show only a generic stale-click message. | Gate open, Capture, and "I finished this step" actions now preserve redacted backend failure reasons and missing target names in the visible status line. |
| Missing Vercel/GitHub runtime env values were reported as provider checks rather than guided Resend tasks. | Missing `RESEND_*` runtime values now route to either setup-key Capture for `RESEND_API_KEY` or a Resend API setup retry for generated sender/audience values. |
| Copy-once provider secrets required side-channel capture after the user clicked Copy inside the VM. | Secret-bearing gates now show VM-clipboard capture buttons that write the selected value directly into the encrypted vault and return only redacted status. |
| Public Run Records could sanitize vault metadata but still preserve padded, duplicate, blank, or credential-looking vault proof rows that stricter launch checks would reject later. | Run Record generation now canonicalizes vault proof rows before writing them: only complete trimmed id/kind/provider/label metadata survives, duplicate ids collapse, secret-looking rows are skipped, and live acceptance enforces the same exact shape. |
| Run Record generation, live acceptance, and cleanup preflight still depended on separate timeline vocabularies, so recovery step/checkpoint fields could drift between generated proof and launch gates. | A shared timeline-proof contract now owns step fields, checkpoint fields, required text fields, optional recovery text fields, and timestamp proof for recovery timeline validation. |
| Resend key verification returned `403` even when the key had full access. | Resend API verification now sends a `User-Agent`, avoiding false key-rejection gates. |
| Resend had no domain or audience after API auth was available. | Resend setup now runs before Cloudflare/DNS, creates or reuses the sending domain through Resend's API, creates an audience only when the app requires one, stores runtime values in the encrypted vault, and hands Resend DNS records to Cloudflare for approved apply/verify. |
| Launch proof could pass conceptually even if a run accidentally attempted DNS before Resend had emitted its domain records. | Live acceptance now records and checks provider strategy order; when Resend and DNS are both present, Resend must appear before Cloudflare/DNS or the run is not launch-ready. |
| Capture gates still told the user to click "I finished this step" even though the button is hidden for secret capture. | Capture-gate guidance now tells the user to copy inside the VM, click Capture, and let FuseKit auto-resume once every requested value is captured. |
| Generic gate ids could make the control room show generic provider guidance even when the gate record knew the provider. | Static and live control-room rendering now use the gate's `provider` field as the source of truth for guidance, with text inference only as a fallback. |
| The useful "verifying now" message could be replaced immediately by a generic live-refresh message. | Gate-pass refresh now preserves the explicit "Snowman is rechecking the provider now" status until the next real state appears. |
| Pending-safe DNS verification could look like vague waiting even when the remaining action was DNS approval/apply. | Verification cards now translate pending-safe DNS approval states into the exact protected control-room action: click `Approve DNS apply`; FuseKit then applies the setup-plan records and keeps verifying propagation. |
| Static control-room guidance and live-refresh guidance could drift because provider instructions were duplicated in Python and JavaScript. | Live control-room JavaScript now consumes the serialized Python guidance payload, so provider instructions have one source of truth across static and refreshed views. |
| Provider route cards exposed raw strategy names without explaining whether FuseKit was using deterministic automation or VM follow-me. | Provider route rows now translate selected strategies into plain-language summaries such as API automation, vault capture, or VM follow-me. |
| First-time provider authorization gates could tell the user a token was needed without rendering the safe Capture button, or capture a token under an ID the next setup loop did not read. | Authorization gates now carry the token env target and exact follow-me steps, and VM clipboard capture writes both the env-specific vault record and the canonical provider token alias used by deterministic setup. |
| Live launch proof could ignore durable control-room gate state, leaving a run marked ready even though the launcher still had a waiting provider gate. | Live acceptance now requires `gates.json` and fails unless every durable gate is resolved before launch readiness is claimed. |
| The VM browser launch inherited the runner process environment, which could expose provider/API tokens or vault passphrases to a child browser process. | Control-room gate-open now strips token, key, password, auth, credential, cookie, and session environment variables before launching the shared VM browser while preserving required visual settings. |
| Gate-open responses could expose the VM browser's full executable path, making public demo/debug payloads leak runner filesystem layout. | Control-room gate-open now returns only the browser executable name while keeping full paths internal to the fixed argv launch path. |
| Acceptance proof could snapshot raw gate URLs from provider callbacks even though those URLs might contain provider-owned codes or token-like query parameters. | Live acceptance now writes a minimal redacted gate-state proof instead of raw browser/session URLs while still proving every gate is resolved. |
| Provider strategy proof could be too shallow to explain whether FuseKit used deterministic API automation, secure vault capture, or VM follow-me, making the control room feel like it skipped the important reasoning. | Live acceptance now requires complete selected-route evidence and considered candidates for every provider strategy decision before a run can be marked launch-ready. |
| VM clipboard capture read helpers inherited the full runner environment, so `xclip`/`xsel` could receive provider tokens or vault passphrases even though browser launch stripped those values. | VM clipboard reads now reuse the sanitized visual environment, preserving only display/session settings like `DISPLAY`/`XAUTHORITY` while stripping token, key, password, auth, credential, cookie, and session variables before reading copy-once provider values. |
| Provider gates could tell the user what provider page to use but not the exact next action or what FuseKit would do after the click/capture, making the flow feel stuck even when the worker was alive. | Durable gate records now carry `next_action` and `resume_hint`, static/live control rooms render them, and live acceptance fails if any gate is missing guided next-step proof. |
| The noVNC password could be duplicated into frontend dataset state just to avoid iframe refreshes. | Live control-room refresh now compares the existing iframe URL instead of copying the password into extra DOM state; the password remains only in the autoconnect URL/copy affordance where needed. |
| Control-room gate open/resume/capture actions could be recorded in gate state without launch acceptance proving they were also in the audit ledger. | Live acceptance now requires every durable control-room gate to have a matching redacted `control_room.*` audit event before a run can be launch-ready. |
| Acceptance blockers were only visible in `.fusekit/acceptance/report.json`, so the launcher could still feel stuck when the order-of-operations proof failed. | Static and live control rooms now render `blockers[]` as launch-blocker cards with plain next actions, including Resend-before-DNS ordering failures. |
| Provider setup was sorted Resend-before-DNS but could still continue to downstream providers if Resend paused on an API-key gate, risking an incomplete DNS plan. | Provider setup now pauses at the first unresolved authorization gate, records that downstream providers are waiting, and resumes later with complete upstream provider data. |
| Provider verification could still run downstream DNS/deploy checks while an upstream provider gate was waiting, creating confusing failures before Resend had produced its domain records. | Provider verification now uses the same provider dependency order as setup and parks downstream providers as pending-safe behind the active gate. |
| Capture or "I finished this step" could feel inert because the worker was still sleeping for the full gate retry interval after the control room requested resume. | Gate waits now poll durable gate state and wake as soon as the control room marks a gate passed or resume-requested. |
| DNS apply approval could be visible in the control room but still depend on terminal input, so a launcher approval woke the worker without actually approving DNS. | DNS approval gates now accept the protected control-room approval state, use an explicit "Approve DNS apply" button, and continue directly into provider DNS apply/verify. |
| After approving setup or DNS, the control room could still say FuseKit was retrying generic provider verification, making the user wonder whether the approval actually applied the right thing. | Resume-requested gates now carry setup/DNS-specific next-action and status copy across static HTML, live refresh, and POST responses. |
| Resend DNS records can return provider-managed TTL values such as `Auto`, which could break parsing before Cloudflare received the record plan. | Resend DNS record parsing now treats provider-managed TTL values as the safe default TTL while preserving record name, type, value, and priority. |
| Resend runtime repair could send the user to Audiences even when the only missing value was the verified sender address, and mixed provider messages could leak non-Resend env names into the Resend capture gate. | Resend runtime gates now target only `RESEND_*` values and route the user to Domains, Audiences, or API Keys based on the exact missing Resend value list. |
| Resend verification could still surface a missing sending domain as a manual domain task even though FuseKit is supposed to create the domain after key capture. | Missing Resend domains now carry an API-setup repair hint and surface a control-room gate that tells the user not to create the domain by hand; FuseKit retries Resend setup before Cloudflare DNS. |
| Resend setup could generate `RESEND_FROM_EMAIL` or `RESEND_AUDIENCE_ID`, but later deployment/runtime checks could still ask the user to capture those values again. | Runtime env collection now reads provider-generated vault settings and manifest runtime env values consistently, so generated Resend values flow into downstream provider setup. |
| Cloudflare token creation exposed many permission, resource, IP filtering, and TTL choices with too little guidance for a no-thinking demo. | Cloudflare gates now spell out the exact Custom token wizard choices: two permission rows, one specific zone, blank IP filtering/TTL unless required, then Continue to summary/Create Token. |
| Cloudflare exposes both User API Tokens and older API Keys/Global API Key surfaces, which can send a new user down the wrong copy path for scoped DNS automation. | Cloudflare handoff and gate guidance now explicitly say to use My Profile > API Tokens > User API Tokens and not API Keys or Global API Key before creating the scoped Custom token. |
| Vercel token creation could leave users guessing whether they were operating under Personal Account or a team, and which token scope should be selected. | Vercel handoff and gate guidance now directs users to the top-left account/team switcher, says to use Personal Account unless FuseKit named a team, and scopes the token to Personal Account or the exact named team. |
| Inferred and generic provider gates could tell the user to click a generic "resume button" even though the control room button says "I finished this step", and token gates still mentioned CLI fallback capture prompts. | Inferred and shared follow-me gates now use the visible control-room button label and direct token/key gates to the safe VM clipboard Capture button. |
| Browser/provider playbook events could still describe CLI fallback prompts as an equal capture path, even though public launcher users should stay in the VM browser and control room. | Guided browser playbooks now tell users to copy approved provider secrets inside the VM browser and use the target-specific `Capture <ENV> from VM clipboard` button; non-echoing prompts are documented only as CLI fallback. |
| Provider strategy gates generated useful next-action copy but the durable gate recorder dropped it, forcing the launcher back to generic instructions. | Provider strategy gates now persist their specific next action and resume hint into `gates.json`, so the control room can show the intended follow-me instruction. |
| Launch readiness could treat a secret-bearing provider gate as audited just because the gate was opened or resumed, even when the launcher had not proven the token was captured. | Live acceptance now requires a matching redacted `control_room.clipboard_capture` audit event for every secret/env target on a durable gate before the run can be launch-ready. |
| Resend domain setup retry could say "click I finished this step" while still targeting `RESEND_API_KEY`, causing the control room to render Capture controls instead of the finished/retry button. | Resend domain setup retry gates now have no secret target once the setup key is already valid, so the launcher shows the normal finished/retry action and FuseKit reruns Resend API setup before Cloudflare DNS. |
| Private GitHub App source handoff could still tell users FuseKit captures app-issued tokens through CLI fallback prompt or environment variable paths. | GitHub App source handoff now uses the same public launcher pattern as provider gates: copy the approved token inside the VM browser, then click the `Capture from VM clipboard` button so it lands only in the encrypted vault. |
| A stale Capture click/request after a secret gate had already auto-resumed could overwrite the vault record before verification finished. | Clipboard capture now fails closed once the gate is `resume_requested`, preserving the first captured value while FuseKit verifies the provider state. |
| Launch readiness could prove a provider gate was resumed or captured without proving the provider page was opened through the launcher/VM browser. | Live acceptance now requires a matching redacted `control_room.gate_open` audit event for every durable gate with a provider resume URL. |
| Acceptance reports could contain `missing[]` launch-readiness proof but no structured `blockers[]`, leaving the control room on a vague waiting card instead of telling the user what to fix. | Static and live control rooms now fall back to rendering `missing[]` as launch-evidence blockers with a clear repair-and-rerun action. |
| The local OCI launcher summary still described provider secret capture as hidden Cloud Shell prompts, making the fallback sound like the normal user path. | Launcher and CLI help now describe the public path as VM clipboard Capture into the encrypted vault, with terminal capture reserved as fallback. |
| Legacy worker status could still render CLI fallback capture instructions inside the control room, even though the public launcher path uses VM clipboard Capture. | Static and live control-room rendering now rewrites stale fallback wording into VM-browser Capture instructions before displaying or bootstrapping the page. |
| Provider guidance used generic button names like "Open provider gate button" and "Capture in FuseKit" while the control room rendered "Open provider gate in VM" and target-specific Capture buttons. | Provider guidance now names the exact visible controls: Open provider gate in VM and target-specific controls such as `Capture RESEND_API_KEY from VM clipboard`. |
| Private GitHub app-source authorization could pause before the full launch control room existed, leaving the user with terminal-only waiting during the first provider gate. | Source-fetch authorization gates now write a minimal guided control-room artifact beside `gates.json`, showing the GitHub gate, VM-browser open action, and Capture button before full launch starts. |
| The prelaunch source-fetch control room could be written but not named in the terminal waiting message, so a user still might not know where to click or how to activate live controls. | Source-fetch waiting output now prints the exact guided control-room path plus the `fusekit control-room --serve` command for live VM-browser open and Capture controls. |
| The prelaunch source-fetch live control room could show Capture controls without carrying enough vault state for the server to save the copied token. | Source-fetch job state now includes the vault and passphrase-file artifacts, and live Capture reads the passphrase artifact before falling back. |
| Verification-time Resend gates could include follow-me steps but omit the durable next action/resume hint that tells the user exactly whether to Capture or click I finished this step. | Resend API-key, runtime-value, and domain-verification gates now persist exact next actions and resume hints, including auto-resume for Capture gates and Resend-before-Cloudflare retry copy. |
| Generic provider verification gates could display a target URL/detail that contained provider callback codes or token-like query values. | Control-room gate targets are now redacted in the browser payload and rendering path while preserving useful target shape for guidance. |
| Live acceptance could prove a partial multi-value Capture gate if `captured_targets` listed one value while the original gate target still required more values. | Gate audit proof now requires clipboard-capture events for the union of env targets in `target` and `captured_targets`, so missing Resend runtime values cannot look launch-ready. |
| Public acceptance report blockers could copy failed check details directly, risking token-like provider callback values in the launch-readiness report and control-room blocker cards. | Acceptance check and blocker details now run through shared public-text redaction before they are serialized or displayed. |
| A new Resend account can have a Full access key but no sending domain, making users wonder whether they must create domains or audiences by hand before FuseKit can continue. | The public launch contract now treats this as FuseKit-owned order of operations: the user only captures the setup key, then FuseKit creates/reuses the Resend domain by API, creates audiences only when required, and feeds Resend DNS records into Cloudflare approval. |
| Tokenized control-room deep links could leave `?token=` in the address bar after opening API or nested routes, which is risky for public recordings and screenshots. | Any authenticated control-room GET with a safe token now sets the strict cookie and redirects back to the same route with only the token query parameter removed. |
| Launch readiness could prove the intended Resend-before-DNS strategy order without proving the worker receipt actually included Resend-generated DNS records in the Cloudflare proposal. | Live acceptance now checks the setup receipt for successful `resend.domain` before `dns.propose` and verifies that the DNS proposal for the same manifest domain contains the exact records returned by Resend. |
| Launch readiness could prove Resend domain setup and Vercel verification separately without proving Vercel received the app-required `RESEND_*` runtime env keys. | Live acceptance now checks the setup receipt for successful `vercel.env` actions covering every manifest-declared `RESEND_*` runtime variable when Resend and Vercel are both present. |
| Acceptance ledger snapshots used structured secret-key redaction but could preserve token-like strings in ordinary fields such as callback URLs, notes, or provider evidence. | Harness snapshot artifacts now also apply public token-shape and path redaction recursively, so callback codes, long provider tokens, and local paths do not land in launch proof files. |
| Public acceptance report serialization could still return manually supplied blocker fields verbatim even though normal blocker construction redacted details. | `AcceptanceReport.to_dict()` now redacts every blocker field at the public serialization boundary. |
| Launch-readiness proof and ledger artifacts could expose local absolute app and artifact paths, making a public demo report look like an internal debug dump. | Acceptance reports and harness ledger artifact entries now serialize public paths such as `.fusekit/acceptance/report.json` while keeping local in-memory paths usable for verification. |
| Resend and Cloudflare had separate unit coverage, but the manifest setup loop did not directly prove that Resend-generated DNS records reached Cloudflare in the same run. | A manifest-level regression now simulates Resend domain creation and asserts Cloudflare sees the generated records after the enforced Resend-before-DNS order. |
| After clicking `I finished this step`, generic retry copy could make the control room feel inert even when the worker had accepted the resume request. | Durable gate resume messages now distinguish provider authorization, provider setup retry, provider domain, and provider runtime-value checks, and tell the user to keep the same VM/control-room session open while FuseKit either continues or resurfaces the gate with updated follow-me instructions. |
| Clicking `I finished this step` on a secret-capture gate could be correctly rejected but still feel like nothing happened because the visible error only named the missing env var. | Blocked resume clicks now return and render the exact next launcher action, such as `Click Capture RESEND_API_KEY from VM clipboard`, while preserving the rule that secrets can only enter through encrypted VM clipboard capture. |
| A provider gate launch could fall back to `xdg-open`, which cannot enforce FuseKit's shared VM Chrome profile and can open a disconnected/default browser session. | Live control-room provider gate launches now require Chrome/Chromium or a configured `FUSEKIT_VISUAL_BROWSER`, preserving the shared VM browser profile used for provider login, token creation, and capture. |
| Re-clicking `Open provider gate in VM` on the same active gate could spawn another provider tab after the short debounce window, making login/auth tabs feel disconnected or duplicated. | Active provider gates now reuse the already-open shared VM browser URL for the life of that gate attempt, and the control room tells the user to use the live VM browser instead of opening another tab. |
| Resend's empty Domains page says "No domains yet" and shows an Add domain button, which can make users think they should manually create the domain before FuseKit has the setup key. | Resend API-key gates now explicitly say to stay on API Keys and not click Add domain; FuseKit creates or reuses the Resend domain through the API after Capture succeeds. |
| An existing Resend API key can show Full access for all domains, but that metadata does not satisfy FuseKit if the raw copy-once secret value is unavailable. | Resend guidance now tells users that existing key permission is not enough unless they can copy the raw value; if not, create a new setup key and capture it through the VM clipboard into the encrypted vault. |
| A generic DNS/provider gate could ask the user to capture `CLOUDFLARE_API_TOKEN` but save the clipboard value under `provider.dns.*`, leaving deterministic Cloudflare setup unable to find the token. | VM clipboard capture now canonicalizes known provider token env names before writing vault aliases, so copy-once tokens land under the records the provider setup code reads. |
| After clicking `I finished this step`, the control room could switch the active gate to a running recheck state and hide the gate-help panel, making the accepted click feel like the UI went blank. | Resume-requested gates now render a visible rechecking panel with the provider, next action, and session hint while FuseKit verifies or resurfaces the next guided blocker. |
| The Resend computer-use playbook still tried to open Domains/Add Domain and wait for DNS records, contradicting the API-first Resend-before-DNS order. | Resend UI automation now stops at setup-key creation and Capture; FuseKit creates/reuses the Resend domain through the API, then hands returned DNS records to Cloudflare. |
| Resend runtime repair could make generated values feel like user-owned setup by telling the user to open Domains or create/select Audiences. | Resend runtime gates now explain that From email and audience IDs are normally generated by FuseKit's API path, and provider UI recovery is only for copying an existing verified sender or audience ID. |
| Missing-token provider strategies could carry a project/domain handoff URL, sending the VM browser to Vercel New Project or Resend Domains instead of the token/API-key page. | Browser-guided setup strategies now prefer provider token URLs and serialize that URL directly into the strategy action so durable gates open on the capture-ready page. |
| Provider strategy summaries could still say "open the provider gate" and "let FuseKit capture" instead of naming the actual launcher controls a public user sees. | Strategy next actions now say to click `Open provider gate in VM`, finish the provider-owned gate in the VM browser, copy any revealed token, and click the target-specific `Capture <ENV> from VM clipboard` button. |
| A malformed durable provider gate URL was rejected before browser launch, but the live `/open` route could close the connection instead of returning a controlled launcher error. | Provider-gate URL validation now happens inside the route error boundary, returning a JSON `400` and preserving gate state without launching the VM browser. |
| Generic provider setup and verification fallback gates could still say to open or complete a provider gate without explaining whether to use `Capture from VM clipboard` or `I finished this step`. | Fallback gates now name `Open provider gate in VM`, explain the VM browser flow, split copy-once token capture from non-secret completion, and persist those instructions in durable gate state. |
| Vercel's GitHub Login Connection repair gate could say to "resume FuseKit" or "mark the gate finished", which did not match the launcher button label. | The Vercel repair gate now tells users to click `Open provider gate in VM`, approve only the named account/repo access, then click `I finished this step`. |
| Older or partial acceptance reports with `missing[]` but no `blockers[]` could render a generic "Repair this acceptance item" card in the control room. | Static and live control-room blocker fallbacks now map known missing launch-proof items to the same plain-language categories and next actions as the harness. |
| Acceptance blockers could know the exact missing proof, such as a skipped VM-browser open or clipboard capture, but hide that detail from the visible control-room card. | Static and live control-room blocker cards now show the redacted acceptance detail so the user can see the exact gate/open/capture proof FuseKit is waiting on. |
| A rare Resend domain-verification recovery gate could still tell users to add or open a domain, even though the normal magic path creates/reuses domains through the Resend API first. | Resend domain-verification recovery copy now says to review only the existing domain/provider-owned prompt and not create the domain or DNS records by hand. |
| The control room trusted `visual.json` for the noVNC iframe URL even though the frame gets clipboard permissions for provider-gate work. | Visual session state is now sanitized before rendering: only credential-free noVNC `/vnc.html` URLs and same-host control-room links survive, and unsafe passwords/query values are dropped. |
| Custom-provider fallback gates still asked users to decide whether a revealed copy-once token meant Capture or "I finished this step." | Generic strategy gates now tie the action to the visible FuseKit controls: use Capture only when named Capture buttons are rendered, otherwise click `I finished this step` after provider confirmation. |
| A green rehearsal acceptance report could make the control room say the live run was launch-ready and safe to record. | Acceptance cards now distinguish rehearsal proof from public/demo proof: only a live report with `launch_ready: true`, `public_launch_ready: true`, and `recording_ready: true` tells the user to record the demo. |
| Older live acceptance reports with only `launch_ready: true` could still make a refreshed control room infer public/demo readiness even though the report never explicitly proved `public_launch_ready` or `recording_ready`. | Static payloads, server-rendered cards, and live refresh JavaScript now fail closed: a demo is recordable only when the live report explicitly sets `public_launch_ready: true` and `recording_ready: true`. |
| A stale or hand-edited live acceptance report could set top-level `recording_ready=true` while omitting `recording_proof_ready` or disagreeing with the recording-contract summary, making the control room look recordable before strict acceptance/preflight rejected it. | Static payloads, server-rendered cards, and live refresh JavaScript now require `recording_ready`, `recording_proof_ready`, live public launch readiness, and `recording_contract.recording_ready` to agree before showing the public demo as recordable. |
| Acceptance report serialization could mark `recording_proof_ready=true` from a passing Run Record check even if the embedded recording contract was absent or false. | Public reports now emit `recording_proof_ready` and `recording_ready` only when the proof flag and `recording_contract.recording_ready` agree, keeping the report artifact itself fail-closed. |
| A hollow or partial recording contract with only `recording_ready=true` could still satisfy the public report or control-room formula even though it exposed no full section-check proof. | Report serialization and control-room report loading now require the recording contract to have the expected schema, the full recording-check set, every check literal true, and no blockers before recording proof can become public-ready. |
| Once the recording contract carried the full checklist, the control room's six-card preview could show arbitrary alphabetic sections instead of the public-demo proof story. | Static and live control rooms now prioritize worker replacement, rehearsal review, model inference, provider playbook, verifiers, and detonation in recording-contract cards. |
| The acceptance CLI could still print `Recording proof ready: true` from the raw proof flag while JSON reports and the control room treated the same run as unrecordable because the recording contract was missing or false. | Terminal output now prints the effective contract-backed recording-proof flag, so CLI, JSON report, control room, and `--require-recording` all agree. |
| Full-suite control-room clipboard-capture tests could time out while encrypted vault writes eventually succeeded, producing browser-disconnect tracebacks instead of a clean response boundary. | Control-room JSON writes now tolerate disconnected clients without traceback noise, and vault-backed capture tests use a realistic HTTP timeout for encrypted writes under load. |
| The acceptance report generator serialized `recording_ready` by reusing `public_launch_ready` directly, hiding the demo-recording proof contract behind an implementation detail. | Acceptance reports now expose `recording_ready` as a first-class property, serialize from that property, and record it in the acceptance ledger so future recording-specific checks have an explicit proof surface. |
| Live acceptance could prove that some provider strategy existed without proving strategy coverage for every provider declared by the manifest. | Live acceptance now fails with a specific Provider routes blocker when manifest providers are missing from `provider_strategies.json`. |
| Live acceptance could prove that present verification checks were safe without proving every manifest provider had a verification check. | Live acceptance now fails with a specific Verification blocker when manifest providers are missing from `verification_report.json`. |
| Live acceptance could prove rollback metadata existed without proving rollback coverage for every manifest provider. | Live acceptance now fails with a specific Rollback blocker when manifest providers are missing from `rollback_plan.json`. |
| Skipped rollback rows could satisfy provider rollback coverage even though `skipped` means FuseKit could not identify the provider resource to revoke or delete. | Live acceptance and detonation preflight now count only `planned` or `done` rollback rows as provider rollback proof, so skipped rollback placeholders cannot support launch readiness or worker cleanup. |
| Rollback metadata could carry padded action/status text or sidecar rollback notes while still counting as provider rollback proof. | Live acceptance and detonation preflight now reject loose rollback metadata before provider rollback proof can support launch readiness or worker cleanup: rollback rows must use generated fields only and trimmed public text. |
| Live acceptance and cleanup preflight still carried separate rollback vocabularies, so provider rollback coverage fields or accepted statuses could drift across launch gates. | A shared rollback-proof contract now owns the rollback metadata envelope, action row fields, public action text fields, and accepted proof statuses. |
| Setup receipts could carry padded action/status text or sidecar receipt notes while still proving zero raw-secret exposure. | Live acceptance and detonation preflight now reject loose setup-receipt envelopes before receipt proof can support launch readiness or worker cleanup: receipt rows must use generated fields only and trimmed public action/status text. |
| Run Record audit generation, live acceptance, and cleanup preflight still carried separate setup-receipt vocabularies, so zero-secret receipt fields could drift across launch gates. | A shared setup-receipt contract now owns the receipt envelope, raw-secret counter field, public text fields, action row fields, and required action text fields. |
| Workspace detonation receipts could carry padded cleanup status/resource text or sidecar cleanup notes while still proving the OCI workspace was destroyed. | Live acceptance and Run Record validation now reject loose workspace-detonation receipts before no-trace cleanup proof can support launch readiness: receipt, resource-summary, and remote-worker cleanup rows must use generated fields only and trimmed public text. |
| Run Record generation, live acceptance, and cleanup preflight still depended on separate detonation vocabularies, so no-trace cleanup receipt fields could drift across launch gates. | A shared detonation-proof contract now owns the detonation envelope, workspace receipt fields, resource-summary fields, remote-worker cleanup fields, and cleanup text/list/boolean groups. |
| Runner-readiness survivors could carry padded VM/browser profile text or sidecar readiness notes while still proving the disposable browser worker was prepared. | Shared runner-readiness validation now rejects loose `runner_readiness.json` proof in live acceptance and detonation preflight: readiness, profile-contract, browser-stack, observed-fact, health-check, and installed-binary rows must use generated fields only and trimmed public text. |
| The rollback planner could emit DNS rollback as a generic `rollback.dns.*` action and omit Resend setup actions, causing fresh runs to fail the provider-coverage proof even after successful setup. | Rollback plans now emit provider-shaped actions for Cloudflare DNS and Resend setup, with legacy DNS rollback names still accepted by the launch-readiness harness. |
| Shared capture copy could still say "Capture in FuseKit" or "Capture here" while the visible button says `Capture <ENV> from VM clipboard`. | Static, live, Resend-specific, and generic gate fallback copy now tells users to click the exact target-specific `Capture <ENV> from VM clipboard` button. |
| Live acceptance could treat a gate as guided with only a next-action sentence, and its redacted gate proof still serialized raw `target` text. | Live acceptance now requires non-empty follow-me steps for each durable gate and redacts gate targets in public acceptance artifacts. |
| DNS apply approval could still depend on an upfront `--approve-dns` flag or pending-safe skip, so a control-room run might reach Cloudflare without a clear post-Resend approval moment showing the exact app and Resend-generated records. | Control-room launches now pause after Resend emits DNS records and before Cloudflare runs, render the app and provider-generated DNS record summaries in the approval gate, then continue Cloudflare apply/verify after the launcher approval is recorded. |
| GitHub fine-grained token setup still required the user to infer the correct owner, repository selector, and permission rows. | GitHub provider gates and generated handoffs now tell the user to set Resource owner to the named user/org, select only the named repo, grant only Secrets and Administration read/write, leave unrelated permissions at No access, and approve only named org/SSO gates. |
| Provider strategy artifacts could record only a generic token-gate next action, relying on later gate rendering to recover the provider-specific follow-me steps. | Blocked provider strategy actions now carry pack-derived follow steps and resume hints immediately, so Resend/GitHub/Vercel/Cloudflare artifacts and durable gates share the same no-thinking guidance. |
| The provider-routes panel could show that FuseKit chose a VM follow-me route without showing the exact next action or provider-specific steps. | Provider strategy artifacts now preserve follow steps, next action, resume hint, and resume URL for human-gate routes; static/live control rooms render them, and live acceptance fails if human-gate strategy guidance is missing. |
| Verification report repair copy could tell users to rerun verification or repair through provider UI/API, which is engineer-facing and not launcher-actionable. | Verification reports now point human/provider repairs back to visible launcher controls: Open provider gate in VM, target-specific Capture buttons for copy-once values, or I finished this step for non-secret gates. |
| The provider-gate open route launched a configured VM browser via argv form, but did not fail closed if the configured binary was not actually executable Chrome/Chromium-family. | The live control room now accepts only executable Chrome/Chromium-family visual browser binaries, sanitizes the visual `DISPLAY`, and keeps provider-gate opens on a fixed argv command path. |
| Live acceptance could prove only `.fusekit/worker` and `.fusekit/tmp` were gone, leaving a possible false green if browser profiles, visual scratch, provider auth state, uploaded app archives, passphrase files, or control-room/gateway logs survived. | Launch readiness now treats those plaintext runner surfaces as detonation blockers while preserving only encrypted or redacted proof artifacts such as vault, audit, receipt, gates, verification, rollback, provider strategies, and sanitized `visual.json`. |
| Custom or newly generated provider gates could fall back to generic copy that mentioned only `I finished this step`, even when a copy-once API key/token Capture button was the safe next action. | Generic provider guidance now distinguishes the two visible launcher paths: use `Capture from VM clipboard` when FuseKit renders a Capture button for a copy-once value, otherwise click `I finished this step` after provider confirmation. |
| Live acceptance could treat any non-empty follow step as guided, so generated gates using hidden-prompt, side-channel, manual, or figure-it-out wording could still look demo-ready. | Launch readiness now rejects non-launcher human-gate guidance, requires VM-browser wording for provider URLs, and requires `Capture from VM clipboard` wording for every env-style copy-once target in both durable gates and human-gate provider strategies. |
| Provider-owned gates could have good follow-me copy but no openable provider URL, leaving `Open provider gate in VM` impossible and making gate-open audit proof optional. | Live acceptance now requires provider-class durable gates to include a `resume_url`, then requires matching redacted `control_room.gate_open` audit proof before a run can be launch-ready. |
| Non-secret provider checks and setup/DNS approval gates could be considered audited after any control-room event, without proving the user clicked the visible `I finished this step`, `Approve setup plan`, or `Approve DNS apply` control. | Live acceptance now requires a redacted `control_room.gate_resume_requested` audit event for non-secret provider gates and setup/DNS approval gates; secret gates continue to require Capture proof instead. |
| Older or partial acceptance reports could list missing Resend DNS, RESEND_* env setup, detonation, leak-scan, vault, receipt, verification, or rollback proof but fall back to generic repair copy in the control room. | Static and live control-room fallback blockers now mirror the harness guidance for those items, including the Resend-domain-first order, DNS apply approval, Vercel env capture, and plaintext state detonation steps. |
| Failed acceptance checks for missing provider URLs, missing gate-open/capture/resume audit proof, Resend DNS receipt proof, or detonation proof could still say to repair an artifact instead of naming the visible launcher action. | Acceptance-generated `blockers[]` now gives launcher-actionable recovery text: `Open provider gate in VM`, `Capture from VM clipboard`, `I finished this step`, DNS apply approval, Resend domain creation, and detonation. |
| Provider route cards could still describe Resend domain and audience setup as generic API automation, leaving new-account users unsure whether "No domains yet" means they must click Add Domain. | Resend API strategy evidence now records that FuseKit owns domain/audience creation after key capture, and the control room explains that FuseKit creates/reuses the domain, collects DNS records, waits for DNS approval, and creates audiences only when the app requires one. |
| A stale strategy artifact could still claim Resend used an API route without proving that the API route owns domain/audience setup and keeps DNS behind Resend. | Launch acceptance now requires Resend API strategy evidence for `api_owns`, no manual domain/audience step, domain-before-DNS order, and audience-only-when-required behavior. |
| A user could accidentally copy a provider page URL, multi-line page text, or malformed Resend runtime value before clicking Capture, causing FuseKit to encrypt the wrong clipboard value and continue. | Control-room clipboard capture now validates token/key targets, `RESEND_FROM_EMAIL`, and `RESEND_AUDIENCE_ID` before writing the vault; obvious wrong values are rejected without moving the gate or writing audit proof. |
| Downstream Vercel verification could report missing generated `RESEND_FROM_EMAIL` or `RESEND_AUDIENCE_ID` and route the user back to Resend to copy values that FuseKit should own. | Provider API fallback now regenerates API-owned Resend runtime values first, then retries the downstream provider setup with those generated values before surfacing a human gate. |
| A mixed downstream failure could ask the user to capture `RESEND_API_KEY` plus generated Resend values in the same gate. | Resend runtime recovery now captures only the copy-once setup key when it is missing; sender and audience values are generated through Resend's API after the key is stored. |
| A stale launcher artifact could still prove launch readiness after asking the user to capture generated Resend sender or audience values. | Live acceptance now rejects Resend runtime capture gates that target API-generated values, forcing the setup-key or API-retry path instead. |
| The launch blocker for stale generated Resend capture gates could still fall back to generic guided-gate repair text. | Acceptance blockers now tell the user to keep the live launcher/control room open while FuseKit rebuilds the Resend runtime gate so only `RESEND_API_KEY` uses Capture and generated values use Resend API setup retry. |
| A stale live control-room gate could still let the user click Capture for generated Resend sender or audience values before acceptance failed later. | The control-room capture endpoint now refuses `RESEND_FROM_EMAIL` and `RESEND_AUDIENCE_ID` on Resend runtime capture gates and tells the user to capture only `RESEND_API_KEY` before API retry. |
| Generic provider, verification, acceptance, and control-room fallback copy could still use `Capture RESEND_API_KEY from VM clipboard` as the example next action even when the active gate was another provider or did not yet know the env target. | Generic recovery copy now stays provider-neutral: it points to the single highlighted launcher gate and requires the exact env-named Capture button rendered for that gate, while Resend-specific copy names `RESEND_API_KEY` only on real Resend key-capture paths. |
| Provider API setup could start mutating projects, DNS, domains, or repo settings before proving the token-backed provider API contract was still healthy. | Provider-pack setup now performs a read-only contract-health preflight before the first API mutation, records redacted audit/receipt evidence, and stops before mutation if the provider contract check fails. |
| A provider token could exist but be expired, revoked, or scoped wrong, causing the new API contract preflight to fail as a hard setup error instead of a guided launcher repair. | Failed provider contract-health now becomes a `needs_human_gate` token-refresh path with the provider token page, exact VM-browser follow steps, and a real `Capture from VM clipboard` target such as `RESEND_API_KEY`, `VERCEL_TOKEN`, or `GITHUB_TOKEN`. |
| Live acceptance could pass a receipt that claimed API setup succeeded without proving the provider API health preflight happened first. | Live acceptance now fails `receipt.provider_contract_health` unless every successful API-backed `provider_pack.setup` action has a preceding `<provider>.contract_health` success, with blocker guidance to refresh/capture provider tokens before retry. |
| The control room could list individual provider route cards but still not give the user a plain ordered plan, so a new-account Resend page with "No domains yet" looked like something the user should fix manually. | The provider-routes panel now renders a Route plan derived from strategy evidence: token gates through VM-browser Capture, Resend domain first by API, Vercel `RESEND_*` env after generated values exist, and DNS approval after Vercel and provider-generated records are ready. |
| Provider strategy decisions were visible in the provider-routes panel but not persisted into the durable recovery map, so a paused or resumed run could fall back to generic setup-worker guidance. | Provider routes now upsert recovery checkpoints into `checkpoints.json`, preserving token capture, Resend domain-first, Vercel env wiring, DNS approval, and provider token-capture next actions across refreshes and resumes. |
| Provider gates could give many setup instructions but still leave first-time users unsure what final state counts as done and which nearby provider UI paths are traps. | Provider guidance now renders compact `Success looks like` and `Avoid` panels from the shared guidance payload in both static and live control rooms, including Resend-specific warnings that an existing Full access card is not enough without the raw key and that users should not click Add domain. |
| A remote VM could pass basic package bootstrap but still be the wrong architecture or miss noVNC/visual helper binaries, letting provider gates start on a brittle runner that cannot actually show or control the browser. | `fusekit-runner-verify` now fails before provider setup unless the VM is x86_64, runner helper scripts are executable, visual/noVNC commands are present, Playwright Chromium can launch, and the shared Chrome provider profile path exists. |
| A resumed or retrieved run could render provider success/avoid hints from local code, but the durable `gates.json` artifact did not prove those no-thinking instructions survived with the gate. | Provider gate records now persist `success_criteria` and `avoid_steps`, static/live control rooms prefer those durable fields, and live acceptance rejects provider gates that lack them. |
| Approval or capture gates without a provider URL could still be considered guided even when they omitted success criteria or avoid steps, leaving users to infer when it was safe to approve or what actions were out of bounds. | Gate acceptance now requires `success_criteria` and `avoid_steps` on every durable gate, not just URL-opening provider gates, so DNS/setup approvals and capture-only gates carry explicit done-state and safety guidance. |
| A saved or retrieved provider gate could still omit durable `follow_steps`, relying on renderer fallback code even though launch readiness requires the artifact itself to prove no-thinking guidance. | Gate serialization now writes provider default follow-me steps into `gates.json`, so resumed runs and OCI artifacts carry `Open provider gate in VM` and `Capture from VM clipboard` instructions without relying on local fallback rendering. |
| The control-room CSRF model documented "no CORS" behavior but did not prove rejected preflights and attacker-origin POSTs omit every CORS allow header. | Security tests now assert control-room preflights and rejected cross-site POSTs emit no `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods`, or `Access-Control-Allow-Headers` headers, preserving the no-CORS state-changing surface contract. |
| Control-room origin checks compared normalized strings, which was too easy to reason about incorrectly for spoofed loopback-looking origins such as `127.0.0.1.attacker.example` or userinfo URLs. | State-changing control-room requests now parse browser `Origin` values as URLs, reject userinfo/path/query/fragment tricks, require exact same host/port, and keep remote rooms tied to the authenticated host. |
| A user could copy a real-looking token from the wrong provider tab and still pass generic no-space token validation, putting the wrong secret into the encrypted vault before deterministic setup failed later. | Control-room clipboard capture now enforces known provider token prefixes for Resend, GitHub, and OpenAI, and rejects cross-provider token prefixes before writing any vault record. |
| A stale provider gate or strategy artifact could mention the VM browser but still tell the user to use a local browser tab for part of the same gate. | Launch acceptance now rejects local-browser side-channel wording in durable gates and human-gate strategy artifacts while still allowing safety warnings such as "Do not use a local browser." |
| Launch acceptance treated every `manually` occurrence as bad guidance, which could reject correct guardrail copy like "Do not manually create the Resend domain" while still missing phrasing such as "manual Resend setup." | Manual-action guidance now fails only on affirmative manual work instructions and explicitly allows negated/no-manual warnings that keep users away from provider setup traps. |
| Stale `Success looks like` or `Avoid` panels could still tell users to use a local browser, manually configure provider state, or click Resend Add Domain even when the main follow-me steps were clean. | Launch acceptance now scans success and avoid guidance for local-browser side channels, affirmative manual work, and manual Resend domain/audience setup without allowing those panels to satisfy the required VM-browser/Capture action proof. |
| Recovery checkpoint cards can become the user's only visible next-step map after refresh, but acceptance only checked that checkpoint text existed, not whether it sent users to local tabs, manual provider setup, or Resend Add Domain traps. | Launch acceptance now scans provider-route checkpoints for non-launcher wording, local-browser side channels, affirmative manual actions, and manual Resend domain/audience setup while allowing safe negated copy such as "Nothing to copy manually." |
| Resend's strategy layer preferred API Keys, but the lower-level handoff catalog still carried a Domains project URL that could reopen the "No domains yet" trap if a flow included project pages. | Resend static and generated handoffs now omit the project URL until after setup-key capture; handoff URL expansion skips empty and duplicate URLs so first-run setup opens only signup/login and API Keys while FuseKit owns domain creation by API. |
| The visual-session button said "Open browser surface," which could sound like another local browser tab instead of the observed disposable VM session where provider gates must happen. | Static and live control-room visual controls now say `Open live VM browser` and `Copy live VM browser link`, keeping all provider-gate interaction language tied to the VM session. |
| Remaining retry, setup, and status copy still used "browser surface" language in a few places, which weakened the otherwise exact VM-browser guidance. | Runtime messages now say `live VM browser`, `Open live VM browser`, or `VM browser` instead of asking users to reason about a browser surface. |
| Unknown control-room routes returned bare 404s, leaving a small inconsistent HTTP surface compared with protected launcher pages and API responses. | Unknown GET/POST routes now return zero-length 404s with the same no-store, CSP, frame, nosniff, and no-CORS posture as documented control-room routes. |
| Live control-room copy failures selected nearby values but only said copy was blocked, leaving users unsure whether to press Command+C, Ctrl+C, or retry. | Copy-failure fallback now says when FuseKit selected the visible value and tells the user to press Command+C or Ctrl+C; non-input copy failures still state that the value remains visible. |
| Live acceptance could treat a reused provider-tab event as proof that the run actually launched the provider gate in the VM, even if the only audited click reused stale browser metadata. | Gate-open proof now requires a non-reused `control_room.gate_open` event with a resume URL and recorded VM-browser URL; reused clicks remain UI feedback but cannot satisfy launch-readiness proof by themselves. |
| Resend route-plan and provider guidance said not to click Add Domain "unless FuseKit asks," leaving a vague exception that could make new-account users wonder whether manual domain setup might still be expected. | Resend guidance now states the public path plainly: do not click Add Domain or create domains/audiences by hand; FuseKit creates or reuses Resend resources by API after key capture and then sends records to DNS approval. |
| Route-plan copy for token and human gates could still say to use the VM browser without naming the actual launcher controls, leaving first-time users to infer which button advances the run. | Route-plan token and human-gate steps now name `Open provider gate in VM`, the shared VM browser, `Capture from VM clipboard`, and `I finished this step` directly. |
| Durable provider-route checkpoints could pass acceptance with vague "use the VM browser" wording or copy-once token names without proving the recovery map names the actual launcher controls. | Launch acceptance now rejects waiting provider-route checkpoints unless they name `Open provider gate in VM`, and rejects copy-once token/key checkpoints unless they name `Capture from VM clipboard`. |
| Live control-room retry errors and acceptance blockers could still say "mark the gate done" or "resume button", which did not match the button the user sees. | Gate-pass error copy and unresolved-gate blockers now name the visible `I finished this step` button. |
| Verification-report repair copy could still say "visible FuseKit Capture" instead of the actual clipboard-capture control, leaving report-driven recovery less precise than the control room. | Verification reports now name `Capture from VM clipboard` for copy-once values and `I finished this step` for non-secret provider gates. |
| Source handoffs, browser playbooks, inferred UI repair, and launcher helper copy could still say "Capture button" or "Capture button in FuseKit" instead of the exact visible capture control. | Public guided copy now consistently names `Capture from VM clipboard` buttons for copy-once provider values. |
| Multi-value capture gates could name the first Capture control exactly but fall back to vague "then capture VALUE" copy after one value was stored. | Partial capture states now continue to name the matching `Capture from VM clipboard` button for the remaining value. |
| Failed VM clipboard captures could reject URLs, wrong-provider tokens, short tokens, empty clipboards, or stale Resend generated-value captures without naming the exact launcher recovery action. | Capture failures now tell users to copy the correct value inside the VM browser and click the target-specific `Capture <ENV> from VM clipboard` button again; stale Resend generated-value captures also say not to create domains or audiences by hand. |
| Route-plan and acceptance-blocker copy could say to "capture or generate" `RESEND_*` values, which made first-time users wonder whether they had to create Resend domain, sender, or audience values by hand. | Resend/Vercel route guidance now says the user captures only `RESEND_API_KEY`; FuseKit then creates or reuses Resend domain/audience values by API and writes the required `RESEND_*` runtime variables into Vercel. |
| After clicking Copy inside noVNC/provider UI, users could think they had to paste the token into their Mac, host browser, or terminal before FuseKit could read it. | Provider gate, handoff, strategy, and token-refresh guidance now states that no paste into the user's computer is needed because the `Capture from VM clipboard` button reads the VM clipboard directly. |
| A control-room `Open provider gate in VM` click could launch Chrome with a different profile than the visual/worker handoff path, making provider login state look disconnected after the user already passed a gate. | OCI visual setup, the live control-room daemon, and the launch worker now share one `FUSEKIT_PROVIDER_BROWSER_PROFILE`; the control-room `/open` route honors that profile so provider login, token creation, and capture stay in the same VM browser session. |
| A live visual artifact could prove noVNC was ready without also proving the user-facing browser surface was tied to the shared provider profile, leaving room for a future disconnected-session regression. | Remote visual setup now writes the shared provider profile into `visual.json`; control-room sanitization drops drifted profile metadata; live acceptance rejects visual proof that does not name the FuseKit-owned shared Chrome provider profile. |
| The `Open live VM browser` button used the noVNC password-bearing autoconnect URL, but `Copy live VM browser link` copied the raw noVNC URL and could send users into a password prompt loop. | Static and live control rooms now copy the same autoconnect VM-browser URL they open, so pasted links preserve the noVNC connection details needed for the live demo path. |
| Resend could show an existing API key row with Full access, making users think the gate was satisfied even though Resend hides old raw key values and FuseKit cannot use a row label as a secret. | Static and generated Resend handoffs now state that an existing Full access key row is not enough by itself; FuseKit needs the raw key value captured into the encrypted vault, while empty Resend domains/audiences remain safe because FuseKit creates or reuses them by API after capture. |
| Live URL verification failure copy still told users to inspect provider status or redeploy if needed, which sounds like a host-side debugging chore instead of the launcher-owned path. | Verification reports now keep live URL recovery in the control-room model: FuseKit retries health checks, reapplies provider setup through API routes where possible, and resurfaces guided launcher gates for provider-owned approval. |
| Vercel env and DNS propagation repair copy could still sound like the user should compare records or redeploy manually after verification failed. | Verification reports now make downstream recovery FuseKit-owned: Vercel env is reapplied through Vercel's API with a deployment refresh, DNS is compared/reapplied through the DNS API, and any provider-owned approval resurfaces as a guided launcher gate. |
| Pending and repairing verification report states could still read like operator advice: wait, retry, or let FuseKit reopen pages later. | Verification reports now keep pending live URL and repair-pass states in the live control room: FuseKit keeps retrying health checks, deterministic routes retry through APIs, and provider-owned approvals resurface as guided launcher gates. |
| Unknown or partial acceptance blockers could fall back to "Repair this acceptance item" or "Run acceptance again after fixing this," leaving users with no visible next control. | Harness and control-room fallbacks now remain launcher-actionable: keep the control room open, use visible provider/DNS/setup controls when they appear, and let FuseKit rebuild proof artifacts inside the same live control room. |
| Resend domain setup relied on provider defaults for region/capabilities, so a fresh-account receipt could prove order but not the exact sending-domain contract FuseKit used. | Resend API setup now creates domains with an explicit sending region and sending-only capability payload, records the actual/requested region and capabilities in the receipt, and launch acceptance rejects stale receipts that lack that proof before DNS can count as ready. |
| Remote control-room pages had a client-side fallback that could treat the URL access token as an action token if the embedded action token was missing, muddying the security model even though the server rejected mismatches. | The live client now sends state-changing requests only with the owner-only action token embedded by FuseKit; remote access tokens only authenticate the page and are scrubbed from the URL after cookie setup. |
| Weak tokenized visual/control-room URLs could survive sanitized visual state even though the remote server requires strong URL-safe access tokens. | Visual state now drops tokenized control-room links unless the token has the same strong URL-safe shape, and acceptance rejects weak-token survivors before launch-ready proof. |
| Token capture could accidentally grow brittle prefix assumptions for providers such as Cloudflare or Vercel, causing a valid copied token to fail because the product expected a folklore prefix. | Clipboard-capture validation now has regression coverage proving Cloudflare and Vercel tokens do not require fixed prefixes, while still rejecting placeholders, URLs, whitespace, and known wrong-provider token prefixes. |
| If a provider strategy artifact omitted a headline next action, the fallback gate could still say `Capture <ENV> from VM clipboard`, forcing users to translate a placeholder during a resumed run. | Provider strategy fallback copy now derives exact Capture controls from the provider pack or strategy target, so stale/partial Vercel-style gates still say `Capture VERCEL_TOKEN from VM clipboard` instead of placeholder wording. |
| The prelaunch source-fetch control room rendered a correct Capture button, but its recovery checkpoint still said to "capture the approved source token" without naming the exact button. | Source-fetch checkpoints now carry the env-specific launcher action, for example `Capture GITHUB_TOKEN from VM clipboard`, so refresh/recovery views stay as guided as the live gate card. |
| Vercel env verification recovery could still say to "Capture copy-once provider values" even when the missing values were API-owned Resend runtime values such as `RESEND_FROM_EMAIL`. | Vercel env recovery now says Capture buttons appear only for true copy-once provider-token gates, while FuseKit regenerates API-owned values such as `RESEND_FROM_EMAIL` or `RESEND_AUDIENCE_ID` before reapplying Vercel env. |
| Maintained and inferred provider packs could append exact Capture controls but still include earlier vague secret-copy wording such as "capture the approved secret key" or "capture it into the encrypted vault." | Provider-pack secret steps now keep the same no-thinking pattern throughout: copy inside the VM browser, then use exact env-named controls such as `Capture STRIPE_SECRET_KEY from VM clipboard` or `Capture STRIPE_WEBHOOK_SECRET from VM clipboard`. |
| State-changing control-room routes decoded gate IDs directly from URL path segments, so a malformed or corrupted gate ID containing encoded slashes could still be addressed if it existed in gate state. | Control-room POST routes now accept only simple FuseKit gate IDs made of letters, numbers, dots, underscores, and hyphens before any gate mutation, returning hardened 404s for encoded-slash or malformed route IDs. |
| Provider guidance could still pass launch readiness with vague "use the VM browser" wording, leaving first-time users unsure which control opens the live provider page. | Launch acceptance now requires openable provider gates and human-gate strategy guidance to name the exact `Open provider gate in VM` control before a run can be demo-ready. |
| Built-in, catalog, and inferred provider handoff packs could still start with "open/sign in in the VM browser" copy instead of naming the launcher button that opens that page. | Provider-pack validation now requires account-opening steps to name `Open provider gate in VM`, and maintained provider handoffs use that exact public control before provider-specific setup instructions. |
| A user could copy a masked provider key, placeholder text, or `undefined` into the VM clipboard and generic token validation could store it as if it were real. | VM clipboard Capture now rejects placeholder and masked token values before vault write, names the target, and tells the user to copy the real token value in the VM browser and click the same Capture button again. |
| Vercel env verification could still say to "Capture or derive" missing runtime values, making generated Resend values feel like a user decision. | Vercel verification now describes launcher-owned recovery: Capture only copy-once provider values from the VM clipboard, regenerate API-owned provider values, reapply Vercel env, and then verify deployment. |
| If the live control room could not parse a Capture failure response, the browser fallback said only to copy in the VM and try again. | Capture fallback errors now name the VM browser and exact `Capture <ENV> from VM clipboard` button so the user still has a precise recovery action without backend detail. |
| Synthesized Resend provider packs could still mention a domain-ownership gate during first account setup, contradicting the no-manual-domain public path. | Resend pack handoffs now keep first-run setup on login/API-key capture only, explicitly avoid Resend domain setup screens, and omit domain-ownership service gates because FuseKit owns domain creation and DNS record collection by API after key capture. |
| Launch acceptance rejected explicit Resend Add Domain/Add Audience instructions, but not the softer stale phrase "domain ownership gate," which could still nudge a new user into manual Resend domain setup. | Resend launch-readiness guidance checks now reject domain-ownership gate, step, or verification wording on Resend durable gates unless it is a negated warning, keeping first-run Resend setup on API-key capture and API-owned domain creation. |
| Provider fallback gates could say "Return to FuseKit and click I finished this step," which is less precise than the launcher/control-room button a public user sees during a VM run. | Provider repair copy now tells users to click the visible `I finished this step` button in the control room, and launch acceptance rejects stale "return to FuseKit" wording in durable human-gate guidance. |
| The provider route plan could still say "click I finished this step" without naming the visible control-room button, making the recovery map slightly less concrete than the gate cards. | Static and live route-plan copy now tells users to click the visible `I finished this step` button in the control room after provider-owned login, MFA, consent, or billing gates. |
| Token route-plan copy said `Capture from VM clipboard for GITHUB_TOKEN`, which did not exactly match the visible button label a demo user must click. | Static and live route-plan copy now names target-specific controls such as `Capture GITHUB_TOKEN from VM clipboard`, matching the rendered launcher buttons exactly. |
| Launch acceptance could still pass a target-specific secret gate if guidance only said `Capture from VM clipboard`, leaving users to infer which rendered button matched the provider token. | Human-gate and provider-strategy acceptance now require exact target-specific Capture control labels such as `Capture GITHUB_TOKEN from VM clipboard` for every env/token target. |
| Provider-route recovery checkpoints could still pass with generic `Capture from VM clipboard` wording even when the checkpoint named a concrete token target, weakening the refreshed recovery map after a paused run. | Provider-route checkpoint acceptance now requires exact target-specific Capture control labels such as `Capture GITHUB_TOKEN from VM clipboard`, so resumed route cards match the rendered launcher buttons. |
| Acceptance blocker recovery could still tell users to click a generic Capture control even when the failed proof named the missing token target. | `gates.guided` and `gates.audited` blockers now extract env/token targets from failure details and name exact recovery controls such as `Capture OPENAI_API_KEY from VM clipboard`. |
| Unresolved-gate blocker recovery could still say to use the "matching Capture" control even when the blocker detail named the missing secret target. | `gates.resolved` blockers now extract env/token targets from unresolved gate details and name exact recovery controls such as `Capture RESEND_API_KEY from VM clipboard`. |
| Control-room fallback cards for missing gate-audit proof said "open, capture, or resume," forcing users to translate backend verbs into visible launcher buttons. | Static and live control rooms now spell out the visible recovery controls: `Open provider gate in VM`, `Capture from VM clipboard`, and `I finished this step`. |
| Control-room server errors for multi-secret gates and wrong capture targets still used generic Capture wording, so users could see `Capture RESEND_API_KEY from VM clipboard` buttons but recovery copy that asked them to infer the matching control. | Resume and capture-target errors now list the exact target-specific visible buttons, such as `Capture CUSTOM_API_KEY from VM clipboard` and `Capture CUSTOM_WEBHOOK_SECRET from VM clipboard`, before FuseKit lets the gate continue. |
| Verification-report repair guidance could know a provider was waiting for token capture but still fall back to generic "copy-once values" copy, making retrieved reports less precise than the live gate card. | Verification reports now infer capture targets from the provider/result context and name exact buttons such as `Capture CLOUDFLARE_API_TOKEN from VM clipboard`, `Capture RESEND_API_KEY from VM clipboard`, or every required multi-secret Capture button. |
| Multi-secret verification repair guidance still introduced exact controls as "each target-specific Capture button," forcing users to parse a generic instruction before the real labels. | Multi-secret verification repair now leads with the exact visible controls, such as `Capture CUSTOM_API_KEY from VM clipboard` and `Capture CUSTOM_WEBHOOK_SECRET from VM clipboard`. |
| Multi-secret control-room capture panels still introduced the visible buttons as "each target-specific button below," which made users translate generic wording during copy-once secret capture. | Static and live control-room capture panels now lead with the exact visible controls, such as `Capture CUSTOM_API_KEY from VM clipboard` and `Capture CUSTOM_TOKEN from VM clipboard`. |
| Maintained provider packs, strategy summaries, and token-refresh gates could still say "matching Capture from VM clipboard button" even though the flow knew the exact token env, leaving demo users to map generic prose to target-specific buttons. | Provider packs, static handoffs, generated capture steps, strategy summaries, and contract-health token-refresh gates now name exact controls such as `Capture GITHUB_TOKEN from VM clipboard`, `Capture VERCEL_TOKEN from VM clipboard`, `Capture CLOUDFLARE_API_TOKEN from VM clipboard`, and `Capture RESEND_API_KEY from VM clipboard`; provider-pack validation rejects generic capture copy when `token_env` is known. |
| Built-in live gate guidance for GitHub, Vercel, Cloudflare, and Resend could still say "matching Capture from VM clipboard button" even though those gates render known token targets. | Maintained gate guidance now names the exact visible Capture buttons for each provider: `Capture GITHUB_TOKEN from VM clipboard`, `Capture VERCEL_TOKEN from VM clipboard`, `Capture CLOUDFLARE_API_TOKEN from VM clipboard`, and `Capture RESEND_API_KEY from VM clipboard`. |
| The live control-room secret panel and stale hidden-prompt copy sanitizer could still say "matching Capture" while the rendered controls were target-specific. | Static and live control-room capture panels now instruct users to click the literal target button such as `Capture RESEND_API_KEY from VM clipboard`, list every target-specific Capture button for multi-secret gates, and sanitize old hidden-prompt text to `target-specific Capture from VM clipboard`. |
| Inferred UI takeover could classify a provider page as missing an API key but still tell the user to click a generic target-specific Capture button even for providers with known token env names. | Missing-token UI inference now derives exact Capture controls from explicit env targets or maintained provider defaults, so Resend, GitHub, Vercel, Cloudflare, and OpenAI takeover gates say `Capture <ENV> from VM clipboard` without asking the user to infer the matching button. |
| Generated provider strategy artifacts could lose the pack's token env at the persistence boundary, so a resumed browser-guided route for a custom provider could fall back to generic Capture wording. | Browser-guided and human-follow-me strategy evidence now carries `token_env`/`token_label`, and summaries read that evidence when the full pack is absent, preserving exact controls such as `Capture NEWPAY_API_KEY from VM clipboard` across saved artifacts. |
| Editing the app source in the local launcher rewrote an already `bash -lc` quoted bootstrap command with plain shell quotes, risking broken copy/paste commands and ambiguous command-injection posture. | Launcher-side source updates now write `app_source` through a base64-decoded shell assignment, so the copied command contains only safe base64 text for the dynamic source and still parses under `bash -n` for malicious-looking input. |
| Duplicate Capture clicks on a partially completed multi-secret gate could reread the VM clipboard, overwrite an already captured vault record, and create duplicate wake/audit proof before the remaining value was captured. | Capture is now idempotent per target: already captured values return a safe acknowledgement with the exact remaining `Capture <ENV> from VM clipboard` control, without rereading clipboard, rewriting the vault, appending audit, or minting wake proof. |
| Detonation receipts could treat the OCI network cleanup as complete after any one network resource class was deleted, which was too weak for the "nothing left behind" public promise. | Workspace detonation now requires every standard FuseKit-created network resource class to be deleted before the receipt is complete; missing subnet, route table, internet gateway, network security group, security list, or VCN classes are named in the receipt and block recording readiness. |
| Recording readiness could accept a generic detonation audit event even when the audit trail did not enumerate the worker, VM, boot volume, public IP, and network cleanup classes. | Recording readiness now requires resource-level detonation audit entries from `workspace_detonation.json`, so a demo cannot turn green without auditable no-trace proof for each disposable OCI cleanup class. |
| Run Record human-action proof could accept vague visible-control names for provider open or finished-click actions, leaving room for stale evidence that did not match the actual launcher buttons. | Human-action recording readiness and live acceptance now require exact controls: `Open provider gate in VM`, target-specific `Capture <ENV> from VM clipboard`, or known finish/approval controls such as `I finished this step`, `Approve DNS apply`, and `Approve setup plan`. |
| Launch readiness could accept a hand-written audit line with the right event shape as proof of a provider open, Capture, or `I finished this step` click. | Control-room audit events now include a redacted `protected_action` marker emitted only after the CSRF/action-token-protected POST path, and live acceptance rejects gate audit proof that lacks that marker. |
| Generic provider fallback guidance could still say "target-specific Capture button," making users infer which visible button matched a copy-once value. | Shared gate guidance, verification fallback gates, provider strategy summaries, and Capture-blocked errors now show the exact label pattern `Capture <TARGET> from VM clipboard` or list the exact buttons to click. |
| A concrete provider gate could include both a real Capture label and stale placeholder copy like `Capture <TARGET> from VM clipboard`, leaving first-time users to decide which text to trust. | Launch acceptance now rejects placeholder Capture labels on durable gates and provider strategy artifacts whenever a concrete secret target such as `GITHUB_TOKEN` or `RESEND_API_KEY` is known. |
| The security surface map still described public guided secret capture as generic `Capture from VM clipboard` buttons, while the actual launcher contract had moved to target-specific labels. | The security map now defines exact env-target controls such as `Capture RESEND_API_KEY from VM clipboard` as the public path and documents that placeholder `Capture <TARGET> from VM clipboard` copy is not allowed when the target is known. |
| Public runbook and launcher summary copy still opened with generic `Capture from VM clipboard` wording before later naming exact buttons, giving demo users two mental models for the same action. | Public recording rules now lead with exact `Capture <ENV> from VM clipboard` controls, and the Cloud Shell launcher summary gives a concrete example such as `Capture RESEND_API_KEY from VM clipboard`; tests reject the older generic public-runbook sentence. |
| Fallback strategy, acceptance-blocker, and live-control-room copy still used phrases like "target-specific Capture button" or generic `Capture from VM clipboard`, so unknown-provider recovery could still make users infer which button to press. | Generic provider fallbacks and blocker surfaces now say to use exact env-named Capture buttons and give a concrete example such as `Capture RESEND_API_KEY from VM clipboard`, keeping even recovery paths aligned with the visible launcher controls. |
| Repeated receipt actions such as multiple `dns.apply` rows could collapse into one Run Record audit entry, making a long run look less complete after detonation. | Run Record audit summaries now preserve every setup-receipt action with a non-secret receipt index, while still omitting receipt details and raw provider values. |
| A live receipt could claim `dns.apply` succeeded while the DNS approval gate artifact was missing, letting launch readiness rely on the receipt without independently proving the protected launcher approval click. | Live acceptance now fails any successful DNS apply unless the audit log contains a protected `Approve DNS apply` control-room resume event for a DNS approval gate, and the blocker tells the user to use the visible launcher approval. |
| One protected DNS approval click could satisfy live acceptance for multiple applied domains, even if only one `dns.<domain>.approval` gate was actually approved. | Live acceptance now matches successful `dns.apply` receipt domains to protected per-domain launcher audit events like `dns.moonlite.rsvp.approval`, and fails any applied domain without its own approval proof. |
| Control-room DNS approval could still try to prompt in the terminal before rendering the visible `Approve DNS apply` gate, making the launcher look stuck during a public demo. | In control-room runs, DNS approval now skips stdin entirely, immediately surfaces the protected launcher gate with the exact records, and waits for the visible approval button. Local non-control-room CLI runs can still answer the terminal prompt. |
| Stale hidden-prompt copy sanitized by the static or live control room could still say "target-specific Capture" even when the gate knew the exact env target. | Static and live gate rendering now pass known capture targets into the sanitizer, so stale hidden-prompt copy becomes exact visible controls like `Capture RESEND_API_KEY from VM clipboard`; targetless fallbacks say "visible env-named" instead of "target-specific." |
| Resend recovery next actions and stale generated-value capture errors could say `Capture RESEND_API_KEY from the VM clipboard`, which was close but not identical to the visible `Capture RESEND_API_KEY from VM clipboard` button. | Resend runtime and token-refresh recovery now use the exact visible Capture button text, and multi-value recovery lists exact controls instead of a combined `Capture A, B` phrase. |
| Provider-route and Resend-order blockers could still tell users to rerun setup or inspect backend artifacts such as `checkpoints.json`, instead of saying what to do in the launcher. | Harness, static control-room, and live control-room blocker guidance now keeps users in the same live control room, tells them to wait for provider-route cards with next actions/resume hints, capture `RESEND_API_KEY`, let FuseKit create/reuse the Resend domain by API, and approve DNS only after that API-owned Resend step. |
| After `RESEND_API_KEY` capture, the backend still returned generic provider-verification copy, leaving users unsure whether FuseKit would create the Resend domain or wait for manual domain work. | The Resend setup-key Capture response now says FuseKit will create or reuse the Resend sending domain by API, collect returned DNS records, and keep Cloudflare DNS waiting until those records exist. |
| Targetless hidden-prompt sanitizers could still render generic `Capture from VM clipboard` fallback wording, which taught a different control pattern than the actual env-named buttons. | Static and live control-room sanitizers now fall back to "the visible env-named Capture button such as `Capture RESEND_API_KEY from VM clipboard`," while generic gate guidance says to use `I finished this step` only when no env-named Capture button is shown. |
| Public README guidance still described generic VM-browser `Capture from VM clipboard` buttons even though the launcher renders exact env-named controls. | README public-run sections now teach exact controls such as `Capture RESEND_API_KEY from VM clipboard` and `Capture GITHUB_TOKEN from VM clipboard`, and public-doc tests reject the older generic wording. |
| Missing redacted-receipt proof could still tell users to "rerun setup," making a live launcher pause sound like a backend task. | Harness, static control-room, and live control-room receipt blockers now tell users to keep the live launcher/control room open while the setup worker finishes provider setup and saves a redacted receipt with no raw secrets. |
| Live acceptance parsed `raw_secrets_exposed` with Python coercion, so a malformed receipt could crash the harness or let a float such as `0.1` truncate into a false zero-secret proof. | Receipt acceptance now requires `raw_secrets_exposed` to be literal JSON `0`; strings, booleans, floats, or nonzero counts fail as redacted-receipt blockers instead of satisfying launch readiness. |
| Missing Resend DNS receipt proof could still tell users to rerun setup so an artifact proves the right order, instead of reinforcing the public Resend-first flow. | Acceptance blockers now tell users to capture `RESEND_API_KEY`, let FuseKit create/reuse the Resend sending domain by API, and approve DNS only after Cloudflare/DNS shows the exact Resend verification records. |
| Verification and rollback launch blockers could still use operator verbs like "run provider verification," "record checks," or "generate rollback metadata." | Harness, static control-room, and live control-room blockers now keep users in the live control room while FuseKit verifies every provider, resolves visible VM-browser gates, marks only safe pending waits, and writes provider rollback actions before launch. |
| Remote bootstrap checkpoint recovery could still tell users to rerun from encrypted vault/job state after VM setup trouble. | Bootstrap checkpoint guidance now keeps users in the launcher/control room and says FuseKit will surface the next visible VM or provider gate there if setup needs attention. |
| Provider-route recovery hints for Resend, Vercel, and failed custom routes could still tell users to rerun setup or rerun FuseKit. | Provider-route checkpoints now keep users in the live control room while FuseKit retries Resend setup, carries records into DNS approval, reapplies Vercel env wiring, or rechecks provider-owned blockers from the visible route card. |
| Old acceptance reports with only `missing[]` proof could still render operator-style actions like "Record selected-route," "Regenerate gate state," or "Regenerate provider capability packs." | Harness, static control-room, and live control-room fallbacks now keep the user in the launcher/control room while FuseKit writes provider-route proof, rebuilds gate cards, validates provider packs, and runs leak scans with vault/provider-secret recovery guidance. |
| Stale or repeated `I finished this step` clicks could regress a passed gate back to `resume_requested` or mint duplicate wake/audit proof after FuseKit was already rechecking. | The control-room pass route is now idempotent for `passed` and `resume_requested` gates: it reports the current state without mutating `gates.json`, adding wake events, or appending protected audit rows. |
| Stale `Open provider gate in VM` clicks could relaunch provider pages or mutate gate open metadata after FuseKit had already passed the gate or started rechecking it. | The control-room open route is now idempotent for `passed` and `resume_requested` gates: it reports the current state without launching Chrome, mutating gate state, or appending extra open-audit proof. |
| The Run Record could say the disposable worker was replaceable when durable state files existed, even if the replacement runner profile was missing or too thin to run OpenClaw, Playwright, noVNC, and the shared provider browser. | Durable resume proof now depends on `runner_readiness.json` and an intact `fusekit.runner-profile.v1` contract; recording readiness fails if the x86 visual runner cannot be recreated from the same verified profile. |
| Detonation launch blockers could still tell users to "Run detonation," making cleanup sound like a separate command instead of part of the guided safety path. | Harness, static control-room, and live control-room detonation blockers now keep users in the launcher/control room while FuseKit destroys plaintext worker, browser, visual, provider-auth, control-room, and gateway scratch state after encrypted proof is preserved. |
| Missing encrypted-vault proof could still tell users to run the launcher or retrieve `.fusekit/fusekit.vault.json`, which sounded like file handling instead of secure guided capture. | Vault blockers now keep users in the live launcher/control room with vault capture enabled, use only VM clipboard Capture controls for provider secrets, and wait for FuseKit to save encrypted vault proof. |
| The no-app-source Cloud Shell fallback told users to "rerun the fusekit launch command," making a missing upload/source case sound like a failed launcher loop. | The bootstrap now keeps users in the same Cloud Shell and says FuseKit will print the exact launch command to run after app files are present. |
| Acceptance report error and rehearsal-ready cards could still tell users to rerun or run acceptance, which sounded like an operator task outside the launcher. | Static and live control-room acceptance cards now keep users in the live launcher/control room while FuseKit rebuilds launch-readiness proof or gathers live provider evidence before recording. |
| Acceptance blockers generated upstream for malformed gates could still say "Regenerate..." before the control room rendered them, making report-level recovery sound like backend repair work. | Harness blocker guidance now keeps users in the live launcher/control room while FuseKit rebuilds stale gate cards, Resend setup gates, provider URLs, and exact Capture controls. |
| VM clipboard Capture rejected lowercase URLs and whitespace blobs, but uppercase copied URLs or comma/semicolon-separated token blobs could still look like a custom token. | Capture validation now rejects URL-looking values case-insensitively and refuses obvious multi-value clipboard blobs before anything enters the encrypted vault. |
| Provider gate URL validation required HTTPS but still allowed HTTPS loopback/private IP targets by default, leaving an unnecessary path from stale gate artifacts to local/private browser navigation. | Safe URL validation now rejects local and private network hosts by default, while explicit loopback allowance remains available only for health checks that intentionally opt in. |
| Custom provider packs without `handoff.token_env` could still pass validation with generic `Capture from VM clipboard` wording even when required env secrets were known. | Provider-pack validation now infers Capture targets from required env secrets and requires exact visible controls such as `Capture CUSTOM_API_KEY from VM clipboard` before a pack can drive the public launcher path. |
| Targetless provider strategy, verification-report, provider-pack, and control-room fallback copy could still show placeholder `Capture <TARGET>` button names. | Runtime fallback guidance now says to use the visible env-named Capture button and gives a concrete example such as `Capture RESEND_API_KEY from VM clipboard`, while acceptance keeps rejecting placeholder copy when concrete targets are known. |
| Unknown launch-evidence blockers could still tell demo users to rerun the same live launch or acceptance if no specific control appeared. | Static control room, live control room, and acceptance blocker generation now keep that recovery inside the current control room while FuseKit rebuilds the missing proof artifact. |
| A user could copy an env assignment or HTTP header-shaped token blob such as `RESEND_API_KEY=re_...` or `Authorization:Bearer ...`, which looked single-line enough to pass earlier token checks. | VM clipboard Capture now rejects assignment/header-shaped secret blobs before vault write and tells the user to copy only the raw provider token value inside the VM browser, then click the exact env-named Capture button again. |
| Cloud Shell launcher clipboard fallback could tell Mac users to press `Command+C` but did not name the opened backup panel or the cross-platform `Ctrl+C` path. | Launcher copy feedback now says when the command is copied to paste into OCI, and if copying is blocked it opens/selects the backup command and tells users to press `Command+C` or `Ctrl+C` before pasting into Cloud Shell. |
| Temporary live-control-room refresh failures could tell users to reopen or restart the control-room server, which sounds like a backend/operator task during a public demo. | Live refresh recovery now keeps users in the same control room and says FuseKit will keep trying to reconnect. |
| Clicking `I finished this step` could leave the button disabled as `Checking again...` while provider verification continued, making a successful click look stuck if refresh lagged. | The control room now changes the clicked button to `Recheck requested` and says FuseKit accepted the step, is rechecking, and will show the next action in the same control room. |
| After a gate moved into `resume_requested`, the rechecking panel could drop the durable success and avoid guidance, making the accepted click feel less grounded during slow provider verification. | Static and live control-room rechecking panels now keep rendering the gate's durable `Success looks like` and `Avoid` checklists, so users can still see the done-state and safety boundary while FuseKit retries. |
| Provider-gate open and finished-step failures could still mention static snapshots or low-level VM browser launch problems, making a live recovery feel disconnected from the launcher. | Live control-room action failures now keep the user in the current control room, translate VM browser launch failures into `Open live VM browser` recovery, and say FuseKit will keep the gate visible for retry. |
| Visual session sanitization allowed any credential-free host with a `/vnc.html` path, so a corrupted artifact could point the launcher iframe at an arbitrary hostname or private network target. | Static/live control rooms and launch acceptance now keep visual noVNC/control-room URLs only when they use the same public IP VM host; hostnames, loopback, and private IPs are rejected before rendering or launch-ready proof. |
| Visual session sanitization preserved expected noVNC query keys without checking their values, so a corrupted artifact could still add surprising iframe query state under `autoconnect` or `resize`. | Static/live control rooms and launch acceptance now keep only FuseKit-generated noVNC query values (`autoconnect=1`, `resize=scale`) and reject the visual session before rendering if those values drift. |
| `fusekit-runner-verify` could stop a bad VM before provider gates, but the successful proof was not preserved in the retrieved live artifact bundle, so acceptance could not independently prove the background workstation was prepared before the demo. | Remote bootstrap now writes `.fusekit/runner_readiness.json`, artifact retrieval requires it, and live acceptance fails unless the proof shows x86_64, runner helpers, visual/noVNC commands, OpenClaw, Playwright Chromium, and the shared provider browser profile were ready. |
| The live control-room CSP allowed all inline script and style so the generated one-page app could run, which was broader than the launcher needs during public remote sessions. | Served live control-room HTML now gets a per-response nonce applied to FuseKit's generated stylesheet, bootstrap JSON, and event script; CSP no longer uses `unsafe-inline`, disables inline style/script attributes, and keeps `object-src 'none'`. |
| Resend setup guidance said to create a Full access key, but the provider UI also asks for a Domain selector, leaving demo users to decide whether "All domains" was safe or required. | Resend handoffs, generated packs, playbooks, gate guidance, verification gates, and the public runbook now name the exact setup choices: `Permission: Full access` and `Domain: All domains`; FuseKit still creates/reuses domains and audiences by API after capture. |
| Stale Resend provider errors could still say only "Full access" and the public runbook left a future-gate exception around Add domain/Add audience, reopening the same user decision during a recording. | Resend authorization gates now normalize stale rejection text to include `Permission: Full access` and `Domain: All domains`, and the runbook flatly says not to click Resend Add domain or Add audience because FuseKit owns those API setup steps. |
| Resend's exact setup-key selector copy was present in generated packs but not enforced by provider-pack validation, so future generated or edited packs could regress to vague Full access wording. | Provider-pack validation now rejects Resend handoffs unless secret steps explicitly name both `Permission: Full access` and `Domain: All domains` alongside the exact VM clipboard Capture control. |
| Launch acceptance still allowed a stale Resend setup-key gate to say only "Full access" or "all domains," even though generated packs now enforce the exact selector labels. | Launch-readiness checks now reject Resend `RESEND_API_KEY` gates unless the live gate guidance explicitly names both `Permission: Full access` and `Domain: All domains`, keeping the user-facing control room aligned with the no-thinking demo path. |
| The Resend action steps named the exact setup-key selectors, but the control-room "Success looks like" checklist still summarized the key as only "Full access." | Resend gate success criteria now also name `Permission: Full access` and `Domain: All domains`, so the user's checklist matches the provider UI labels and the exact Capture flow. |
| Existing Resend-key guidance still said an existing "Full access" row was not enough, which could make users check only one selector and miss the Domain dropdown during a recording. | Resend handoffs, gate guidance, verification recovery, and the public runbook now say even a row with `Permission: Full access` and `Domain: All domains` is not enough unless the raw key value is copyable; otherwise create a new setup key and capture it from the VM clipboard. |
| Launch acceptance enforced Resend's exact setup-key selector labels but could still allow a stale gate that did not explain why an existing key row is unusable once the raw copy-once value is hidden. | Resend setup-key gates now fail launch readiness unless the durable guidance explains that existing `Permission: Full access` / `Domain: All domains` rows are not enough without the raw key value captured into FuseKit's encrypted vault. |
| When Resend setup-key guidance failed launch readiness, the blocker recovery text could still fall back to generic gate rebuild instructions instead of telling the launcher what no-thinking guidance to show. | Resend setup-key blockers now name the exact recovery surface: Resend API Keys, `Permission: Full access`, `Domain: All domains`, the raw-key-value warning, and `Capture RESEND_API_KEY from VM clipboard`. |
| Persisted provider-strategy route cards could contain stale generic text such as "click Capture from VM clipboard" even when the strategy target was known, so a refreshed control room could be less precise than the rendered gate button. | Static and live strategy rows now pass known strategy targets into the launcher-copy sanitizer, turning generic Capture wording into exact labels such as `Capture GITHUB_TOKEN from VM clipboard` before the user sees it. |
| Provider strategy route headlines could still say to complete generic login/MFA/CAPTCHA/consent/token creation even when the pack knew the exact provider token the user needed to create. | Strategy next actions now use provider-pack display names and token labels, so the route headline says to follow the GitHub, Vercel, Cloudflare, or Resend steps below and create or reveal the exact API token before clicking the exact Capture button. |
| Token-refresh gates after a failed provider API health check still used env-var shorthand such as "fresh VERCEL_TOKEN" in the headline, forcing users to translate the recovery path during a demo. | Contract-health recovery now uses provider display names and token labels in the headline, for example telling users to follow the Vercel steps below to create or reveal a fresh Vercel API token before clicking `Capture VERCEL_TOKEN from VM clipboard`. |
| Public source download accepted GitHub zip entries after path containment checks but did not explicitly reject symlink or special-file metadata from an untrusted archive. | Source fetch now rejects symlink, device, FIFO/socket, absolute, and backslash zip entries before replacing the app directory, keeping public launcher source retrieval on normal files only. |
| OCI cleanup could be attempted after a run while still marking the workspace detonation checkpoint green even if the cloud provider reported a failed VM or network-resource deletion. | Workspace detonation now writes a redacted `.fusekit/workspace_detonation.json` receipt, marks the run-state contract when the disposable workspace is actually destroyed, and fails the detonation checkpoint if any remote-worker or OCI resource delete reports a `failed.*` result. |
| The launcher had many durable proof files, but no single product object that connected run state, checkpoints, provider gates, verifier status, credential metadata, artifacts, approvals, errors, and detonation proof. | Launch saves now write `.fusekit/run_record.json` as a central non-secret Run Record; OCI artifact retrieval requires it, the control room exposes it in the live payload, and the record includes only vault metadata plus redacted/provider-safe status. |
| Live acceptance could still turn green from the individual OCI survivor artifacts without proving the central Run Record was present and shaped correctly. | Live acceptance now requires a valid `.fusekit/run_record.json` contract and blocks public/recording readiness until it ties together state, gates, provider routes, verifier checks, artifacts, approvals, errors, vault metadata, and detonation proof. |
| Provider gates could persist `resume_requested` state, but there was no independent event stream proving the capture or approval woke the worker for automatic retry. | Gate captures and resume approvals now append redacted `.fusekit/gate_events.jsonl` wake events, remote OCI retrieval preserves them, and the central Run Record summarizes event counts and recent wake events without storing raw secrets. |
| A runner could pass individual readiness checks without proving the exact OCI visual-browser profile needed for a public demo and safe detonation. | Runner readiness now carries a `fusekit.runner-profile.v1` contract for the x86_64 OCI visual/browser worker: supported Linux images, 16 GB memory floor, noVNC/control-room/gateway ports, OpenClaw/Playwright/Chromium stack, shared provider profile, and required health checks; live acceptance and the Run Record require that profile proof. |
| The Run Record centralized state, gates, artifacts, approvals, errors, and detonation proof, but logs and visual evidence were still only implicit artifact paths. | Run Records now include a `fusekit.evidence-inventory.v1` section that classifies audit/event logs, controlled runtime logs, visual state, screenshots, and receipts by path/type only; live acceptance rejects missing or credential-looking evidence inventories without embedding raw logs, screenshot bytes, provider URLs, clipboard values, or secrets. |
| Public proof schema versions for evidence inventory, verifier summary, audit trail, and recording contract still lived in Run Record or validators while their field contracts lived in separate modules. | The shared proof modules now own those schema versions too, and regression coverage proves Run Record generation, control-room recordability checks, live acceptance, and cleanup preflight consume the same schema authorities. |
| A new-site rehearsal could still require reconstructing human actions from gate state and audit logs to see whether the user followed the guided launcher path. | Run Records now include a `fusekit.human-action-trace.v1` summary, and the control room renders it as a rehearsal audit: provider opens, VM-clipboard Capture clicks, and approval clicks must map to known guided gates with exact visible controls, while live acceptance rejects unguided actions. |
| The Run Record proved provider routes and human actions separately, but it did not explicitly state the automation boundary: humans should only use VNC for unavoidable gates, then FuseKit should own provider mutations by API/CLI/vault on disposable OCI state. | Run Records now include a `fusekit.automation-boundary.v1` section, live acceptance requires it, and the control room shows which routes FuseKit owns after authorization versus which routes are human-gate only, with no user-machine state and resume-after-worker-replace proof. |
| Live provider verification existed as a raw report and control-room trust cards, but the central Run Record did not independently summarize which provider checks were green, pending-safe, or blocking. | Run Records now include a `fusekit.verifier-summary.v1` ledger; live acceptance requires every verifier to be passed, skipped, or pending-safe, and the control room renders provider verifier proof from the Run Record before launch readiness or detonation proof is trusted. |
| Audit evidence existed as raw JSONL, receipts, gate events, and detonation receipts, but the central product object did not give users a plain-language audit trail for credential capture, provider actions, DNS writes, approvals, and cleanup. | Run Records now include a `fusekit.audit-trail.v1` summary derived from redacted runtime evidence; live acceptance requires matching audit categories for observed actions, rejects credential-looking audit text, and the control room renders the audit trail without provider URLs, clipboard values, raw tokens, or secrets. |
| Provider playbooks were validated as a separate live artifact, but the final recording contract could still be green without proving the ordered no-thinking provider playbook was present. | Recording readiness now fails closed unless the Run Record includes a valid provider playbook with ordered steps and safety notes, so a demo cannot claim readiness without the exact provider order and user-facing guidance. |
| Even with durable state, runner profile, provider verifiers, guided human actions, audit trail, and detonation proof present, demo readiness still had to be inferred across several Run Record sections. | Run Records now include a `fusekit.recording-contract.v1` proof that fails closed unless durable OCI state, the x86 visual runner, ordered provider playbooks, guided human actions, post-gate automation, live verifiers, audit evidence, no-trace detonation, and an empty error list all agree; the control room renders this as the final "Ready to show the magic path" panel. |
| The workspace detonation receipt could say `complete` without a resource-class summary, making it harder to prove during a public demo that the remote worker, OCI VM, and FuseKit-created network resources were all covered. | OCI detonation receipts now include `fusekit.workspace-detonation-resources.v1`; live acceptance and the recording contract fail closed unless the remote worker, compute instance, network resources, failure list, and root-compartment preservation/detonation scope are explicitly represented, and the control room renders this as an OCI no-trace receipt. |
| The live acceptance harness and the Run Record recording contract each checked runner readiness, but they could drift into two subtly different definitions of a valid OCI visual worker. | The OCI runner profile validator is now shared, so recording readiness and live acceptance both require the same x86_64 architecture, supported OS image family, 16 GB memory floor, noVNC/control-room/gateway ports, OpenClaw/Playwright/Chromium stack, shared provider profile, browser cache, and health checks. |
| Evented resume existed as a redacted `gate_events.jsonl` stream, but the current gate record did not remember which event changed it, so a resumed gate could look current without a durable event anchor. | Capture and resume actions now stamp `last_wake_event_id`, event name, and timestamp onto the gate record using the same id written to `gate_events.jsonl`; live Run Record acceptance rejects resumed or partially captured gates whose current state is not anchored to a matching wake event. |
| Control-room audit events could prove that a protected Capture or `I finished this step` action happened, but they were not tied to the exact wake event that resumes the worker after user input. | Capture and resume audit payloads now include the wake event id, and live acceptance only counts those audit events when the id exists in `gate_events.jsonl`; after OCI detonation, the Run Record can still prove which protected human action woke the run. |
| The central Run Record audit trail summarized gate-event actions but did not carry the wake event id, so post-detonation proof still required cross-checking raw `gate_events.jsonl`. | Run Record audit entries derived from gate events now include the non-secret wake event id, acceptance rejects missing or stale ids, and repeated protected actions with different wake ids remain distinct audit entries. |
| Long live runs could produce more than 40 redacted audit events, but the Run Record only retained the first 40 while counting the full set, risking an internally inconsistent post-detonation proof. | Run Records now retain the full redacted audit trail, including every wake-linked Capture and approval event, so long OCI rehearsals do not lose evidence or fail acceptance because of local truncation. |
| The control-room CSRF docs implied every state-changing POST always carries browser `Origin` and `Sec-Fetch-Site` metadata, even though deterministic local/runner automation can omit those headers while still using the owner-only action token. | The threat model and security surface map now distinguish browser-origin metadata from local automation: declared cross-site or same-site browser requests fail closed, missing metadata remains compatible only with the control-room header plus action token, and tests pin that boundary. |
| The central Run Record could include a hollow `provider_strategies` object while a separate `provider_strategies.json` artifact carried the real route proof, weakening the post-detonation product object. | Live Run Record acceptance now requires a provider-strategy contract with schema version, provider list, strategy rows, selected route status, and fallback candidates, so provider route decisions survive OCI detonation inside the central record. |
| The Run Record provider-strategy contract could be well-shaped but stale compared with the standalone `provider_strategies.json` artifact produced by the worker. | Live acceptance now compares normalized provider route signatures between the Run Record and `provider_strategies.json`, so provider/recipe/strategy/status/selected-route/candidate drift blocks recording readiness before OCI detonation. |
| The Run Record provider playbook could be well-shaped but stale compared with the ordered playbook in `provider_strategies.json`, leaving the control room free to show old follow-me instructions after the worker had produced the corrected route plan. | Live acceptance now compares normalized provider playbook signatures between the Run Record and `provider_strategies.json`, so step order, controls, instructions, and safety notes cannot drift before public recording readiness. |
| Detonation preflight trusted the central provider strategy/playbook proof without requiring and comparing the standalone `provider_strategies.json` survivor, so cleanup could proceed with stale or missing route decisions even after live acceptance learned to reject that drift. | Cleanup preflight now requires `provider_strategies.json` and compares normalized provider route and playbook signatures between that survivor and the central Run Record before worker destruction is trusted. |
| Detonation preflight could compare provider strategy signatures while ignoring malformed central route rows that matched the survivor artifact, such as missing recipes, selected statuses, or fallback candidates. | Cleanup preflight now validates the central `provider_strategies` schema, provider list, strategy rows, selected route status, fallback candidates, and public provider coverage before route/playbook drift comparison can support worker destruction. |
| Provider strategy fallback candidates could be present but hollow, with empty or non-object rows that let the route ledger claim alternatives were considered without preserving kind/status evidence. | Live acceptance and cleanup preflight now validate every fallback candidate row as an object with non-empty kind and status before provider route proof can support public readiness or worker cleanup. |
| Detonation preflight could accept central provider strategy rows that named a selected kind/status but omitted the live route-decision evidence users rely on, such as deterministic/implemented booleans, route reasons, or follow-me fields for human gates. | Cleanup preflight now validates selected-route deterministic/implemented/reason fields and the follow_steps, next_action, resume_hint, success_criteria, and avoid_steps required for `needs_human_gate` strategies before cleanup can trust route proof. |
| Live acceptance could validate central provider-strategy presence while omitting selected-route deterministic/implemented/reason, provider-specific evidence, or human-gate follow-me fields, leaving the Run Record thinner than `provider_strategies.json`. | Run Record acceptance now requires the same selected-route proof as standalone strategies and compares route signatures including reason/evidence/follow-me fields, so central route proof cannot drift or thin out. |
| Detonation preflight could trust a Resend API route that looked deterministic but omitted the provider-specific evidence proving FuseKit owns domain/audience creation, users do not manually create those resources, and Resend domain setup precedes DNS approval. | Cleanup preflight now enforces the same Resend selected-route evidence as live acceptance before central provider strategies can support worker cleanup. |
| Detonation preflight could compare provider playbook signatures while ignoring stale route semantics shared by both survivors, such as wrong actor ownership, generic Capture controls, wrong proof sources, unsafe Add Domain instructions, host-browser safety notes, or DNS approval before Resend/Vercel setup. | Cleanup preflight now applies the live provider-playbook actor, control, proof-source, resume-event, unsafe-instruction, safety-note, and Resend-before-DNS order checks to the central Run Record before matching `provider_strategies.json` can support worker destruction. |
| Provider playbook steps could preserve padded actor/control/proof text or extra sidecar route-plan notes while still normalizing enough to satisfy route semantics. | Live acceptance and cleanup preflight now reject padded provider-playbook step fields and unexpected sidecar fields before route-plan proof can support recording or cleanup trust. |
| Provider playbook safety notes could preserve padded, duplicated, empty, or object-shaped guidance while still joining into text that contained the required launcher phrases. | Live acceptance and cleanup preflight now reject loose provider-playbook safety-note rows before safety guidance can support public recording or cleanup trust. |
| The raw `provider_strategies.json` survivor could carry sidecar route notes or padded route fields that normalized to the same central Run Record signatures, letting live acceptance or cleanup trust a hand-edited provider route artifact. | Live acceptance and cleanup preflight now validate the standalone provider-strategy artifact shape before route proof or signature comparison: top-level, provider, strategy, decision, selected-route, candidate, and embedded playbook-step rows must use generated fields only. |
| Generated Run Records could still copy loose provider-strategy route rows into the central proof object, preserving padded selected-route fields, sidecar decision notes, duplicate fallback candidates, or secret-shaped candidate data until validators caught the malformed route proof. | Run Record generation now canonicalizes provider-strategy route proof first: provider, strategy, decision, selected-route, evidence, and candidate rows are trimmed/redacted into the generated shape, duplicate fallback candidates collapse by kind/status, sidecar fields are dropped, and unsafe route rows cannot become public proof. |
| The CLI artifact writer, Run Record generator, live acceptance, and cleanup preflight still depended on separate provider-strategy vocabularies, so a route-decision field or schema version could drift between generated proof and launch gates. | A shared provider-strategy contract now owns the artifact schema version, public envelope, provider rows, strategy rows, decision fields, selected-route fields, fallback-candidate fields, and ordered writer fields for deterministic route proof. |
| Generated Run Records could still copy padded provider-playbook rows, sidecar fields, malformed booleans, or secret-shaped guidance into the central proof object and embedded `provider_strategies.playbook`, leaving validators to discover that the published playbook surface was not canonical. | Run Record generation now canonicalizes provider-playbook proof first: step rows keep only generated fields with trimmed/redacted public text and literal booleans, secret-shaped or malformed rows are dropped, safety notes are public-safe text only, counts are derived from canonical steps, and embedded provider-strategy playbook proof reuses the same clean shape. |
| The model/inference lane became acceptance-critical, but public launch docs and the security map could still omit `llm_contract`/`model_inference` proof from the recording bar, making a demo checklist weaker than the code path. | README, the acceptance runbook, public launch readiness docs, and the security surface map now require matching `model_inference` and `llm_contract` proof: encrypted API-key or OpenClaw authorization, raw-secret export denied, and encrypted-vault storage. |
| Provider playbook steps could include plain instructions without a machine-checkable provider, route, or visible control, leaving the control room unable to prove who owns the next action or whether the user should Capture, approve, or simply watch FuseKit work. | Provider playbook generation now writes `provider`, `route`, and `control` for every step, and live Run Record acceptance rejects unsupported route/control combinations, so follow-me steps and API-owned steps are explicit before recording readiness. |
| The Run Record vault summary could claim a different number of captured credential records than the public metadata list contained, or include anonymous/blank records, weakening post-detonation proof of which secrets were actually captured without revealing values. | Live acceptance now requires `vault.record_count` to be a real integer matching shaped `vault.records` metadata (`id`, `kind`, `provider`, and `label`), while still rejecting any raw `value` field, so credential-capture proof cannot be inflated, hidden, or anonymous. |
| The final recording contract could mark `vault` ready from padded vault metadata or sidecar vault notes that acceptance would not want to present as public credential-capture proof. | Recording readiness now rejects loose vault metadata before `recording_contract.checks.vault` can turn green: `record_count` must be a literal integer, `vault` and `vault.records` must use only generated fields, id/kind/provider/label values must already be trimmed and public-safe, and sidecar vault proof is refused. |
| Detonation preflight still trusted looser Run Record vault metadata than the final recording contract, so cleanup could proceed from padded vault rows, sidecar notes, or coerced counts that public recording readiness would reject. | Cleanup preflight now applies the same exact vault bar before worker destruction: literal integer `record_count`, no sidecar vault fields, trimmed public-safe id/kind/provider/label strings, and no credential-looking vault record text. |
| Run Record recording checks, live acceptance, and cleanup preflight imported vault metadata shapes from Run Record, leaving the public vault envelope and raw-secret field denylist coupled to the writer instead of a neutral contract. | A shared vault-proof contract now owns the public vault envelope, record fields, and forbidden raw-secret field names, with regression coverage proving all three launch gates consume that canonical authority. |
| The Run Record durable-state section could include extra survivor/resume sources that smuggled VM-local browser profiles, provider auth scratch, passphrase files, or logs into the "durable" side of the detonation boundary. | Live acceptance now rejects volatile worker-state markers in `durable_state.sources`, `detonation_preserves`, `detonation_scope.must_preserve`, and worker-replacement `resume_sources`, so runtime resume remains encrypted/redacted rather than depending on dirty VM-local state. |
| Live verifier cards could be green in the central Run Record while the raw `verification_report.json` artifact carried a different provider-check result. | Live acceptance now compares normalized verifier signatures between the Run Record and `verification_report.json`, including pending-safe checks, so control-room green checks cannot drift before OCI detonation or public recording readiness. |
| Live acceptance compared the Run Record verifier summary against `verification_report.json` but did not compare the embedded redacted `verification` object, leaving room for stale provider-check rows in the central proof object. | Live acceptance now compares both Run Record verifier views against `verification_report.json`, so public readiness fails if the summary and embedded verification proof drift apart. |
| Detonation preflight checked the Run Record verifier summary and standalone verifier report separately, leaving cleanup able to proceed when the two survivor artifacts disagreed about which provider check actually passed. | Cleanup preflight now compares normalized verifier signatures between the central Run Record and `verification_report.json`, including pending-safe checks, before worker destruction is trusted. |
| Verification report rows could preserve padded provider/check/status text, empty generated summary or repair text, non-object details, or sidecar fields while still normalizing enough to match Run Record signatures. | Shared verification-report validation now rejects loose standalone and embedded verification rows before verifier proof can support public readiness or cleanup trust. |
| The verification report writer and preflight validator still carried separate check-row and status vocabulary, so generated verifier artifacts could drift from the launch gate's verifier proof rules. | The verification report writer now owns check-row fields, schema, status/count vocabulary, safe statuses, and pending-safe check ids consumed by live acceptance and cleanup preflight. |
| Detonation preflight could trust a mechanically green verifier summary that omitted the public statement explaining live provider verifiers, green checks, and pending-safe checks. | Cleanup preflight now requires the same live-verifier guidance statement as Run Record acceptance before verifier proof can support worker destruction. |
| Standalone verification reports could mark anonymous rows as passed, relying on fallback names such as `provider_status` instead of proving which provider check actually ran. | Shared verification-report validation now requires every check row to name provider, check, and status before live acceptance or detonation preflight can treat it as launch-safe proof. |
| Run Record verifier summary rows could preserve padded provider/check/status text, non-boolean pending-safe flags, or sidecar verifier notes while still normalizing enough to satisfy coverage. | Live acceptance and cleanup preflight now reject loose verifier-summary rows before green verifier proof can support public recording or worker cleanup. |
| Skipped verifier rows could satisfy public provider coverage just by naming GitHub, Resend, Vercel, DNS, or `live_app`, letting optional or intentionally skipped checks look like launch proof. | Recording readiness, live acceptance, standalone verification coverage, and detonation preflight now count only passed or explicitly pending-safe verifier rows toward public-provider and `live_app` coverage. |
| Control-room verifier cards still painted skipped verifier rows as green even after proof gates stopped counting them, so the demo surface could visually bless missing live evidence. | Static and live-refresh verifier cards now render skipped rows as pending/checking with explicit skipped copy, matching the launch and detonation proof gates that only count passed or pending-safe rows. |
| Verifier summaries could still say every verifier was green or pending-safe while optional skipped rows were present, leaving humans to infer that skipped rows were launch proof. | Generated Run Records, live acceptance, cleanup preflight, and control-room summaries now state that skipped verifier rows are optional transparency records and do not count as provider or live-app proof. |
| The Run Record could carry a complete-looking workspace detonation receipt while the standalone `workspace_detonation.json` artifact had different cleanup evidence. | Live acceptance now compares normalized detonation receipt signatures between the Run Record and `workspace_detonation.json`, so no-trace OCI cleanup proof cannot drift before public recording readiness. |
| The Run Record evidence inventory could claim logs, visual state, screenshots, or receipts existed without proving those paths were real survivor files in the retrieved `.fusekit` artifact bundle. | Live acceptance now requires inventoried evidence paths to be relative `.fusekit` files that exist, match their section kind, and have accurate counts, so public demo proof cannot rely on invented or stale evidence paths. |
| The Run Record could summarize Capture or approval wake events that no longer matched the raw `gate_events.jsonl` stream, weakening evented resume proof after OCI detonation. | Live acceptance now compares normalized wake-event signatures between the Run Record and `gate_events.jsonl`, so audit entries for Capture and approval actions stay anchored to the event stream that actually woke the worker. |
| The final recording contract and cleanup preflight could trust wake-event summaries after normalizing padded event rows, sidecar notes, coerced counters, or malformed target/timestamp fields. | Recording readiness and cleanup preflight now require generated-field-only wake-event rows, literal integer totals/event counts, trimmed event/gate/provider/status/target and captured-target values, integer target counts, and non-negative event timestamps before wake proof can support recording or worker cleanup. |
| The raw `gate_events.jsonl` survivor could still contain sidecar wake-event fields or padded row values that normalized to the same Run Record signature during cleanup. | Detonation preflight now validates every raw `gate_events.jsonl` row against the generated wake-event shape before signature comparison, so sidecar survivor fields, padded values, malformed target counts, and bad event timestamps block worker destruction directly. |
| The raw `gates.json` survivor could still contain sidecar provider-gate fields, padded gate ids, or malformed captured-target/attempt/timestamp values that normalized to the same Run Record signature during cleanup. | Detonation preflight now validates raw `gates.json` provider-gate rows against the generated gate shape before signature comparison, so sidecar survivor fields, padded structural values, malformed target lists, and bad attempt/timestamp proof block worker destruction directly. |
| Live acceptance could trust resolved `gates.json` rows while ignoring sidecar raw survivor fields that cleanup preflight rejected later. | Live acceptance now validates the raw `gates.json` envelope and provider-gate rows against the generated gate shape before gate readiness can pass, so sidecar gate proof, padded fields, malformed target lists, or bad attempt/timestamp proof cannot support launch readiness. |
| `GateService` wrote the raw `gates.json` envelope while cleanup preflight carried its own envelope key set, leaving the outer survivor shape easy to drift. | The shared gate-proof contract now owns the raw `gates.json` envelope used by `GateService` serialization and detonation preflight. |
| Run Record recording checks, live acceptance, and cleanup preflight depended on Run Record-local provider-gate and wake-event key sets, making guided-control proof shapes easy to drift as new gate fields were added. | A shared gate-proof contract now owns provider-gate summary/record and wake-event summary/record shapes, and regression coverage proves Run Record, live acceptance, and detonation preflight all consume that canonical authority. |
| Run Record approval rows could preserve padded id/provider/status/reason text or sidecar approval notes while still normalizing enough to match durable gates and wake events. | Live acceptance and cleanup preflight now reject loose approval rows before protected approval proof can support public readiness or worker cleanup. |
| Run Record generation, live acceptance, and cleanup preflight still carried separate approval row field/status vocabularies, so protected approval proof could drift across launch gates. | A shared approval-summary contract now owns approval row fields, public text fields, timestamp field, and accepted statuses across the writer and both validators. |
| Generated approval summaries could still preserve padded fields, blank provider/reason text, duplicate ids, or malformed timestamps from durable gate state, making a valid user approval look malformed by the time acceptance reviewed it. | Run Record writing now canonicalizes approval rows before persistence: id/provider/status/reason are redacted and trimmed, provider/reason get public fallback text, duplicate ids collapse, and approval timestamps normalize to non-negative numbers. |
| The Run Record could claim an x86 visual/browser runner profile that differed from the actual `runner_readiness.json` proof produced by the OCI worker. | Live acceptance now compares normalized runner-profile fields between the Run Record and `runner_readiness.json`, so recording readiness cannot rely on stale memory, architecture, browser-stack, or health-check claims. |
| Detonation preflight trusted the Run Record runner profile without requiring the standalone `runner_readiness.json` survivor, leaving cleanup able to erase the worker before the VM/browser handoff proof survived independently. | Cleanup preflight now requires `runner_readiness.json`, validates the shared runner-readiness contract, and compares normalized runner-profile fields against the central Run Record before worker destruction is trusted. |
| Generated Run Records could still copy loose runner-readiness sidecars, padded VM/browser labels, malformed health-check values, or raw helper binary paths into the public `runner_profile` surface before acceptance rejected the raw survivor. | Run Record generation now canonicalizes runner-profile proof first: only shared readiness/profile/observed/check/binary contract fields are emitted, text is trimmed/redacted, VM-local provider-profile/cache/binary paths are relabeled, malformed checks are omitted, and incomplete runner proof remains a recording blocker. |
| Run Record generation, live acceptance, and cleanup preflight could still rely on runner-readiness constants without a regression proving they shared the same VM/browser handoff authority. | The shared runner-readiness contract now owns immutable readiness/profile/browser/observed/binary key sets plus schema, status, and expected runner-profile constants, with regression coverage across the writer and both validators. |
| Detonation preflight trusted the Run Record provider-gate and wake-event summaries without requiring standalone `gates.json` or `gate_events.jsonl` survivors, so cleanup could erase the worker before evented resume proof survived independently. | Cleanup preflight now requires `gates.json` and `gate_events.jsonl`, rejects unsafe public text in both, and compares gate/wake signatures against the central Run Record before worker destruction is trusted. |
| The worker replacement contract could say the OCI VM was disposable while referencing state that was not actually listed as durable, omitting volatile surfaces, or disagreeing with the detonation preserve list. | Live acceptance now checks replacement resume sources against `durable_state.sources`, requires replacement volatile surfaces to cover the detonation surfaces, and requires detonation preserve scope to match, so "kill/recreate the VM" proof cannot depend on hidden local browser profiles or VM scratch state. |
| Older acceptance reports that only carried failed audit-log or per-provider pack snapshot check rows could still fall back to generic launch-evidence repair copy. | `audit.*` and `provider_pack.*` checks now become named Audit log and Provider packs blockers across acceptance reports and static/live control rooms, pointing the launcher back to redacted JSONL audit evidence and provider capability-pack validation without raw secrets. |
| Stale reports that failed during manifest loading, manifest snapshotting, or setup-plan generation could still leave the launcher with generic launch-evidence repair copy. | `manifest.*` and `plan.generated` checks now become named Manifest and Setup plan blockers across acceptance reports and static/live control rooms, tying recovery to app rescanning, manifest snapshots, and the visible Approve setup plan control. |
| The rehearsal review could prove that all human actions matched by count while omitting a per-action review ledger, leaving post-detonation auditors to infer which gate/event evidence proved each match. | Run Records now include `rehearsal_review.reviewed_actions`, live acceptance rejects any drift from `human_actions`, and static/live control rooms display the non-secret proof source (`gates.json` plus `gate_events.jsonl` for wake-driven actions) on matched human-action cards. |
| The acceptance report could say `recording_ready=true` while forcing public reviewers to open the Run Record to see which recording-contract sections actually passed. | Live acceptance reports now embed a redacted `recording_contract` summary with boolean section checks and blockers, so `.fusekit/acceptance/report.json` carries the public recording bar directly without raw secrets or bulky evidence. |
| A refreshed control room could consume the acceptance report's `recording_ready` boolean but hide the report-level recording-contract checklist from the launch blockers panel. | Static and live control rooms now render `recording_contract.checks` from the acceptance report itself, showing passed proof sections when recordable and pending sections when public launch proof exists but recording proof is incomplete. |
| Public acceptance reports converted recording-contract check values with truthiness, so a hand-edited string or number could display as a green section even though Run Record validation failed elsewhere. | Acceptance reports now render a recording-contract section as true only when the source value is literal JSON `true`, keeping malformed proof visibly unready in the public artifact. |
| A stale or hand-edited acceptance report could include raw token-shaped details or extra fields that bypassed the normal report generator's redaction before reaching static HTML or `/api/job`. | Control-room acceptance report loading now fail-safes through a narrow redacted schema: flags are strict booleans, blockers/checks/missing proof and recording-contract text are public-redacted, paths become public artifact paths, and unknown fields are dropped. |
| A stale or hand-edited Run Record could carry raw callback URLs, token-shaped statement text, or unexpected extra fields into the browser payload even though generated Run Records are normally redacted. | Control-room Run Record loading now recursively redacts public text and local absolute paths before embedding static HTML or serving `/api/job`, and frontend public-copy rendering applies the same token-shaped redaction as a last-line guard. |
| Stale verifier or provider-strategy artifacts could carry raw repair text, provider callback URLs, or token-shaped playbook details directly into trust cards and route-plan cards. | Verification reports and provider strategy/playbook artifacts now pass through the same control-room public-payload redaction as Run Records, preserving provider/check/route ids while scrubbing repair, summary, reason, next-action, proof, and extra-field text before static HTML or `/api/job`. |
| Base control-room state still carried job step details, checkpoint hints, artifact paths, run-state notes, and the standalone model/inference contract through separate rendering paths from the hardened proof artifacts. | Base job payloads, static artifact rows, run-state notes, and `llm_contract.json` now use context-aware public redaction before browser delivery, while preserving structural ids and the live action token needed for protected controls. |
| Control-room POST responses could display durable gate hints or provider errors directly after protected actions, leaving a smaller browser-visible path for stale token-shaped text even after static and `/api/job` payloads were hardened. | All control-room JSON responses now pass through the shared context-aware public redactor before socket write, preserving structural gate/status/capture/wake fields while scrubbing token-shaped messages, callback URLs, provider errors, and path-like details. |
| New Run Record, verifier, or response fields could use path-shaped names such as `trace_path`, `log_file`, `workspace_dir`, or `bundle_root` and bypass the older exact-key public path normalization. | Control-room public payload redaction now classifies suffix-shaped path keys and normalizes their values before browser delivery, preserving `.fusekit/...` artifact paths or basenames while removing local runner/workspace prefixes. |
| A corrupted or hand-edited `visual.json` could include extra note/path/token fields, or omit `novnc_url` while still exposing control-room URLs and noVNC passwords through `/api/job`. | Visual-state loading now validates the noVNC/control-room fields first, preserves only the fields required by the live VM iframe, drops partial visual credentials when no safe iframe URL exists, and redacts extra text/path metadata before browser delivery. |
| Durable `gates.json` records could still carry token-shaped provider callback URLs or copied-token text in reasons, resume URLs, follow steps, success/avoid criteria, next actions, or resume hints even after gate targets were redacted. | Control-room gate loading now applies the shared context-aware public redactor to the full gate display record while preserving structural gate ids, statuses, env targets, and Open/Capture controls. |
| The final recording contract could claim `errors_empty` while the central Run Record still carried unresolved errors from verification, gates, acceptance, or detonation. | Live acceptance now requires `recording_contract.checks.errors_empty` to agree with the Run Record error list, so public demo readiness cannot hide unresolved setup or cleanup failures behind otherwise green proof sections. |
| Run Record errors, steps, or checkpoints could carry raw provider callback URLs, bearer text, token-looking strings, or unshaped error values from failed verification/gate/detonation payloads. | Run Record error and timeline collection now redacts callback URLs and token-shaped material before writing, and live acceptance rejects error, step, or checkpoint entries that are unshaped or still contain credential-looking text. |
| Tokenized remote control-room links cleaned the token out of the address bar, but the resulting browser cookie had no explicit lifetime. | Remote control-room cookies now remain `HttpOnly` and `SameSite=Strict` and also carry an eight-hour `Max-Age`, keeping public demo sessions convenient without creating unbounded browser credentials. |
| Run Record human-action proof could name exact visible controls while still pointing at a stale or nonexistent provider gate, or capturing the wrong provider token for the gate being shown. | Live acceptance now ties every recorded human action to an existing `provider_gates.records` id and rejects copy-once Capture actions unless the captured env target matches that gate's declared target. |
| An empty OCI cleanup result could be interpreted as a complete workspace detonation because no missing resource classes were derived from the blank report. | Workspace detonation now fails closed unless cleanup positively reports remote-worker cleanup, OCI VM termination, and all standard FuseKit-created network resource classes. |
| The central Run Record provider playbook could survive OCI detonation with stale DNS-before-Vercel ordering, even though the public demo path needs Resend values, then Vercel env/deploy work, then DNS approval with the full record set. | Provider playbook generation now orders Capture, Resend API setup, Vercel env wiring, then DNS approval, and recording readiness/live acceptance reject stale playbooks where Resend or Vercel steps appear after DNS. |
| The durable provider playbook was ordered correctly, but the visible Route plan in static/live control rooms could still show DNS approval before Vercel env wiring. | Static and live Route plan renderers now use the same public order as the Run Record: Capture, Resend API setup, Vercel env wiring, then DNS approval with the complete record set. |
| Provider-route checkpoint and route-card recovery copy could still say Resend setup goes straight to DNS approval, omitting the Vercel env wiring step that public recordings need before final DNS apply. | Resend recovery checkpoints and static/live route cards now say Resend-generated values feed downstream Vercel env wiring before FuseKit surfaces DNS approval with the complete record set. |
| Launch acceptance required provider-route checkpoints to mention Resend before DNS, but could still accept a stale Resend route card that skipped Vercel env wiring in a Vercel-backed demo. | Provider-route checkpoint acceptance now requires Resend route recovery cards to mention downstream Vercel env wiring before DNS approval whenever Vercel routes are present, and blocker recovery copy names the full Resend API -> Vercel env -> DNS approval path. |
| Remote worker detonation could pass through the receipt as a bare `remote_worker` success string, proving that SSH cleanup returned but not proving which VM browser/auth/log/passphrase surfaces were targeted. | Remote worker detonation now returns `fusekit.remote-worker-cleanup.v1` proof with the exact process patterns, disposable VM paths, and `host_machine_state_required=false`; workspace receipts, Run Record recording readiness, and live acceptance fail closed unless that proof is present. |
| The control room could prove remote-worker cleanup internally but still show users only a generic "Remote worker state" card, weakening the public demo's no-trace story. | Static and live control rooms now render a dedicated "Remote worker cleanup proof" card that names the disposable VM path/process coverage and states that host-machine state was not required. |
| Service-level resume calls could still mint a second wake event if an already-resuming gate was retried outside the HTTP control-room guard, weakening evented-resume proof after repeated clicks or worker retries. | `GateService.request_resume` is now idempotent once a gate is already `resume_requested`, returning the original wake event id without mutating gate state or appending duplicate `gate_events.jsonl` proof. |
| Service-level clipboard capture calls could still append duplicate wake events for an already-captured target if a worker retried below the HTTP duplicate-capture guard. | `GateService.mark_captured` now reuses the original `clipboard_captured` wake event id for an already-captured target instead of mutating gate state or appending duplicate evented-resume proof. |
| Placeholder provider-gate or wake-event summaries could satisfy detonation preflight section presence even when provider/status totals drifted or a captured target had no matching `clipboard_captured` wake proof. | Detonation preflight now validates provider-gate totals, provider/status summaries, wake-event counts, duplicate event proof, and captured-target wake consistency before cleanup can proceed. |
| The Run Record recording contract could mark detonation proof ready from VM, public IP, and network cleanup while not explicitly requiring the boot volume deletion flag that the standalone workspace receipt already requires. | Recording-contract detonation readiness now fails closed unless `resource_summary.boot_volume_deleted` is true, keeping the central no-trace proof aligned with the OCI detonation receipt and control-room boot-volume card. |
| The Run Record recording contract could still trust a shaped resource summary without requiring the receipt's explicit `deleted` list to name the same OCI cleanup classes. | Recording-contract detonation readiness now requires the receipt `deleted` list to include the remote worker, compute instance, boot volume, ephemeral public IP, and every standard FuseKit-created network resource before the public demo proof can turn green. |
| The final recording checklist could mark detonation ready from a semantically complete but loose workspace cleanup receipt containing sidecar fields, padded status/list values, or incomplete remote-worker cleanup guidance. | Recording-contract detonation readiness now requires the generated workspace-detonation, resource-summary, and remote-worker cleanup envelopes with trimmed public lists and no sidecar fields before no-trace proof can turn green. |
| Generated Run Records could still embed a redacted but loose `workspace_detonation.json` receipt, preserving sidecar cleanup notes, padded resource rows, duplicate deleted-resource proof, or unsafe failure text until acceptance rejected the survivor artifact. | Run Record generation now canonicalizes workspace-detonation proof first: only the cleanup receipt/resource-summary/remote-worker cleanup contract fields are emitted, public text is trimmed/redacted, cleanup lists are deduped, unsafe failure details remain as redacted blockers, and sidecar receipt rows are dropped. |
| The Run Record recording contract could treat an empty human-action trace as ready even when provider gates, wake events, or human-gate routes existed elsewhere in the same record. | Recording-contract human-action readiness now fails closed unless at least one exact guided action is present whenever the run contains provider gates, gate wake events, or human-owned routes, while still allowing true API-only runs with no human gates. |
| The Run Record recording contract could mark automation-boundary proof ready while skipping the stricter route-count, VNC-scope, detonation-scope, and post-gate list checks used by live acceptance. | Recording-contract automation-boundary readiness now requires the OCI detonation scope, complete allowed VNC gate classes, route counts that match the route list, zero blocked routes, and shaped post-gate automation lists before the public demo proof can turn green. |
| The Run Record recording contract could trust a top-level verifier summary even if individual verifier checks or count buckets still contained failed, pending, repairing, human-gate, or unknown statuses. | Recording-contract verifier readiness now fails closed unless every check is passed, skipped, or explicitly pending-safe and every blocking count bucket is zero, matching the live verifier proof expected before public recording readiness. |
| The Run Record recording contract could mark evidence proof ready for a visual OCI runner without any screenshot evidence, even though screenshots are part of the public proof inventory. | Recording-contract evidence readiness now requires at least one screenshot when the runner profile is the OCI visual-browser profile or names a shared provider browser profile, while nonvisual/API-only runs are not forced to invent screenshot proof. |
| The Run Record recording contract could mark audit proof ready with only generic provider or credential audit entries even when the same record contained DNS approvals, wake events, vault captures, verifier checks, or detonation proof requiring specific audit categories. | Recording-contract audit readiness now derives required audit categories from the Run Record itself and requires matching count-backed entries for credential captures, provider actions, DNS writes, human approvals, and detonation before public demo proof can turn green. |
| FuseKit-owned detonation cleanup accepted caller-provided paths without a workspace root guard, leaving a thin surface where a bug could aim cleanup outside the disposable workspace even though receipts later prove expected OCI resources. | Detonation cleanup now supports a required workspace-root scope for FuseKit-owned cleanup, refuses deleting the root itself, resolves symlinks before deletion, and rejects paths outside the declared workspace while preserving encrypted survivor artifacts. |
| Recording readiness could trust a thinner durable-state check than live acceptance, so a Run Record might claim the worker was replaceable while preserving volatile browser/profile/passphrase state or mismatching the detonation preserve list. | Recording-contract durable-state and worker-replacement checks now require relative non-volatile survivor sources, exact detonation preserve alignment, complete volatile delete coverage, empty runner-profile failures, and resume sources that reference only declared durable state. |
| A Run Record durable-state row could keep the right source id while pointing at a different relative file, making the central proof say `run_state` or `gates` survived even if the canonical survivor filename did not. | Live acceptance and recording-readiness checks now compare every `durable_state.sources` id with its canonical survivor filename from the durable-source contract before the Run Record can count as public proof. |
| Detonation preflight could still trust a Run Record with drifted durable-source paths after live acceptance had learned to reject them, leaving cleanup able to destroy the worker before canonical survivor proof was independently aligned. | Cleanup preflight now validates the same canonical `durable_state.sources` filename contract, including required source ids, literal existence, detonation-preserve coverage, and replacement resume sources, before worker destruction is trusted. |
| Durable-state schema, detonation-scope schema/mode, no-trace wording, and replacement state-owner terms still lived partly in Run Record generation and partly in acceptance/preflight validators. | A shared durable-state proof contract now owns those schema/scope/statement values, and regression coverage proves Run Record generation, live acceptance, automation-boundary proof, and cleanup preflight consume the same authority. |
| Detonation preflight could still trust a thinner worker-replacement contract than live acceptance, leaving cleanup able to proceed without the no-trace OCI statement, exact replacement runner profile, state-owner boundary, volatile-surface coverage, or plaintext-scratch exclusion. | Cleanup preflight now requires the same durable-worker statement, no-trace OCI wording, `oci-visual-browser-x86_64` runner, encrypted-vault-and-Run Record state owner, volatile-surface coverage, and plaintext VM scratch exclusion that recording readiness and live acceptance require. |
| DNS/setup approval clicks could be marked guided in the Run Record based only on gate classification, even if the durable gate text did not actually name the visible `Approve DNS apply` or `Approve setup plan` control. | Human-action trace guidance now requires the exact visible finished/approval control to appear in durable gate instructions before a resume click counts as guided proof for recording readiness. |
| Recording readiness could accept human-action proof with mismatched action counts, a missing trace schema, or actions that did not name the durable gate they came from. | Recording-contract human-action readiness now requires the `fusekit.human-action-trace.v1` schema, count buckets that match the recorded actions, no missing gate ids, and exact visible launcher controls before a demo can be marked recordable. |
| Generated human-action summaries could omit zero-count visible-control buckets or turn diagnostic wake rows into unsupported human actions, making the rehearsal proof depend on validator cleanup instead of the writer's contract. | Run Record generation now emits all known visible-control count buckets, keeps absent controls at zero, and ignores non-visible diagnostic wake rows when building `human_actions`; wake diagnostics stay in wake proof without becoming user-action proof. |
| Recording readiness could accept provider playbook steps without ids, unsafe manual Resend setup instructions, or a Resend browser-guided step that captured the wrong token target. | Recording-contract provider-playbook readiness now rejects missing step ids, unsafe manual Resend/Add Domain instructions, and any Resend browser-guided route that does not capture `RESEND_API_KEY` before API-owned setup. |
| Durable-state detonation proof said worker replacement did not need host-machine state, but the detonation scope itself did not carry the same machine-checkable no-host-state flag. | Run Records now write `host_machine_state_required=false` into `fusekit.detonation-scope.v1`, and both recording readiness and live acceptance reject scopes that omit or contradict it. |
| If a VM clipboard capture stored every required target but the auto-resume write was interrupted, clicking Capture again could acknowledge the already-captured value while leaving the gate visually waiting. | Duplicate final Capture now rereads no clipboard data and rewrites no vault records, but it repairs the missing resume wake/audit proof and moves the gate to `resume_requested` so the worker continues. |
| Resend DNS proof could treat an incomplete provider record as present if the record type existed, allowing an empty DNS value to drift into later DNS proposal checks before failing clearly. | Resend provider parsing now rejects DNS records missing type, host/name, or value, and live acceptance ignores incomplete receipt DNS records so launch readiness cannot pass with hollow Resend DNS proof. |
| Live acceptance could pass the standalone detonation worker-state check without independently requiring the OCI workspace detonation receipt, relying on Run Record validation to catch missing VM/network cleanup proof. | Live detonation acceptance now has a dedicated `detonation.workspace_receipt` check that validates `.fusekit/workspace_detonation.json` for VM, boot volume, ephemeral public IP, network-resource, and remote-worker cleanup proof with launcher-first recovery guidance. |
| Audit proof could satisfy provider-action or DNS-write requirements with generic `audit.jsonl` category counts instead of receipt-indexed setup/DNS evidence. | Run Record acceptance and recording readiness now require provider actions and DNS writes to include `setup_receipt.json` source proof, while human approvals and clipboard captures must stay anchored to `gate_events.jsonl`. |
| The durable-state source list named only early resume artifacts while the detonation preserve contract also promised final proof artifacts such as setup receipt, verifier report, rollback plan, workspace detonation receipt, and the Run Record. | Durable-state sources, worker-replacement resume sources, live acceptance, and recording readiness now use the same explicit source inventory, so public demo proof cannot claim preserved audit/detonation artifacts that are not listed as durable state. |
| The Run Record automation boundary could say post-gate automation existed with any shaped route list, even if it did not match the provider-route ledger that says which API/CLI work FuseKit owns after the human gates. | Live acceptance and recording readiness now require `post_gate_automation.api_or_cli_routes` and `human_gate_routes` to exactly match the automation-boundary routes, so "login/capture, then FuseKit does the rest" is proved by the same route ledger the control room renders. |
| Runner readiness could still be summarized as booleans like `runner_helpers=true` without naming the actual installed helper/browser binaries, leaving a brittle surface for wrong images or partial cloud-init installs. | `runner_readiness.json` now carries a non-secret installed-binary inventory for Python, FuseKit, runner helpers, OpenClaw, Xvfb/x11vnc/fluxbox, the noVNC gateway, and Playwright Chromium; live acceptance and Run Record recording readiness fail unless that inventory is present. |
| Detonation preflight could trust encrypted vault, receipt, verifier, and rollback artifacts while the central Run Record had not yet been written, leaving the main product object outside the survivor proof. | Detonation preflight now requires a shaped `.fusekit/run_record.json`, and local launch writes the current Run Record before cleanup can proceed. |
| Remote OCI artifact validation fetched the Run Record but not the raw durable `run_state.json`, making worker replacement proof depend on a summary instead of the resume state source itself. | Remote artifact fetch and validation now require `.fusekit/run_state.json` before OCI detonation, so the worker can be recreated from durable state instead of VM-local memory. |
| Detonation preflight required the Run Record file but did not independently reject raw callback URLs, bearer text, or token-looking strings inside it before cleanup destroyed the worker. | Detonation preflight now walks the Run Record and fails closed on credential-looking text while allowing explicitly redacted values, keeping no-trace cleanup gated by redacted durable proof. |
| Detonation preflight could still trust standalone public survivors such as the setup receipt, verifier report, rollback metadata, model contract, or replacement drill when only the central Run Record had been scrubbed. | Detonation preflight now scans those JSON survivor artifacts for callback URLs and credential-looking text before cleanup is trusted, so no-trace destruction cannot rely on later acceptance/browser redaction to hide stale provider return data. |
| Detonation preflight scanned the standalone setup receipt for secret-looking strings but did not require the receipt's raw-secret exposure counter to prove zero before destroying worker context. | Cleanup preflight now requires `setup_receipt.json` to carry `raw_secrets_exposed` as literal JSON `0`; missing, string, boolean, float, or nonzero values block worker destruction. |
| Detonation preflight required `audit.jsonl` to exist but did not parse the raw audit survivor before destroying worker state. | Detonation preflight now parses audit JSONL object rows and rejects callback URLs, malformed rows, non-object rows, or credential-looking text before cleanup can proceed. |
| Detonation preflight and live acceptance each carried their own durable-secret regex, creating a drift risk where cleanup and recording readiness could disagree about whether redacted Run Record proof was safe. | Durable public-proof secret detection now lives in one shared `contains_durable_secret_text` helper used by both detonation preflight and live acceptance, with tests proving raw values fail and redacted values pass. |
| OCI workspace detonation could mark the boot volume cleanup green from the `preserve_boot_volume=false` termination request alone, which proved intent but not provider-observed deletion. | OCI detonation now discovers the runner boot volume before termination and only reports `boot_volume` after OCI confirms the volume is gone or terminally deleted; otherwise the workspace receipt records `failed.boot_volume` and public detonation proof stays incomplete. |
| The live control-room provider-gate launcher honored any absolute `FUSEKIT_PROVIDER_BROWSER_PROFILE`, which could accidentally persist provider login/auth state outside FuseKit-owned disposable visual state. | Provider gate opens now reject browser profile paths outside the owned local visual state or OCI `/var/lib/fusekit-runner/visual` tree, so shared provider sessions remain in a detonation-owned surface and arbitrary host profile paths cannot be launched. |
| The provider playbook could be syntactically safe while omitting a core public-demo provider family, or could label an env-named GitHub/Vercel/Cloudflare token capture as generic `provider`, leaving the Run Record unable to prove the complete no-thinking launch path after OCI detonation. | Recording readiness and live acceptance now require provider-playbook coverage for GitHub, Resend, Vercel, and DNS/Cloudflare, and playbook generation labels known token captures with their actual provider family so partial or generic route plans cannot make a demo look recordable. |
| The verifier summary could be internally green while only proving a subset of the providers promised by the public playbook, allowing the control room to look launch-ready without evidence that every wired provider and the live app were actually checked. | Recording readiness now compares verifier providers against the provider families in the Run Record playbook and also requires `live_app` verifier proof, so green checks must cover GitHub, Resend, Vercel, DNS/Cloudflare, and the deployed app before the no-trace demo contract can pass. |
| Live acceptance trusted the Run Record's own verifier-ready claim more than it independently checked whether verifier evidence covered the public playbook, leaving a stale or hand-shaped record able to look complete. | Live acceptance now recomputes verifier coverage from `verifiers.checks` and `provider_playbook.steps`, rejects missing GitHub, Resend, Vercel, DNS/Cloudflare, or `live_app` proof, and keeps fixture reports aligned with the full public demo verifier set. |
| The central Run Record could include the full public provider playbook while its `provider_strategies.providers` ledger proved only a subset of the provider routes, leaving an opening for the UI to promise no-thinking setup without durable route proof for every provider. | Run Record acceptance now compares provider route coverage against the public playbook and rejects missing GitHub, Resend, Vercel, or DNS/Cloudflare route proof; the Resend route checkpoint also has to state the Resend-domain, DNS-approval, and Vercel-env order of operations. |
| OCI workspace detonation receipts proved what FuseKit deleted but only described the encrypted/redacted survivor set in prose, leaving room for the receipt and Run Record preserve contract to drift. | The detonation receipt now writes a machine-readable `resource_summary.survivors` list from the same `DETONATION_PRESERVES` contract as the Run Record, and live acceptance plus recording readiness reject receipts missing that exact survivor set or naming volatile worker/browser/auth state as preserved. |
| Copy-once provider token guidance could name the Capture button but still leave the user unsure whether FuseKit would continue automatically after capture. | Human-gate strategy and checkpoint acceptance now require token-capture guidance to name the exact `Capture ... from VM clipboard` control and explain that FuseKit resumes, continues, retries, verifies, or rechecks after clipboard capture; already-passed gates remain allowed to say no action is needed. |
| The worker replacement contract could say the disposable OCI VM was replaceable from survivor artifacts without a separate drill artifact proving the kill/recreate path actually reopened the control room and resumed a gate or verifier. | Run Records now preserve `worker_replacement_drill.json`, include it in the detonation survivor set, and fail live acceptance/recording readiness unless the drill proves worker destruction, replacement runner readiness, restored durable source labels, reopened control room, resumed gate or verifier, `host_machine_state_required=false`, and no VM-local plaintext reuse. |
| The remote OCI artifact fetch and detonation preflight still did not require `worker_replacement_drill.json`, so a future live run could fail to preserve the new replacement proof before attempting cleanup. | Remote artifact retrieval now includes `worker_replacement_drill.json`, remote bundle validation treats it as a required survivor, and OCI detonation preflight fails closed if requested drill proof is missing, hollow, secret-looking, host-machine-dependent, or reused VM-local plaintext. |
| Launch saves could still rely on later code paths to create `worker_replacement_drill.json`, leaving the remote artifact bundle to fail as missing instead of showing a truthful not-yet-drilled state. | Launch job saves now write a private, redacted pending `worker_replacement_drill.json` from the same shared schema used by preflight and Run Records; a real passed drill is preserved, but pending proof keeps recording readiness and OCI detonation blocked until the worker replacement exercise actually succeeds. |
| A completed remote run could still reach detonation preflight with only pending worker-replacement proof, proving the file existed but not that FuseKit could actually lose the OCI VM and resume. | Safe OCI launches now provision a distinct replacement runner when the drill is pending, detonate the original workspace, restore only durable artifacts onto the replacement, verify the runner profile, reopen the control room, refresh the remote Run Record, and only then allow detonation preflight to see a passed `worker_replacement_drill.json`. |
| Provider playbook cards showed `You:` and `FuseKit:` in the control room, but recording readiness did not fail closed if the durable Run Record or live acceptance artifact omitted those ownership fields. | Run Record recording readiness and live acceptance now require every provider-playbook step to carry matching `actor` and `human_action_required` fields for API, CLI, browser-guided, vault-capture, and follow-me routes, so stale playbooks cannot make responsibility ambiguous after OCI detonation. |
| The CSRF/command-injection route map was code-backed and doc-tested, but the live control room did not show the browser-reachable mutation surface as product proof. | The control-room payload and static/live UI now render the shared route inventory, including the only three state-changing routes and their required control-room header, origin/fetch-site, and owner-only action-token protection. |
| The browser route-security proof could be shown from live code while the central Run Record stayed silent, weakening post-detonation proof that the control room's state-changing surface was bounded. | Run Records now embed `fusekit.control-room-security-surface.v1`, recording readiness requires the protected gate-open, gate-resume, and VM-clipboard Capture routes, and live acceptance rejects stale records whose route counts, no-CORS statement, or owner-only action-token proof drift. |
| Detonation preflight required a central Run Record but did not independently require that the Run Record carried the protected control-room mutation-surface proof before destroying the worker. | Detonation preflight now blocks cleanup unless the survivor Run Record includes the protected gate-open, gate-resume, and VM-clipboard Capture route inventory with action-token/origin/fetch-site and no-CORS proof, so no-trace cleanup cannot erase the worker before that security evidence survives. |
| Detonation preflight checked the protected control-room mutation routes but still trusted hand-edited route counters, so a float such as `3.0` could equal the three state-changing routes and a missing total route count could go unnoticed. | Cleanup preflight now requires integer `route_count` and `state_changing_route_count` values that match the route inventory and unique protected mutation routes before worker destruction is trusted. |
| The final recording contract could mark `control_room_security` ready from padded route labels or sidecar security notes that the generated route-surface writer never emits. | Recording readiness now rejects loose control-room security proof, including sidecar surface/route fields, padded route and state-changing-route labels, padded methods, padded protection strings, and padded security statements before the control room can call the mutation surface recordable. |
| Live acceptance and cleanup preflight still accepted looser control-room security rows than the final recording checklist, leaving route sidecars or padded method/protection text to fail late. | Live acceptance and cleanup preflight now reject sidecar control-room security fields and padded route, method, protection, or state-changing route text before protected mutation proof can support launch readiness or worker cleanup. |
| The route-surface writer, Run Record recording checks, live acceptance, and cleanup preflight depended on separate control-room security vocabularies, so protected mutation routes or required POST-protection wording could drift across launch gates. | A shared control-room security contract now owns the route inventory, public envelope, route fields, protected mutation routes, POST-protection terms, unknown-route protection, and statement terms for all browser mutation proof surfaces. |
| Rehearsal notes could say every human action was compared against control-room instructions, but the central Run Record had no separate proof section for that launch-recording review. | Run Records now include `fusekit.rehearsal-review.v1`, recording readiness and live acceptance require every recorded human action to be compared and matched to exact visible controls, and the control room shows whether the rehearsal stayed inside launcher/VNC instructions without host-browser, terminal, or side-channel steps. |
| The final recording-contract panel could mark `rehearsal_review` pending with the same generic proof-copy used for every checklist item, leaving users unsure what clean rehearsal evidence was missing. | Static and live recording-contract cards now give proof-specific recovery text; the rehearsal-review card requires every human action to match visible launcher/VNC instructions with no host browser, terminal, side channel, or user interpretation. |
| A failed Run Record rehearsal-review check could still become a generic Run Record launch blocker, hiding the exact public-recording recovery action from the acceptance report and refreshed control room. | Acceptance blockers and static/live control-room check fallbacks now classify `rehearsal_review` failures as Rehearsal review blockers and tell users to keep the live room open until every human action matches visible launcher/VNC instructions with no side channel or interpretation. |
| A failed worker-replacement recording check could still become a generic Run Record launch blocker, even though the public no-trace story depends on a specific destroy/restore/reopen/resume drill. | Acceptance blockers and static/live control-room check fallbacks now classify `worker_replacement` failures as Worker replacement blockers and keep the user in the live room while FuseKit destroys the original OCI worker, restores only encrypted/redacted durable sources, reopens the control room, and resumes a gate or verifier without host-machine state. |
| A failed standalone workspace-detonation receipt check could fall back to generic launch-evidence copy in static or live control-room recovery, even though the missing proof is the no-trace OCI cleanup receipt. | Acceptance blockers and static/live control-room check fallbacks now classify `detonation.workspace_receipt` failures as Workspace detonation blockers and keep the user in the launcher until the receipt proves VM, boot-volume, public-IP, network-resource, and remote-worker cleanup. |
| A failed prepared-runner proof could surface as generic launch evidence even though the user-facing recovery is to keep the OCI room alive until the VM/browser contract is proven. | Acceptance blockers and static/live control-room check fallbacks now classify `runner_readiness.prepared` failures as Runner readiness blockers and name the required x86_64, OpenClaw, Playwright Chromium, noVNC, shared provider-browser profile, helper-binary, and encrypted-vault-access proof. |
| A failed visual-session safety check could surface as generic launch evidence even though the recovery is to refresh the noVNC/control-room metadata before trusting the provider browser handoff. | Acceptance blockers and static/live control-room check fallbacks now classify `visual_state.safe` failures as Visual session blockers and name the safe noVNC/control-room URL and noVNC password-metadata proof. |
| Live acceptance and cleanup preflight still carried separate visual-state vocabularies, so VM/browser handoff fields or guidance notes could drift across launch gates. | A shared visual-state contract now owns the generated `visual.json` field set, visual transport fields, runner/status/display values, and fixed VM-browser guidance notes. |
| A failed provider-pack validation check could surface as generic launch evidence even though deterministic provider routes cannot be trusted until the manifest's capability packs validate. | Acceptance blockers and static/live control-room check fallbacks now classify `provider_packs.validated` failures as Provider packs blockers and keep setup, route planning, and verification paused until FuseKit loads and validates the manifest's provider packs. |
| Failed verifier or rollback check rows could surface as generic launch evidence in stale acceptance reports, even though public demo recovery depends on rerunning provider verification or writing rollback actions before launch. | Static/live control-room check fallbacks now classify `verification_report.*` failures as Verification blockers and `rollback_metadata.*` failures as Rollback blockers, preserving launcher-first recovery for provider checks, pending-safe DNS/deploy waits, and rollback coverage. |
| Older acceptance reports that only carried failed check rows for gates, provider routes, vault, receipts, worker detonation, or leak scan could still fall back to generic launch-evidence repair copy. | Static/live control-room check fallbacks now classify those core launch gates as Human gates, Provider order/playbook/routes, Vault, Receipt, Detonation, or Security blockers with launcher-first recovery actions. |
| Provider gate cards were redacted for browser display, but the durable Run Record could still preserve raw callback URLs or token-shaped gate instructions under `provider_gates.records`. | Run Record gate ingestion now redacts provider/callback URLs and token-shaped guidance fields before building the central proof object, while preserving gate ids, provider names, statuses, env targets, and wake metadata for acceptance review. |
| Generated Run Record provider-gate summaries could still derive counts from padded, duplicate, or malformed durable gate rows that the stricter recording and cleanup checks would reject later. | Run Record gate ingestion now canonicalizes provider-gate rows before counting them: unusable rows are skipped, duplicate gate ids collapse, public text is trimmed/redacted, and live acceptance enforces the same generated-field-only gate shape. |
| Generated wake-event summaries could still count padded, duplicate, or malformed event rows before recording readiness rejected them, making evented resume proof look larger than the usable control-room action history. | Run Record wake-event ingestion now canonicalizes event rows before counting: duplicate event ids/proofs collapse, token/password/secret marker text is skipped, target counts and timestamps normalize to generated literals, and the human-action trace consumes the same safe event stream. |
| Gate ids, provider names, wake-event fields, or approval summaries are normally structural, but a malformed provider gate could still poison those fields with token-shaped callback text. | Run Record gate and wake-event ingestion now public-redacts structural strings too, keeping normal ids/providers readable while scrubbing token-shaped values before central proof is written. |
| The final recording contract could mark `provider_gates` ready from padded durable gate rows or sidecar provider-gate notes that the generated gate summary never emits. | Recording readiness now rejects loose provider-gate proof, including sidecar summary/record fields, padded ids/providers/statuses/targets, padded captured targets, and padded provider summaries before provider-gate proof can make the run recordable. |
| Detonation preflight could still trust looser provider-gate proof than the final recording contract, allowing worker cleanup after sidecar gate fields, padded provider/status/target values, coerced totals, or malformed attempt/timestamp rows. | Cleanup preflight now applies the same exact provider-gate bar before worker destruction: generated-field-only summaries and rows, literal integer totals/status counts, trimmed provider/status/target/captured-target/follow-step values, and non-negative attempt/timestamp proof. |
| A hand-edited approval list could claim a setup/DNS approval without matching the durable gate state or the evented `resume_requested` wake proof. | Live Run Record acceptance now validates approval shape, public-safe text, durable gate id/status drift, and matching resume wake events before approval proof can support public readiness. |
| Detonation preflight could still trust a non-empty approval list before cleanup even if the approval row was anonymous, duplicated, unsafe, or drifted from durable gate and wake-event proof. | Cleanup preflight now validates approval id/provider/status/reason/updated_at fields, unique approval ids, public-safe text, provider-gate id/status matches, and `resume_requested` wake-event anchors before worker destruction can proceed. |
| A job artifact outside `.fusekit` could be listed with an absolute host path and then be mistaken for evidence in the Run Record inventory. | Run Record artifact paths are now serialized as public labels, and evidence inventory only includes files that resolve inside the `.fusekit` artifact root, so outside worker/host logs cannot become public post-detonation proof. |
| A stale or hand-edited Run Record artifact row could still carry an absolute or tokenized path, traversal, or anonymous label even though evidence inventory stayed safe. | Live acceptance now validates artifact rows for named public-safe labels, public-relative non-traversing paths, no credential query text, and boolean existence flags before launch readiness can pass. |
| Generated artifact rows could still inherit padded artifact names, duplicate canonical names/paths, or credential-query labels from runner job artifacts before evidence inventory filtered unsafe files. | Run Record writing now trims/redacts artifact names, skips secret-looking or credential-query artifact labels and paths, and collapses duplicate generated artifact names or paths before deriving evidence inventory. |
| Run Record durable-state summaries could treat truthy artifact existence values as present survivor sources before the artifact-list validator rejected the malformed row. | Durable-state source summaries now consider artifact proof present only when `exists` is literal JSON `true`, so malformed rows cannot inflate encrypted vault or resume-source survival proof. |
| Hand-edited Run Record counters could use JSON booleans such as `true` or `false`, which Python treated like `1` or `0` in some proof comparisons. | Live acceptance now treats public proof counters as plain integers only, so gate totals, wake-event counts, evidence counts, human-action/rehearsal counts, automation-boundary counts, verifier counts, and audit counts cannot be satisfied by booleans. |
| Live acceptance, Run Record, and detonation preflight counters still truncated floats, so a value like `1.9` could satisfy a one-row proof count. | Counter parsing now accepts only integers or integer strings, rejecting floats before durable-state, gate, wake-event, verifier, evidence, audit, model-lane, automation-boundary, human-action, or rehearsal counts can support launch or cleanup proof. |
| The central Run Record embedded full provider-strategy/playbook artifacts, so a stale or hand-edited strategy file could preserve callback URLs or token-shaped guidance even when acceptance would later reject it. | Run Record provider-strategy ingestion now redacts callback URLs and token-shaped strings before building `provider_strategies`, `provider_playbook`, and `automation_boundary`, while preserving the route/status signatures acceptance compares to `provider_strategies.json`. |
| The central Run Record embedded the full verifier report, so verifier repair details could keep callback URLs or token-shaped messages even though recording readiness only needs provider/check/status proof. | Run Record verification ingestion now redacts callback URLs and token-shaped strings before writing `verification`, while preserving the `verifiers` provider/check/status signature that live acceptance compares to `verification_report.json`. |
| Live acceptance rejected token-shaped Run Record text but a bare provider callback URL without a token query could still survive in an unexpected central Run Record field. | Run Record public-safety acceptance now rejects callback URLs anywhere in `run_record.json`, matching detonation preflight and standalone survivor checks. |
| The central Run Record embedded `llm_contract.json` exactly, so a malformed model contract could carry callback URLs or token-shaped recovery text into the public record, while a raw-secret survivor artifact could still exist outside the Run Record. | Run Record LLM-contract ingestion now redacts callback URLs and token-shaped strings before writing `llm_contract` and `model_inference`, and live acceptance rejects standalone `llm_contract.json` artifacts that contain credential-looking text. |
| A Run Record could claim a ready model/inference status while leaving `llm_contract.lanes` empty, malformed, or inconsistent with `model_inference.lane_count`, weakening public review of how inference is authorized. | Live acceptance now requires concrete LLM lane records with ids, labels, boolean availability/action flags, public-safe descriptions, a matching `default_lane`, and an accurate model summary lane count before launch readiness can pass. |
| Detonation preflight could trust the same hollow `llm_contract.lanes` proof that live acceptance rejected, allowing cleanup before the model/inference lane was reviewable. | Cleanup preflight now requires shaped LLM auth-lane rows, a matching default lane, required encrypted-auth lane coverage, and a `model_inference.lane_count` matching `llm_contract.lanes` before worker destruction is trusted. |
| The model summary could still drift from the LLM contract on base URL, required flags, default lane, or other public fields while provider/model/status stayed aligned. | Live acceptance and detonation preflight now compare every public model-summary field derived from the LLM contract before inference proof can support launch readiness or cleanup trust. |
| The final recording checklist could still mark `model_inference` ready when the model summary drifted from the LLM contract on base URL, required flags, default lane, or boolean flag types. | Recording readiness now uses the same public model-summary drift fields and literal boolean requirements as live acceptance and detonation preflight before `recording_contract.checks.model_inference` can pass. |
| The recording checklist could still accept sidecar or whitespace-padded `model_inference` summary fields that live acceptance and cleanup preflight would reject. | Recording readiness now requires exact generated model-summary fields with already-trimmed public text before model inference can be marked recordable. |
| The recording checklist could still accept a paired model summary and LLM contract that both omitted `base_url`, or a contract with stringy `required` flags and no generated `record_id`. | Recording readiness now requires the LLM contract's generated identity fields and literal capability booleans before model inference can be marked recordable. |
| The recording checklist could still accept sidecar LLM contract notes or a partial security block that live acceptance and cleanup preflight would reject. | Recording readiness now rejects unexpected contract, security, and lane fields and requires the full generated public security block before model inference can be marked recordable. |
| The recording checklist could still accept callback URLs or token-shaped public text in model/LLM contract fields even though live acceptance and cleanup preflight rejected those strings. | Recording readiness now rejects callback URLs and credential-looking model summary, contract, security, and lane-description text before inference proof can be marked recordable. |
| The recording checklist could still normalize padded LLM lane ids or labels before counting them, and did not reject credential-looking lane labels. | Recording readiness now requires exact public-safe LLM lane ids and labels before lane proof can be marked recordable. |
| Truthy strings or booleans could stand in for model/inference required flags or lane counts, making the public inference contract look complete without literal generated types. | Live acceptance and detonation preflight now require literal boolean `required`/`can_proceed_without_api_key` flags and integer lane counts before inference proof can support launch readiness or cleanup trust. |
| Padded or duplicated LLM lane ids could normalize into valid-looking auth options, letting `model_inference.lane_count` overstate distinct model authorization choices. | Live acceptance and detonation preflight now require trimmed lane ids, labels, and descriptions plus unique lane ids before lane proof can support launch readiness or cleanup trust. |
| A Run Record could claim `api_key_encrypted` or `openclaw_profile_encrypted` while the status-proving lane or selected default lane was still unavailable or waiting for user action. | Live acceptance and detonation preflight now require the default lane and encrypted-status lane to be available and ready without user action before inference proof can support launch readiness or cleanup trust. |
| Generated contracts could keep `openclaw-openai` as the default lane when only the API-key record was encrypted, and recording readiness did not independently reject that selected-lane mismatch. | LLM contract generation now defaults ready API-key proof to `api-key` unless encrypted OpenClaw profile proof exists, and recording readiness requires the default/status lanes to be available without user action. |
| Model summary or contract-security text could preserve padding, callback URLs, or credential-looking repair text while still containing the required encrypted-vault phrases. | Live acceptance and detonation preflight now require trimmed public-safe model summary, LLM contract, and contract-security strings before inference proof can support launch readiness or cleanup trust. |
| Model/inference proof could carry sidecar notes in the summary, contract, security block, or lane rows while the required generated fields still looked ready. | Live acceptance and detonation preflight now require exact generated model/inference fields only before auth-lane proof can support launch readiness or cleanup trust. |
| The recording contract could mark `model_inference` ready from the summary alone even when the paired `llm_contract` lanes were missing, empty, or inconsistent. | Recording readiness now validates the same paired model/LLM-contract proof, including shaped lanes, encrypted auth coverage, denied raw-secret export, encrypted-vault storage, matching fields, and matching lane counts before the section can turn green. |
| The central Run Record embedded `workspace_detonation.json` exactly, so cleanup failure/reason text could preserve callback URLs or token-shaped details while a raw-secret detonation survivor remained. | Run Record detonation-receipt ingestion now redacts callback URLs and token-shaped strings before writing `detonation.workspace_receipt`, and live acceptance rejects standalone `workspace_detonation.json` artifacts containing credential-looking text. |
| Receipt-derived Run Record audit entries trusted `setup_receipt.json` action/status strings after receipt details were already omitted, so a stale or malformed receipt could still preserve callback URLs or token-shaped action text in the central audit trail. | Run Record setup-receipt ingestion now redacts callback URLs and token-shaped strings before deriving audit entries, preserving receipt indexes and action categories while live acceptance rejects standalone `setup_receipt.json` artifacts containing credential-looking text. |
| Standalone survivor artifacts could preserve a provider callback URL without obvious token-shaped query text, and redacted Run Record signature comparison or direct coverage checks could still pass because public display normalizes the URL. | Live acceptance now rejects unredacted callback URLs directly in `provider_strategies.json`, `verification_report.json`, `rollback_plan.json`, `setup_receipt.json`, `llm_contract.json`, and `workspace_detonation.json` before signature or coverage checks, so public launch proof cannot depend on URL redaction hiding stale survivor text. |
| Durable gate and wake-event files could be safe after browser/Run Record redaction while the raw `gates.json` or `gate_events.jsonl` survivor still contained a provider callback URL. | Live acceptance now fails `gates.json` and row-level `gate_events.jsonl` public-safety checks on callback URLs or credential-looking text before gate resolution, audit proof, or Run Record wake consistency can pass. |
| Live acceptance could compare `gate_events.jsonl` wake-event signatures while ignoring sidecar raw survivor fields that cleanup preflight rejected later. | Live acceptance now validates raw gate-event rows against the generated wake-event shape before Run Record signature comparison, so sidecar wake proof, padded fields, malformed target counts, or bad timestamps cannot support launch readiness. |
| A provider capability pack could pass schema validation with a callback URL in handoff or guidance text, then still make the aggregate provider-pack readiness row look green if another pack validated. | Acceptance now runs public-safety checks on each validated provider pack snapshot and fails the aggregate `provider_packs.validated` gate if any required pack contains callback URLs or credential-looking text. |
| `visual.json` needed to preserve safe noVNC/control-room URLs, but extra metadata fields could still carry callback URLs or token-shaped provider return text after browser payload redaction. | Live acceptance now applies public-safety checks to extra visual survivor fields and rejects callback-shaped noVNC/control-room URLs while preserving the validated VM iframe/control-room transport contract. |
| A safe-looking `visual.json` could still carry sidecar metadata, padded generated fields, wrong display proof, or drifted visual guidance notes while the sanitized noVNC URLs looked valid. | Live acceptance now requires the raw visual survivor to match FuseKit's generated launch-proof envelope exactly, while keeping specialized validation for the noVNC/control-room transport fields. |
| Cleanup preflight resolved inventoried visual evidence paths but could still trust a placeholder `visual.json` file before worker destruction. | Detonation preflight now requires `visual.json` and validates the generated noVNC/control-room launch-proof envelope before cleanup trust can pass. |
| Worker-replacement drill proof could include extra restored source labels beyond the durable replacement contract, leaving room for hidden VM-local or ad hoc state to appear compatible with the no-host-state story. | Run Record worker-replacement proof is now recursively redacted before embedding, and recording readiness, live acceptance, and detonation preflight require `restored_from` to exactly match the durable replacement source ids. |
| The central Run Record copied runner-readiness profile/cache/binary paths even though public review only needs to prove the shared provider-browser contract and helper inventory. | Run Record runner-readiness summaries now replace VM-local profile, Playwright cache, and installed-binary paths with public labels while live acceptance still compares them against the raw `runner_readiness.json` VM proof. |
| Runner-readiness acceptance snapshots and raw readiness survivors could still preserve VM-local paths or stale provider callback text even though the Run Record's runner summary was public-safe. | Live acceptance now snapshots the public runner-readiness summary and rejects callback URLs or credential-looking text in raw `runner_readiness.json` before `runner_readiness.prepared` can pass. |
| The central Run Record could preserve the absolute app workspace path in its base identity fields, making the main public proof object reveal host or VM layout even after artifact paths were normalized. | Run Record writing now stores `app_path` as a public path label, and live acceptance rejects absolute app paths in central records before launch readiness can pass. |
| Stale or hand-edited `run_state.json` notes could carry callback URLs or token-shaped text into the central Run Record even though normal `LaunchRunState.add_note()` writes redacted notes. | Run Record ingestion now public-redacts the embedded launch state contract before writing `state`, preserving readiness booleans while stripping unsafe run-state notes from public proof. |
| Retrieved OCI bundles could prove durable `job.json`, `run_state.json`, `checkpoints.json`, or `worker_replacement_drill.json` existed without checking whether those standalone survivor files still contained callback URLs or token-shaped recovery text. | Live remote artifact inventory now parses those non-secret durable survivors and fails `remote_artifacts.loaded` on callback URLs or credential-looking text before the bundle can support public launch readiness. |
| Retrieved OCI bundles could carry stringy launch-state booleans, padded run-state notes, unknown `missing_for_detonation` entries, or sidecar run-state fields while still proving `run_state.json` existed. | Live remote artifact inventory now rejects loose `run_state.json` survivors before remote artifacts can support launch readiness: launch flags and `ready_to_detonate` must be literal booleans, timestamps numeric, and notes/missing lists trimmed generated fields. |
| Retrieved OCI bundles could carry padded `job.json` step/checkpoint text or sidecar `checkpoints.json` resume notes while still proving remote artifacts were loaded. | Live remote artifact inventory now rejects loose job/checkpoint survivor envelopes before artifact bundles can support launch readiness: job, step, checkpoint, artifact, and checkpoint-file rows must use generated fields only and trimmed public text. |
| Runner job serialization and live remote artifact validation still carried separate job/checkpoint/run-state survivor vocabulary, so retrieved OCI proof could drift from the writer that generated it. | A shared remote-survivor contract now owns job, step, checkpoint, checkpoint-file, run-state, and public survivor label fields across `JobState` serialization and live acceptance. |
| Retrieved OCI bundles could carry a pending or hand-edited `worker_replacement_drill.json` with padded restore-source labels, duplicate restored sources, sidecar drill notes, or host-machine-dependent proof while still proving the file existed. | Live remote artifact inventory now rejects loose worker-replacement drill survivors before remote artifacts can support public recording readiness: the raw drill must be passed, exact-field, trimmed, duplicate-free, restored from the durable source ids, and prove no host-machine state or VM-local plaintext reuse. |
| The remote app upload could still carry provider-local CLI state such as Cloudflare `.dev.vars`, `.wrangler`, Netlify, Stripe, Supabase, or OpenAI session folders into the disposable OCI worker. | The OCI upload filter now excludes common provider CLI state and local secret files before packaging the app, while tests inspect the uploaded tarball to prove those files never enter the worker. |
| The remote app upload could also carry local cloud or SSH credential folders such as `.ssh`, `.aws`, `.gcloud`, `.azure`, `.oci`, or `.cloudflared` if they were accidentally present in the app tree. | OCI app archives now exclude those credential folders before upload, and tarball inspection tests prove they never enter the disposable worker. |
| The remote app upload could still include local infra/deploy cache credentials from tools such as Kubernetes, Docker, Terraform, Pulumi, Doppler, Fly, Railway, Render, Sentry, `.netrc`, or direnv. | OCI app archives now exclude those hidden credential/cache paths before upload, and tarball inspection tests prove they never enter the disposable worker. |
| The remote app upload could include package-manager or language registry credentials such as npm/yarn/pnpm auth files, Cargo/Gem/Maven credentials, or Python package index login config. | OCI app archives now exclude those package and language credential files before upload, while preserving source-like tool config that is not credential material. |
| The remote app upload could preserve symlink entries whose names looked safe but pointed at host-local provider sessions or secret files outside the app tree. | OCI app archives now skip symlinks entirely, and remote upload tests assert no symlink or hardlink entries are present in the tarball sent to the disposable worker. |
| Worker replacement could package a required durable survivor path that was actually a symlink, letting restore proof depend on host-local state instead of encrypted/redacted artifacts. | Worker-replacement archives now reject symlinked required survivor files and skip linked optional files before restoring state into a replacement OCI worker. |
| Run Record vault metadata trusted the caller-provided vault index, so an accidental `value`, passphrase, private-key, or token-shaped metadata field could become part of the central credential-capture proof. | Run Record vault summaries now recursively drop raw value-style fields and public-redact remaining metadata before writing `vault.records` or deriving vault audit proof; live acceptance rejects stale or hand-edited records that reintroduce those fields. |
| Detonation preflight could trust a non-empty Run Record `vault` section even when it lacked count-backed id/kind/provider/label metadata or reintroduced raw value-style fields. | Cleanup preflight now validates exact `vault.record_count`, unique vault ids with trimmed values, required public metadata fields, generated-field-only vault rows, credential-looking text rejection, and recursive raw value/passphrase/private-key/password field exclusion before worker destruction can proceed. |
| A corrupt or hand-edited vault bundle could pass the narrow plaintext-marker check and then crash live acceptance during unlock. | Vault acceptance now fails closed with redacted `vault.ciphertext_only` or `vault.unlock` checks: bundle text must have no credential-looking markers, valid bundles must unlock with the supplied passphrase, and modified bundles become launch blockers instead of harness crashes. |
| Detonation preflight treated the vault survivor as encrypted based on file presence, while the general leak scan skipped `.vault.json` files by design. | Detonation preflight now reads the vault bundle text and rejects plaintext or credential-looking markers before worker cleanup can destroy the only context that would explain a bad bundle. |
| A malformed or stale `audit.jsonl` event name could include a provider callback URL or token-shaped text and then survive as a central Run Record audit action or summary. | Run Record audit-log ingestion now redacts event names before writing `audit_trail.entries[].action` or deriving their public summaries, while still preserving category/source/index proof. |
| Live acceptance treated the raw audit survivor as proof when `audit.jsonl` existed, even if the file was malformed or carried callback URLs in non-event fields. | The audit existence check now parses JSONL object rows and rejects callback URLs or credential-looking text before the redacted audit log can satisfy launch readiness. |
| Raw `audit.jsonl` rows could carry padded event names, sidecar top-level fields, or non-object data while still looking like parseable audit proof. | Live acceptance and cleanup preflight now require generated top-level audit row fields only (`event`, optional `data`, optional `ts`), trimmed event names, object-shaped data payloads, and string timestamps before raw audit logs can support launch readiness or worker cleanup. |
| Live acceptance and cleanup preflight still carried separate raw audit row vocabularies, so standalone audit fields could drift across launch gates. | A shared audit-log survivor contract now owns raw audit row fields and generated event/data/timestamp field names. |
| A stale acceptance report could carry callback URLs or token-shaped blocker/error text into the central Run Record even though the control-room reader redacted reports at display time. | Run Record acceptance summaries now public-redact blockers and errors before writing `acceptance`, preserving readiness booleans without turning the central proof object into a stale report dump. |
| Run Record writing could still copy loose acceptance-report recovery fields, leaving central proof dependent on later validators to reject padding, duplicate missing proof, or blocker sidecar fields. | Run Record acceptance summaries now canonicalize embedded proof at write time: missing proof is trimmed and de-duplicated, blockers are narrowed to recovery-card fields, missing items get fallback recovery cards, and error text is trimmed after redaction. |
| Set-based cleanup checks could let duplicate route, cleanup-target, or restored-source rows satisfy detonation preflight even though the public demo needs one unambiguous proof row per cleanup boundary. | Detonation preflight and CLI workspace-detonation readiness now reject duplicate control-room mutation routes, remote-worker process/path cleanup rows, and worker-replacement `restored_from` labels before worker destruction or kill/recreate proof can be trusted. |
| Control-room recovery copy could correctly refuse to call a live run recordable while still omitting the strict command-line gate that public walkthrough docs require. | Static and live control-room acceptance states now direct operators to complete or rerun live acceptance with `--require-recording`, and README acceptance evidence names the same strict OCI remote-artifact command. |
| Detonation preflight could see a non-empty `recording_contract` and still allow cleanup even if non-detonation recording sections such as provider playbooks or rehearsal review were missing. | Preflight now treats `detonation` as the explicit only allowed pre-cleanup recording-contract blocker; every other recording section must be present and true before disposable worker state can be destroyed. |
| A hand-edited recording contract could carry sidecar top-level fields or extra checklist flags while the required generated checks still looked green. | Live acceptance and detonation preflight now require exact generated recording-contract fields and exact checklist names before recordability proof can support public launch or cleanup. |
| A hand-shaped recording checklist could mark provider playbooks, runner proof, verifiers, evidence, or rehearsal review true while omitting the matching Run Record section. | Preflight now checks section drift: each true non-detonation recording-contract check must have its matching Run Record proof section present before cleanup can proceed. |
| Live acceptance could still trust green `recording_contract` section booleans even when the matching Run Record proof section was empty, such as an empty timeline or artifact list. | Live acceptance now applies the same section-drift guard as cleanup preflight: every true recording-contract check must have matching non-empty Run Record proof before public readiness can pass. |
| Recording-contract section drift checks could treat a scalar placeholder such as `"present"` as proof that a required Run Record section existed. | Detonation preflight now accepts section proof only from non-empty objects or lists, so string/number placeholders cannot support worker cleanup. |
| Detonation preflight could treat `recording_contract.checks.worker_replacement=true` as satisfied by durable replacement intent while the central Run Record omitted or carried pending `worker_replacement_drill` proof. | Cleanup preflight now requires a non-empty embedded Run Record `worker_replacement_drill` section and validates that it passed before worker destruction can proceed. |
| Cleanup preflight could validate standalone or embedded worker-replacement drill proof semantically while still accepting sidecar fields or padded generated text that live remote artifact validation would reject. | The shared worker-replacement drill validator now rejects sidecar fields, padded schema/status/restored-source/statement text, and loose restore-source proof before kill/recreate evidence can support worker cleanup. |
| Live Run Record acceptance could still validate embedded worker-replacement drill proof semantically while accepting sidecar fields or padded generated text that cleanup preflight and remote survivor validation rejected. | Live acceptance now rejects sidecar or padded embedded `worker_replacement_drill` proof before kill/recreate evidence can support public launch or recording trust. |
| Final Run Record recording readiness could publish a clean worker-replacement summary while hiding malformed raw drill sidecars or omitted pending-reason text from the green checklist. | Recording readiness now fails closed on raw or embedded worker-replacement drill sidecar fields, padded generated text, callback URLs, credential-looking text, and loose restore-source rows before the public proof object can turn green. |
| Remote worker-replacement survivor validation still carried its own drill field list even though the Run Record writer and embedded acceptance path used the shared worker-replacement contract. | Live acceptance now consumes the immutable shared drill key set for remote `worker_replacement_drill.json` proof as well, with regression coverage proving Run Record and acceptance share the same authority. |
| Detonation preflight could trust a central Run Record with only an id, even if status, runner, or public app identity was missing or `app_path` leaked an absolute host/VM path. | Cleanup preflight now requires id/status/runner plus a public-label app path before worker destruction can trust the central Run Record. |
| Detonation preflight could trust a Run Record with missing or truthy launch-state proof and a stale embedded `verification` object even when the standalone verifier report had changed. | Cleanup preflight now requires `state.detonation_safe` to be literal true, `state.workspace_detonated` to be an explicit boolean, and the embedded Run Record `verification` checks to match `verification_report.json` before worker destruction can proceed. |
| Live acceptance could compare embedded Run Record verification signatures while ignoring malformed extra check rows that did not affect provider/check/status matching. | Live acceptance now validates the embedded `verification` object with the verifier failure rules before comparing it to `verification_report.json`, so ignored placeholders cannot ride along in the central proof object. |
| Detonation preflight could trust a hollow Run Record `detonation` object before cleanup, even if it omitted `preflight_safe` or used stringy workspace-detonation proof. | Cleanup preflight now requires `detonation.preflight_safe` to be literal true and `detonation.workspace_detonated` to be an explicit boolean before disposable worker destruction can proceed. |
| The central Run Record `detonation` section could carry sidecar top-level fields while the required booleans and workspace receipt still looked valid. | Recording readiness, live acceptance, and cleanup preflight now reject unexpected top-level detonation fields before no-trace proof can support public recording or worker destruction. |
| Embedded acceptance blockers could be scalar placeholders or half-shaped objects, so the central Run Record could claim a recovery map existed without the control-room card fields users need. | Live acceptance and detonation preflight now require every embedded acceptance blocker to carry non-empty `item`, `category`, and `next_action` strings, with optional `detail` also string-shaped. |
| Duplicate or whitespace-padded duplicate embedded acceptance blocker cards could inflate the central recovery map even though generated reports de-duplicate blockers by item. | Live acceptance and detonation preflight now reject duplicate embedded acceptance blocker `item` values after trimming before launch readiness or cleanup trust can pass. |
| Embedded acceptance blocker cards could carry extra sidecar fields that the generated report and control-room reader never emit. | Live acceptance and detonation preflight now reject unexpected embedded blocker fields, keeping central recovery cards narrowed to `item`, `category`, `next_action`, and optional `detail`. |
| Run Record generation, live acceptance, and cleanup preflight still referenced separate acceptance-summary and unresolved-error vocabularies, so launch-recovery cards could drift between generated proof and readiness gates. | A shared acceptance-summary contract now owns the embedded acceptance envelope, readiness booleans, blocker-card fields, and unresolved-error fields for all public recovery proof surfaces. |
| A hand-edited embedded `acceptance` summary could carry top-level sidecar notes that the generated report and Run Record writer never emit. | Live acceptance and detonation preflight now reject unexpected top-level acceptance fields, keeping central readiness proof narrowed to the generated summary envelope. |
| A hand-edited Run Record embedded `acceptance` summary could carry stringy ready flags, malformed blockers, or object-shaped errors while the generated writer would only emit booleans, a list, and a string. | Live acceptance and detonation preflight now treat an empty `acceptance` object as pre-report state, require the section to exist, and validate every non-empty embedded acceptance summary before public launch readiness or cleanup trust can pass. |
| A whitespace-padded embedded acceptance `mode` such as `" live "` could be stripped into live proof even though generated reports emit exact mode strings. | Live acceptance and detonation preflight now require exact `live` or `rehearsal` embedded acceptance mode strings before central proof can support launch readiness or cleanup trust. |
| A stale but well-shaped embedded `acceptance` summary could claim `public_launch_ready` or `recording_ready` while launch proof was false, blockers remained, errors existed, or the Run Record recording contract disagreed. | Live acceptance and detonation preflight now require acceptance ready flags to be internally consistent, blocker/error-free when ready, and aligned with `recording_contract.recording_ready` before central proof can support public launch readiness or cleanup trust. |
| The central Run Record acceptance summary could preserve ready booleans without preserving the report mode, leaving a hand-edited rehearsal-shaped summary able to mimic live public proof. | Run Record writing now stores acceptance `mode`, and live acceptance plus detonation preflight require populated summaries to declare `live` before `public_launch_ready` or `recording_ready` can support public launch proof. |
| The central Run Record acceptance summary dropped `recording_proof_ready`, so the final `recording_ready` flag could survive without the intermediate proof flag named by the public report contract. | Run Record writing now preserves `recording_proof_ready`, and live acceptance plus detonation preflight require it to be boolean, required by `recording_ready`, and aligned with `recording_contract.recording_ready`. |
| The central Run Record acceptance summary dropped the report's `missing[]` proof list, so a hand-edited summary could hide named missing evidence while keeping ready-looking flags. | Run Record writing now preserves redacted `missing` proof strings, and live acceptance plus detonation preflight require the list to be shaped and empty before any launch or recording ready flag can support public proof. |
| Embedded acceptance `missing[]` proof could contain blank or duplicate entries, inflating the central launch-evidence list without adding a distinct recovery item. | Live acceptance and detonation preflight now require every embedded missing-proof string to be non-empty and unique before central proof can support launch readiness or cleanup trust. |
| Embedded acceptance recovery strings could carry surrounding whitespace, making public proof look different from the generated report while still matching after validator trimming. | Live acceptance and detonation preflight now require embedded `missing[]` strings and blocker-card text fields to be already trimmed before central proof can support launch readiness or cleanup trust. |
| Embedded acceptance blocker `detail` could be present but empty, even though the Run Record writer omits empty details from canonical recovery cards. | Live acceptance and detonation preflight now reject empty embedded blocker `detail` fields; if present, detail must be useful trimmed text. |
| Embedded acceptance `missing[]` proof could name missing evidence without a matching recovery card in `blockers[]`, leaving public launch evidence unpaired from the visible next action. | Live acceptance and detonation preflight now require every embedded missing-proof string to have a matching blocker `item`, while still allowing extra blockers for failed checks. |
| A well-shaped embedded `acceptance` summary could still drift from the acceptance report's derived readiness formulas, such as live `launch_ready=true` with `public_launch_ready=false`, or mismatched `recording_ready` versus `recording_proof_ready`. | Live acceptance and detonation preflight now recompute the same readiness formulas as the report object before central proof can support public launch readiness or cleanup trust. |
| A stale embedded `acceptance.error` string could survive beside green readiness flags, letting the central Run Record claim launch proof while still carrying the failure text from the report. | Live acceptance and detonation preflight now require embedded acceptance errors to be empty whenever launch, public launch, or recording readiness is true. |
| Embedded `acceptance.error` text could be whitespace-only or padded, creating public error proof that differed from generated report text while still passing string-shape validation. | Live acceptance and detonation preflight now require embedded acceptance errors to be either empty or trimmed non-empty text. |
| A redacted unresolved Run Record `errors[]` row could coexist with `launch_ready` or `public_launch_ready` because only `recording_ready` was checked against unresolved errors. | Live acceptance and detonation preflight now require every launch, public-launch, or recording-ready embedded acceptance flag to be false while unresolved Run Record errors remain. |
| A redacted but unresolved Run Record error could survive while `recording_contract.checks.errors_empty` still claimed the proof was clean before cleanup. | Preflight now rejects `errors_empty` drift: the green checklist must match the actual Run Record `errors` list before worker state is destroyed. |
| Unresolved Run Record error rows could carry padded source/id/detail text or duplicate source/id identities, making public failure proof differ from the generated writer's canonical rows. | Live acceptance and detonation preflight now reject padded error fields and duplicate source/id error identities before launch readiness or cleanup trust can pass. |
| Unresolved Run Record error rows could carry sidecar notes that generated error proof never emits, leaving public failure evidence dependent on private annotations. | Live acceptance and detonation preflight now reject unexpected error fields beyond `source`, `id`, and `detail` before launch readiness or cleanup trust can pass. |
| Generated Run Records could still write padded, duplicate, or blank-detail unresolved error rows, making a failed launch harder to review even though validators would later reject the malformed proof. | Run Record writing now canonicalizes unresolved error rows before persistence: source, id, and detail are redacted and trimmed, blank details get public fallback copy, and duplicate source/id rows are collapsed. |
| Live acceptance only checked whether `recording_contract.blockers` was empty, so padded, duplicated, blank, or non-string rows were not named as malformed recording proof. | Live acceptance now rejects loose recording-contract blocker rows, requiring exact non-empty string values with no surrounding whitespace or duplicates before public recording proof can pass. |
| Detonation preflight could trust a missing or malformed Run Record `errors` section as long as `errors_empty` did not drift. | Cleanup preflight now requires an explicit `errors` list and validates unresolved error rows for source/id/detail fields and public-safe text before worker destruction can proceed. |
| Detonation preflight could trust timeline proof from non-empty `steps` or `checkpoints` lists even when entries were missing id/label/status, duplicated ids, or preserved token-shaped detail text. | Cleanup preflight now validates Run Record timeline rows before worker destruction: step and checkpoint entries need id/label/status fields, unique ids, and public-safe detail/next-action/resume-hint text. |
| Run Record step or checkpoint rows could preserve padded generated fields or sidecar timeline notes while still normalizing enough to satisfy timeline identity and redaction checks. | Live acceptance and cleanup preflight now reject loose timeline rows before step/checkpoint proof can support public readiness or worker cleanup. |
| The final recording contract could mark `timeline` ready from padded step or checkpoint fields, malformed timestamps, sidecar timeline notes, or token-shaped recovery text that generated job rows never emit. | Recording readiness now rejects loose timeline proof with exact step/checkpoint row fields, trimmed id/label/status/recovery text, numeric timestamps, and secret-looking text rejection before timeline proof can make the control room recordable. |
| Generated Run Records included `updated_at` and checkpoint `mascot_state` timeline fields, but acceptance and preflight treated them as sidecars while the writer could still preserve padded timeline text from job state. | Timeline proof is now aligned end to end: the writer redacts/trims rows, collapses duplicate ids, preserves generated timestamp/mascot fields, and acceptance/preflight validate those fields instead of rejecting generated proof. |
| Detonation preflight treated truthy `pending_safe` verifier details such as strings or numbers as safe enough to destroy worker state. | Preflight now accepts only literal JSON `true` for top-level or nested `pending_safe` fields, so malformed pending verifier proof blocks cleanup. |
| A placeholder provider playbook or verifier summary could satisfy preflight section presence without proving the public demo provider set. | Preflight now requires shaped provider-playbook and verifier-summary sections with GitHub, Resend, Vercel, DNS/Cloudflare, and `live_app` coverage before cleanup can proceed. |
| The final recording contract could still mark verifier proof ready from a loose Run Record verifier summary with sidecar fields, padded verifier identities, string counters, or missing live-verifier guidance. | Recording readiness now requires the generated verifier summary schema, exact verifier/check fields, trimmed provider/check/status rows, literal integer counters, boolean pending-safe flags, and live-verifier guidance before the recording checklist can turn verifiers green. |
| Generated verifier summaries could inherit padded, malformed, duplicate, or secret-shaped verification rows and leave later validators to discover that the green live-check panel was not built from clean public proof. | Run Record generation now canonicalizes verifier rows first, trimming/redacting public provider/check/status text, deriving counts from those rows, and turning malformed or secret-shaped verifier identities into blocking `unknown` proof. |
| The Run Record recording contract could mark `human_actions` true from exact-looking Capture rows even when those rows pointed at a stale gate id or captured a token target different from the durable provider gate. | Recording readiness now binds each guided human-action row to `provider_gates.records`, and copy-once Capture rows must match the gate's declared env target before the final recording contract can pass. |
| Placeholder `human_actions` or `rehearsal_review` sections could satisfy detonation preflight section presence even though they did not prove visible control-room actions or clean rehearsal review before worker cleanup. | Detonation preflight now requires count-backed human-action traces bound to durable provider gates and rehearsal-review rows that match those actions, proof sources, and visible control-room instructions before cleanup can proceed. |
| Guided human-action rows could preserve padded gate/control text or extra sidecar notes while still normalizing enough to match provider gates and counts. | Live acceptance and detonation preflight now reject padded generated fields and unexpected sidecar fields in human-action rows before public recording or cleanup trust can pass. |
| Rehearsal-review rows could match human actions while still carrying padded visible-control text or extra sidecar review notes that the generated writer never emits. | Live acceptance and detonation preflight now reject padded reviewed-action fields and unexpected sidecar fields before clean rehearsal proof can support public recording or cleanup trust. |
| Run Record recording checks, live acceptance, and cleanup preflight kept separate human-action and rehearsal-review row vocabularies, so a new visible control or proof-source rule could pass one launch gate and fail another. | A shared rehearsal-proof contract now owns human-action fields, exact visible controls, action/count buckets, reviewed-action fields, schema versions, and proof-source mapping, with regression coverage proving all three launch gates consume it. |
| Placeholder `audit_trail` sections could satisfy detonation preflight section presence even though they did not prove credential captures, provider actions, approvals, DNS writes, or detonation categories from durable source artifacts. | Detonation preflight now validates the `fusekit.audit-trail.v1` schema, entry counts, category counts, wake-event ids, setup-receipt/audit indexes, required categories, and required source anchors before cleanup can proceed. |
| Run Record audit entries could preserve padded generated fields or sidecar audit notes while still normalizing enough to satisfy category, source, and wake-event checks. | Live acceptance and cleanup preflight now reject loose audit-trail entries before audit proof can support public readiness or worker cleanup. |
| Generated audit rows could still inherit padded status/source/provider text from receipts, vault metadata, or raw audit events, leaving public audit copy dependent on later validators to catch proof drift. | Run Record generation now canonicalizes final audit rows first, trimming/redacting public text, applying safe status/summary fallbacks, dropping unsupported categories, and deriving counts from that canonical audit ledger. |
| The final recording contract could still mark audit proof ready from a loose audit envelope with sidecar top-level fields, string counters, string receipt/audit indexes, or missing audit-first guidance. | Recording readiness now requires the generated audit-trail envelope, exact statement, literal integer entry/category counts, and integer source indexes before `recording_contract.checks.audit_trail` can turn green. |
| The final recording contract could mark `audit_trail` ready from loose central audit rows that the later live acceptance and cleanup preflight gates would reject. | Recording readiness now recomputes exact audit-row proof, including generated field names, trimmed required text, setup-receipt/audit indexes, secret-looking text rejection, and wake-event anchors before the control room can call the run recordable. |
| Placeholder `automation_boundary` sections could satisfy detonation preflight section presence even though they did not prove which provider steps FuseKit owns after a human gate. | Detonation preflight now validates the `fusekit.automation-boundary.v1` schema, ready status, complete VNC allow-list, route ownership, zero-blocked/fusekit/human-gate counts, and post-gate API/CLI versus human-gate route lists before worker destruction can proceed. |
| The final recording contract could mark `automation_boundary` ready from padded route labels or sidecar route notes that the generated Run Record writer never emits. | Recording readiness now rejects loose automation-boundary proof, including padded VNC allow-list values, padded route/provider/recipe/owner/status text, sidecar boundary/route/post-gate fields, and padded post-gate route labels before the control room can call the run recordable. |
| Run Record generation, live acceptance, and cleanup preflight still carried separate automation-boundary ready status, repair status, detonation-scope label, and statement-term literals. | A shared automation-boundary contract now owns those status/scope/statement values, with regression coverage proving all three launch gates consume the same authority. |
| Live acceptance and cleanup preflight could still accept looser automation-boundary rows than the final recording checklist, including sidecar notes, padded route text, and stringy or boolean ownership counts. | Live acceptance and cleanup preflight now reject sidecar automation-boundary fields, padded VNC/route/post-gate text, and non-integer ownership counts before post-gate ownership proof can support launch readiness or worker cleanup. |
| The generated automation boundary could inherit padded, duplicate, unsupported, or secret-shaped provider-strategy rows, making the ready post-gate ledger depend on cleanup validators to catch proof drift later. | Run Record generation now canonicalizes automation routes first, drops unsupported or secret-shaped rows from ready boundary proof, dedupes `provider:recipe` signatures, and derives counts plus post-gate route lists from that same canonical route ledger. |
| Run Record recording checks, live acceptance, and cleanup preflight each depended on automation-boundary vocabularies tied to Run Record instead of a neutral contract, so route owners or allowed post-gate route kinds could drift across launch gates. | A shared automation-boundary contract now owns envelope fields, route fields, count fields, post-gate lists, VNC allow-list values, route owners, and route-kind sets for all three launch gates. |
| Placeholder artifact or evidence sections could satisfy detonation preflight section presence even though the Run Record did not prove unique public-relative artifact rows or a count-backed non-secret evidence inventory. | Detonation preflight now rejects duplicated, absolute, traversal, credential-query, or non-boolean artifact rows and requires `fusekit.evidence-inventory.v1` logs, visual proof, receipts, matching counts, and non-secret inventory guidance before cleanup can proceed. |
| Live acceptance could validate Run Record artifact-row shape while still letting a row marked `exists: true` point at a missing file in the retrieved OCI artifact bundle. | Live Run Record acceptance now resolves every existing artifact row under the retrieved `.fusekit` bundle and rejects invented artifact files before public launch readiness can pass. |
| Detonation preflight could trust Run Record artifact rows marked `exists: true` without checking whether the referenced artifact file actually survived in `.fusekit`. | Cleanup preflight now resolves every existing artifact row under the survivor bundle and rejects invented artifact files before worker destruction is trusted. |
| The final recording contract could mark `artifacts` ready from padded artifact names/paths, sidecar artifact notes, credential-query text, or non-boolean existence values that generated artifact rows never emit. | Recording readiness now rejects loose artifact proof, requiring exact `name`/`path`/`exists` rows, public-relative paths, trimmed names, and literal boolean existence flags before artifact proof can make the control room recordable. |
| Detonation preflight could trust Run Record evidence rows marked `exists: true` without checking whether the referenced log, screenshot, visual, or receipt file actually survived in `.fusekit`. | Cleanup preflight now resolves every inventoried evidence path under the survivor bundle and rejects invented files before worker destruction is trusted. |
| The final recording contract could mark `evidence` ready from sidecar evidence notes, padded evidence paths or sources, or a loose inventory statement that the generated evidence writer never emits. | Recording readiness now rejects loose evidence inventory proof, requiring the exact schema, counts, statement, row fields, public-relative paths, trimmed sources, and `exists: true` markers before evidence proof can make the control room recordable. |
| Generated evidence rows could inherit padded or secret-shaped source labels from artifact records before the stricter recording and cleanup checks rejected the inventory later. | Run Record evidence generation now canonicalizes final inventory rows first, trimming/redacting path and source text, rejecting unsafe public-relative paths or secret-shaped sources, collapsing duplicate paths, and deriving counts from the canonical rows. |
| Live acceptance and cleanup preflight could still accept looser artifact/evidence rows than the final recording checklist, including sidecar row fields, padded survivor paths, and boolean or stringy evidence counts. | Live acceptance and cleanup preflight now reject sidecar artifact/evidence fields, padded artifact and evidence text, and non-integer evidence counts before survivor inventory proof can support launch readiness or worker cleanup. |
| Pre-detonation recording-contract blockers could be padded, duplicated, blank, or non-string while still normalizing to `detonation` as the allowed pending cleanup blocker. | Cleanup preflight now rejects loose recording-contract blocker rows, requiring exact non-empty string values with no surrounding whitespace or duplicates before worker destruction is trusted. |
| The recording contract writer, control room, live acceptance, and detonation preflight each carried their own checklist vocabularies, so a future launch-readiness field could drift across demo gates. | A shared recording-contract module now owns the ordered checklist, contract field set, and proof-section mapping; the Run Record writer, control room, live acceptance, and cleanup preflight consume that shared authority, with preflight only excluding detonation section proof before final cleanup. |
| The LLM contract writer, Run Record writer, live acceptance, and cleanup preflight each carried their own model-summary and auth-lane key sets, so the explicit inference contract could drift across demo gates. | The non-secret LLM contract module now owns the model summary, contract, security, and auth-lane key sets consumed by Run Record writing, live acceptance, and detonation preflight before model inference can support launch readiness or cleanup trust. |
| The provider playbook writer, live acceptance, and cleanup preflight each carried their own step-field set and provider-family coverage map, so deterministic setup ordering could drift across launch gates. | A shared provider-playbook contract now owns public provider families and exact generated step fields consumed by Run Record writing, live acceptance, and cleanup preflight before ordered setup proof can support public readiness or worker cleanup. |
| The Run Record verifier summary, live acceptance, and cleanup preflight carried separate verifier check/envelope shape constants, so live-verifier proof could drift between recording readiness and cleanup trust. | A shared verifier-summary contract now owns the public envelope, check-row, and count-key sets consumed by Run Record recording checks, live acceptance, and cleanup preflight before verifier proof can support public readiness or worker cleanup. |
| The Run Record audit trail, live acceptance, and cleanup preflight carried separate audit entry/envelope shape constants, so audit proof could drift between recording readiness and cleanup trust. | A shared audit-trail contract now owns the public envelope, entry-field, and required-category sets consumed by Run Record recording checks, live acceptance, and cleanup preflight before audit proof can support public readiness or worker cleanup. |
| The Run Record artifacts/evidence inventory, live acceptance, and cleanup preflight carried separate artifact and evidence shape constants, so survivor proof could drift between recording readiness and cleanup trust. | A shared evidence-inventory contract now owns artifact row, evidence envelope, evidence row, and count-key sets consumed by Run Record recording checks, live acceptance, and cleanup preflight before survivor proof can support public readiness or worker cleanup. |
| A failed Moonlite OCI run could leave a reachable runner VM when the encrypted SSH cleanup lane was unavailable, forcing operator-side cleanup from `oci_workspace.json` instead of a product path. | `fusekit runner detonate` now falls back to the recorded OCI workspace artifact, deletes the VM, boot volume, public IP, and network resources through OCI auth, and treats verified VM plus boot-volume destruction as remote-worker cleanup proof while still failing partial cloud deletion. |
| A fresh launch after a failed OCI/Cloud Shell attempt could silently resume the failed job id and carry stale failed checkpoints into the new control room. | Launch job recovery now resumes only active `created`, `running`, or `waiting` jobs with no failed steps; failed or completed attempts remain audit evidence while a new launch starts with a clean job id and checkpoints. |
| The final public recording command could be run in live mode without naming the retrieved OCI artifact bundle, letting an operator aim the recordability gate at local `.fusekit` scratch state by accident. | `fusekit acceptance run --require-recording` now fails fast unless it is paired with `--mode live` and `--remote-artifacts`, so the ready-to-record claim is always based on disposable-worker survivor evidence. |
| Static and live control-room cards still told operators to rerun live acceptance with only `--require-recording`, omitting the retrieved artifact bundle now required by the public proof gate. | Acceptance cards now tell launchers to rerun live acceptance with `--remote-artifacts` plus `--require-recording`, keeping the browser guidance aligned with the CLI's survivor-proof requirement. |
| A stale or hand-built acceptance report could still set `recording_ready=true` without proving that live acceptance used retrieved OCI survivor artifacts. | Acceptance report serialization and control-room report loading now require `remote_artifacts.loaded: ok` before effective recording proof or recordability can turn green. |
| A central Run Record could still embed `acceptance.recording_ready=true` from recording-contract proof alone, while omitting retrieved OCI survivor proof. | Run Record acceptance summaries now carry `remote_artifacts_ready`, and live acceptance plus detonation preflight recompute `recording_ready` from public launch, remote artifacts, and recording-contract proof before public recording or cleanup trust can pass. |
| A provider-gate run could carry empty `human_actions` and `rehearsal_review.reviewed_actions` sections, letting a clean rehearsal look ready because no action rows were compared. | Live acceptance, recording readiness, and cleanup preflight now require non-empty guided human-action and rehearsal-review rows whenever provider gates, wake events, or human-gate ownership prove a user-facing provider step existed. |
| Generated Run Records could still serialize `rehearsal_review.status=ready` for a provider-gate run before any guided user action was recorded, leaving validators to catch the weak proof later. | Run Record writing now marks the rehearsal review `needs_review` whenever provider gates or wake events exist but no guided human-action rows have survived, so the control room sees pending clean-rehearsal proof immediately. |
| Generated Run Records could still serialize `rehearsal_review.status=ready` when the automation boundary itself contained human-gate provider routes but no provider gate or wake event had been recorded yet. | Run Record writing now derives the clean-rehearsal requirement from the same automation-boundary `human_gate` route count it serializes, so route-led handoffs stay pending until guided control-room actions exist. |
| Generated Run Records could still preserve stale embedded acceptance readiness booleans from `acceptance/report.json`, leaving acceptance/preflight validators to catch contradictions with blockers, missing proof, errors, missing remote artifacts, or a red recording contract. | Run Record writing now recomputes embedded acceptance readiness from sanitized blockers/missing/error rows, canonical run errors, remote-artifact proof, and the generated recording contract before the central proof object is written. |
| Generated Run Records could still copy `run_state.detonation_safe=true` into `detonation.preflight_safe` even when the same Run Record's recording contract still had non-detonation blockers. | Run Record writing now aligns `detonation.preflight_safe` with the generated recording contract: cleanup preflight stays false until every non-detonation proof section is green, leaving detonation as the only acceptable pre-cleanup blocker. |
| Static or live control-room payloads could still surface stale acceptance `launch_ready` or `public_launch_ready` flags while the same report carried missing proof, blockers, or an error. | Control-room acceptance loading and live-refresh card logic now recompute those ready flags from sanitized unresolved-evidence rows and prioritize actionable blocker cards before stale launch-ready copy. |
| The live public recording gate could accept `--remote-artifacts` pointing at the app's active local `.fusekit` directory, letting local scratch state impersonate a retrieved disposable-worker survivor bundle. | Live acceptance now rejects remote-artifact paths that resolve to the app's own `.fusekit`, keeping `remote_artifacts.loaded` tied to a separate retrieved OCI artifact bundle. |
| Retrieved OCI artifact inventory could treat a directory or other non-file path named like a survivor as present, leaving later validators to discover that the artifact was not usable proof. | Live acceptance now records both path existence and regular-file presence, fails `remote_artifacts.loaded` on non-file survivors, and reports the exact placeholder before survivor proof can support recording readiness. |
| Retrieved OCI artifact inventory could follow symlinked survivor paths, letting a hand-built remote-artifacts directory borrow host-local state while looking like disposable-worker evidence. | Live acceptance now records and rejects linked survivor files before public JSON proof is read, so remote survivor evidence must be regular files from the retrieved OCI bundle. |
| The live public recording gate could accept a symlinked `remote-artifacts` directory or symlinked `.fusekit` bundle directory, letting host-local survivor files appear as retrieved OCI evidence. | Live acceptance now rejects linked remote-artifact roots and linked `.fusekit` bundle directories before path resolution, so public recording proof must come from real retrieved directories. |
| A retrieved remote `.fusekit` bundle could contain complete required survivors while also carrying extra plaintext leftovers such as dotenv files, logs, browser state, or hand-built sidecars. | Live acceptance now allowlists the top-level retrieved survivor bundle and rejects unexpected entries before `remote_artifacts.loaded` can pass, so public proof contains only encrypted/redacted survivor files. |
| OCI artifact retrieval could download a complete pre-detonation survivor set while leaving unexpected bundle entries for later live acceptance to catch. | Remote artifact validation now consumes the shared survivor allowlist and fails retrieval before detonation trust if the downloaded `.fusekit` bundle contains unexpected top-level files or directories. |
| The remote worker wrote `visual.json` for noVNC/control-room browser proof, but the SSH artifact fetch list was hand-maintained and omitted it, leaving final live acceptance to fail after an otherwise successful OCI run. | OCI artifact retrieval now builds the tar fetch list from the shared pre-detonation survivor contract and includes optional `visual.json` while still excluding post-detonation `workspace_detonation.json` until cleanup proof is copied in. |
| A reused local remote-artifacts directory could carry stale final cleanup proof such as `workspace_detonation.json`, or allowed survivor names as symlinks/directories, before the current OCI run had actually detonated. | Pre-detonation artifact validation now accepts only the pre-detonation fetch contract, rejects final-only survivor files until cleanup copies them in, and fails linked or non-file survivor placeholders before detonation trust. |
| A fresh remote artifact archive could be extracted over an older local `.fusekit` bundle, letting stale required survivors satisfy validation when the new download was partial. | Artifact extraction now clears the previous local `.fusekit` bundle after opening a readable archive and before unpacking, so only files from the current retrieval can satisfy pre-detonation validation. |
| An unsafe or empty remote artifact archive could clear the previous local `.fusekit` diagnostics before failing extraction, making recovery harder after a bad download. | Artifact extraction now validates archive paths and requires safe file members before clearing the prior local bundle, preserving the last diagnostic survivor set until a usable download replaces it. |
| A remote artifact archive could fail partway through file copy or produce only a partial survivor set after the old local bundle was cleared, leaving neither the previous diagnostics nor a complete fresh `.fusekit` bundle for launch recovery. | Artifact extraction now unpacks into a staged temporary bundle, validates the complete pre-detonation survivor set there, and swaps it into place only after copy and validation succeed, with rollback to the previous survivor bundle if replacement fails. |
| A malformed remote artifact archive could include ignored top-level leftovers, nested `.fusekit` scratch files, symlink/hardlink survivor members, or duplicate survivor file entries whose final contents depended on tar ordering before bundle validation ran. | Artifact extraction now rejects archive entries outside the pre-detonation survivor contract, non-file survivor members, and duplicate survivor file rows before staging, keeping retrieved OCI proof exact and unambiguous. |
| Final `workspace_detonation.json` proof could be copied into a missing `.fusekit` directory, through a symlinked/non-file survivor target, or be left half-written if the copy failed mid-stream. | Final detonation proof attachment now requires an existing regular retrieved `.fusekit` bundle, rejects symlinked local receipts and invalid remote survivor targets, and atomically replaces the cleanup receipt only after the full copy succeeds. |
| A stale hidden temp path for final detonation proof could be a symlink to host-local state or a directory placeholder, weakening the atomic receipt copy before the real survivor file was replaced. | Final detonation proof attachment now clears stale temp files/symlinks before exclusive writes and fails closed on temp directories, so temp-path leftovers cannot redirect or wedge the cleanup receipt handoff. |
| Final cleanup could attach `workspace_detonation.json` to the retrieved bundle while leaving remote `run_state.json` and `run_record.json` in their pre-detonation state, forcing live acceptance to discover central proof drift after the expensive run. | Final detonation proof attachment now syncs the final local run-state survivor and regenerates remote `run_record.json` after the cleanup receipt lands, so the central proof object and standalone receipt agree before live acceptance. |
| Final Run Record refresh could write directly to the retrieved `job.json`, `checkpoints.json`, or `run_record.json` survivors, so a failed proof regeneration could leave the bundle internally contradictory after cleanup. | Final Run Record attachment now rejects missing, symlinked, and non-file survivor paths, stages job, checkpoint, and Run Record proof through guarded temp files, fails closed on temp directories, and preserves the previous survivor set when regeneration fails. |
| A mixed final survivor bundle could carry `job.json` or `checkpoints.json` rows that say detonation finished while the central Run Record still shows the old step or checkpoint status. | Live acceptance now compares overlapping timeline ids across `job.json`, `checkpoints.json`, and `run_record.json`, failing `run_record.complete` with the exact drifting survivor before public readiness can pass. |
| Retrieved OCI artifact inventory could treat a zero-byte required survivor as loaded, leaving later JSON/vault validators to name the hollow proof. | Live acceptance now records `empty` status for every survivor and fails `remote_artifacts.loaded` on zero-byte required files, while allowing empty `gate_events.jsonl` for no-wake-event runs. |
| Generated Run Records could mark artifact rows as existing when the path was a directory placeholder rather than a regular survivor file, leaving acceptance/preflight to catch weak proof later. | Run Record artifact generation now counts only regular files as `exists: true`, so directory placeholders are recorded as non-proof before the central product object is written. |
| Run Record drift checks could silently skip a standalone artifact comparison when the artifact path existed but was a directory, unreadable file, or malformed JSON. | Live acceptance now requires readable regular JSON objects for standalone comparison artifacts before central Run Record drift proof can pass, so non-file verifier, strategy, detonation, readiness, or LLM-contract placeholders fail `run_record.complete`. |
| Detonation preflight could collapse an unreadable or non-object public JSON survivor into empty proof, producing indirect downstream failures instead of naming the bad cleanup artifact. | Cleanup preflight now adds labeled parser failures for required JSON survivors before worker destruction can be trusted, so malformed verifier, strategy, readiness, visual, gate, receipt, rollback, Run Record, LLM-contract, or worker-replacement artifacts are explicit. |
| A blank `audit.jsonl` file could satisfy survivor file presence and public-safety parsing even though it proved no credential capture, provider action, approval, or cleanup event rows. | Live acceptance and cleanup preflight now require at least one JSON object row in the audit log before redacted audit proof can support public readiness or worker destruction. |
| Central Run Record wake events could carry sidecar summary or row fields while still matching `gate_events.jsonl` after normalized signature comparison, hiding stale human-gate metadata until cleanup preflight. | Live acceptance now validates the central `wake_events` envelope and every wake row against the shared generated shape before Run Record readiness, rejecting sidecars, padded event fields, malformed counts, and bad timestamps up front. |
| The central Run Record `state` section could be looser than raw `run_state.json`, preserving sidecar fields, stringy booleans, or padded recovery notes after the standalone survivor passed exactness checks. | Live acceptance now validates embedded `state` with the generated launch-state contract before Run Record readiness, matching the raw survivor bar while still requiring `detonation_safe` and `workspace_detonated` to be true for public launch proof. |
| Cleanup preflight could still trust a looser central Run Record `state` section than live acceptance, allowing worker destruction after sidecar state fields or stringy run-state flags that the launch gate would reject. | Detonation preflight now validates embedded `state` with the same generated launch-state contract before cleanup trust, while preserving pre-cleanup semantics where `workspace_detonated` only has to be an explicit boolean. |
| The central Run Record envelope could carry extra top-level proof fields or malformed timestamps while every nested section still looked valid. | The Run Record writer now owns the exact top-level field set, and both live acceptance and cleanup preflight require non-negative `created_at`/`updated_at` proof before public launch or worker destruction can proceed. |
| Cleanup preflight could see a top-level Run Record key and skip section validation when proof-bearing sections were empty placeholders such as `{}` or `[]`. | Detonation preflight now requires generated proof sections such as vault, provider routes/playbook, runner profile, worker replacement, artifacts, evidence, verifiers, human actions, rehearsal review, and automation boundary to be non-empty before cleanup trust. |

## Open Acceptance Items

- Finish a full live Moonlite RSVP run where Cloudflare DNS, Resend domain/API key,
  Resend audience/from email, Vercel env vars, GitHub secrets, deployment, and live
  URL health all pass or are pending-safe without side-channel instructions.
- Record a clean new-site/new-account rehearsal and require the
  `rehearsal_review` Run Record proof to show every human action matched the
  control-room instructions before launch readiness is claimed.
